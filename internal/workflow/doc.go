// Package workflow will implement the YAML workflow registry (strict decode,
// validation, global/project scopes, live reload) and the step template
// engine (spec §8; T2.1–T2.2).
package workflow
