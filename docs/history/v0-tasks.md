# vincent v1 — Task Breakdown & Progress

Derived from [spec.md](../spec.md). This file is the single source of truth for
implementation progress; the executing agent updates it in place as work proceeds.

## How to update this file (rules for the agent)

- Statuses: `- [ ]` not started · `- [~]` in progress · `- [x]` done (append `✓ YYYY-MM-DD`) · `- [!]` blocked (append one-line reason).
- Mark a task `[~]` **before** starting it; mark `[x]` only when every done-criterion is verified (tests actually run, not assumed).
- Update the dashboard table in the same edit as any status change.
- Order within a phase implies dependency unless a `Depends:` tag says otherwise. Don't start a task whose dependencies aren't `[x]`.
- Never delete a task. Descoped: strike through (`~~T1.4~~`) with a dated note. Newly discovered work: append to the phase with the next free ID.
- No time estimates are tracked — only status.

## Dashboard

| Phase | Scope | Done | Status |
|---|---|---|---|
| 0 — Scaffolding | 4 tasks | 4/4 | ✅ done |
| 1 — Spine (M1) | 9 tasks | 9/9 | ✅ done |
| 2 — Workflow engine (M2) | 12 tasks | 12/12 | ✅ done |
| 3 — TUI (M3) | 13 tasks | 13/13 | ✅ done |
| 4 — Polish (M4) | 23 tasks | 23/23 | ✅ done (**M4 acceptance met — T4.6 ✓**. Every task needing a human was signed off by the owner on 2026-08-12: T4.1 and T4.21 by the logon walk, T4.4 by the quickstart read, T4.17 by the codex reasoning capture) |
| 5 — Cursor adapter (M5, post-v1) | 9 tasks | 9/9 | ✅ done (**M5 acceptance met 2026-08-12** — real-CLI legs recorded on Windows and macOS, Linux covered by CI) |
| **Total** | | **70/70** | |

---

## Phase 0 — Scaffolding

Repo foundation the spec assumes but doesn't itemize. Module path: `github.com/lezli01/vincent`.

**Phase 0 decisions (grill session, 2026-08-06):**

- *Go version:* latest stable minor, minor-only directive (`go 1.26`); CI reads `go-version-file: go.mod`; bumped manually each Go release. No multi-version matrix.
- *CLI framework:* `spf13/cobra` for the §12.1 command tree.
- *Task runner:* Mage, zero-install. Both `go run mage.go <target>` (committed bootstrap `mage.go`) and `go tool mage <target>` work; mage is pinned via a `go.mod` `tool` directive so tidy can never prune it. Wherever T0.2/T0.4 say `make X`, read `go run mage.go X`.
- *Lint:* golangci-lint v2 pinned as a `go.mod` `tool` directive — `go tool golangci-lint run` is the single invocation path locally and in CI (no golangci-lint-action). Curated strict set (v2 standard defaults + errorlint, gocritic, revive, copyloopvar, intrange, misspell, unconvert, unparam, nolintlint, sqlclosecheck) + `gofumpt` formatter.
- *CI:* runs the same mage targets as local; `-race` enabled in CI only (local Windows may lack a C toolchain); docs-only `go.mod` gating removed; `checkout`/`setup-go` bumped to v7 in-branch (supersedes the dependabot PRs; the lint-action bump becomes moot); PR runs cancel superseded runs.
- *Skeleton:* placeholder packages carry a `doc.go` stating their future role; `internal/cli` and `internal/version` are real from T0.1.
- *Repo hygiene:* `.gitattributes` forces LF on source files (scope addition to T0.2). `vincent version` prints one line (`vincent version X (commit Y, built Z)`) with `debug.ReadBuildInfo` fallback so plain `go build` binaries never print empty fields.
- *Local toolchain:* Go installed via winget (1.26.5 at time of writing); no other global tools required — lint and mage ride the `go.mod` tool directives.

- [x] **T0.1 — Go module & repo layout.** `go mod init github.com/lezli01/vincent`; directory skeleton: `cmd/vincent/`, `internal/{config,store,daemon,api,workflow,agent,worktree,scheduler,taskrun,tui,cli}/`; root command routing (`vincent`, `vincent daemon`, `vincent version` stubs); build info injected via ldflags. (§12.1) ✓ 2026-08-06
  *Done when:* `go build ./...` succeeds; `vincent version` prints version/commit/date. *(Verified: `vincent version 9ad1de4-dirty (commit 9ad1de4, built 2026-08-06)`; also added `internal/version` with a unit test.)*
- [x] **T0.2 — Dev tooling.** `.gitignore`, `.editorconfig`, `golangci-lint` config, Makefile (or magefile) with `build` / `test` / `lint` targets. ✓ 2026-08-06
  *Done when:* `make lint test build` passes clean locally. *(Verified as `go run mage.go lint test build` — 0 lint issues, tests pass, binary built. Scope addition: `.gitattributes` LF normalization.)*
- [x] **T0.3 — CI.** GitHub Actions: build + test + lint matrix on ubuntu-latest, macos-latest, windows-latest. ✓ 2026-08-06
  *Done when:* workflow green on all three OSes for a PR touching Go code. *(Verified: PR #6 matrix green on all three OSes. CI runs the same mage targets as local, with `-race`.)*
- [x] **T0.4 — Phase gate.** Fresh clone → `make build` → working `vincent version` on all 3 OSes (via CI matrix). ✓ 2026-08-06
  *Done when:* gate verified; dashboard updated. *(Verified on PR #6: each matrix job does a fresh checkout, `go run mage.go build`, and smoke-tests `vincent version` output.)*

## Phase 1 — Spine (M1)

Milestone acceptance (§19 M1): via `curl` alone — register a repo, create a one-step agent task, watch it finish, see the branch and diff.

**Phase 1 decisions (grill session, 2026-08-07):**

- *Delivery:* 4 grouped PRs along dependency seams — (1) T1.1+T1.2 config+store, (2) T1.3+T1.4 daemon+API, (3) T1.5+T1.6 projects+worktrees, (4) T1.7–T1.9 adapter+run+gate. Each independently green in CI; tasks.md updated per PR.
- *Git access:* git CLI only (no go-git) — `exec` via one internal `gitx` helper (run/capture/error-mapping). git version logged at daemon start; warn (not fail) below 2.31.
- *Platform dirs:* `os.UserConfigDir()` for config; hand-rolled ~30-line data-dir resolver (`XDG_DATA_HOME`/`~/.local/share`, `%LOCALAPPDATA%`, `~/Library/Application Support/vincent/data`). `VINCENT_CONFIG_DIR` / `VINCENT_DATA_DIR` env overrides (used by tests and the T1.9 gate). No xdg dependency.
- *YAML:* `goccy/go-yaml` everywhere (strict decode now; its line/column-annotated errors are what T2.1 needs later).
- *Config bootstrap/reload:* first start writes a fully-commented `config.yaml` with §12.3 defaults. Invalid config at startup = fatal; invalid on hot-reload = keep last-good + log rejection; `listen:` changes ignored until restart (warn). fsnotify watches the config *directory* (survives rename-on-save), ~100 ms debounce.
- *SQLite:* `modernc.org/sqlite` via `database/sql`; WAL + `busy_timeout` via DSN pragmas; `MaxOpenConns(1)` (single connection — zero SQLITE_BUSY surface; revisit only if profiling demands).
- *Migrations:* hand-rolled embedded runner — `embed.FS` of `NNNN_name.sql`, applied in-transaction at startup into the spec §14 `schema_migrations` table. Up-only; no library.
- *Store:* hand-written repository-style `database/sql` code (no sqlc/ORM); queries as consts beside their methods; covered by CRUD round-trip tests.
- *Daemon lifecycle:* detached start = self-exec `vincent daemon` with platform `SysProcAttr` (DETACHED_PROCESS / setsid). Single-instance via `gofrs/flock` on `daemon.lock` (auto-releases on death). **Spec addition:** authenticated `POST /v1/daemon/stop` endpoint — `daemon stop` calls it, waits for exit, `--force` kills as fallback; same graceful path (§12.4) on all 3 OSes. `token` + `daemon.json` written atomically (temp+rename); `daemon.json` removed on graceful shutdown.
- *Token perms:* 0600 enforced + tested on POSIX; Windows relies on `%LOCALAPPDATA%` per-user ACL inheritance (documented; no DACL code in v1) — test asserts existence/location there.
- *Logging:* `slog` TextHandler everywhere (file + foreground stderr); rotation via `natefinch/lumberjack`.
- *API:* stdlib `net/http` ServeMux with Go 1.22+ method/wildcard patterns (no chi); middleware chain auth → log → recover; constant-time token compare (`crypto/subtle`); `/v1/health` exempt; CORS disabled; stable `snake_case` error codes.
- *default_branch detection:* `origin/HEAD` → local `main` → local `master` → current HEAD branch → reject registration with clear error (detached/unborn HEAD requires explicit `default_branch`).
- *Worktree slug edge:* title sanitizing to empty → branch `vincent/{id}` (no trailing dash); otherwise §5.3 rules.
- *Agent CLI location:* **spec addition** to §12.3 — `agents: { claude: { path }, codex: { path } }`; empty = `exec.LookPath`. `Detect()` reports the resolved path in `/v1/info`. Tests/gate substitute the fake via this knob (no PATH surgery).
- *Fake agent:* committed `cmd/fakeagent` main package — scenario-driven (success / error-event / nonzero-exit / hang-until-killed / big-usage) via env/flag, emits Claude-style stream-json, prompt from stdin. Always compiled by `./...`; excluded from release packaging in T4.5; grows a codex dialect for T2.9.
- *Transcripts:* agent stream-json written verbatim (lossless/replayable); vincent's own annotations as namespaced `{"type":"vincent.…"}` JSONL lines. Parsing is tolerant — unknown event types transcripted but not normalized; exact CLI flags pinned at implementation time (§9.2).
- *M1 task shape:* `workflow` param optional (default `"adhoc"`); daemon synthesizes a real one-step workflow YAML (agent step; prompt = fixed template embedding title+description) stored as `workflow_snapshot` — DB rows shape-final; T2.3 swaps synthesis for registry lookup.
- *M1 admission:* interim FIFO (`created_at`) runner bounded by global `max_parallel_tasks`; priority ignored until T2.5 replaces it wholesale. Real agents cost money — no unbounded spawn even in M1.
- *Diff endpoint:* `git -C {worktree} diff <merge-base(base, HEAD)>` — committed + staged + unstaged tracked changes; untracked files excluded (documented limitation).
- *M1 gate:* `scripts/m1-gate.sh` (bash + curl + jq) run in CI on all 3 OSes (Git Bash on Windows): build vincent+fakeagent → temp dirs via env overrides → start daemon → register temp repo → create task → poll to done → assert branch/diff/transcript → stop daemon.

**Agent/model/effort selection decisions (grill session, 2026-08-07; spec §8.6, §9.6):**

- *Discovery:* adapter `Options()` probes the installed CLI ad hoc — claude: `--help` parse yields the `--effort` enum + model aliases (verified against 2.1.224); codex: `--help` enumerates nothing (only `-c key=value`, verified against 0.142.5) — merged with a curated per-adapter catalog; every option provenance-tagged `cli`/`curated`; free text always accepted.
- *Effort scale:* adapter-native strings, no normalized vincent scale (claude `low…max`, codex `minimal…high`). Known-invalid (agent, model/effort) pairs are validation errors; values unknown to the catalog warn and pass through — the CLI is the final authority at run time.
- *Selection surfaces & precedence:* workflow `defaults` + per-step fields (existing) + new optional task-level override on `POST /v1/tasks`; resolution `step > task override > defaults > adapter default` — explicit step pins always win (cross-agent workflows stay intact).
- *Inheritance:* agent-scoped — `model`/`effort` never leak across an agent switch; they reset to the new adapter's default unless set alongside.
- *Freshness:* `GET /v1/agents` cache keyed by binary identity (resolved path + mtime + version) — never stale by construction since help output is a pure function of the binary; `?refresh=true` re-probes.
- *Phasing:* interfaces shaped final in phase 1 (T1.7: `RunSpec.Effort`, `--model`/`--effort`, `Options()` probe; T1.8: override params + `*_override`/StepRun columns); catalog endpoint + resolution/validation engine as new T2.11; codex probe in T2.9; TUI pickers in T3.5. M1 gate unchanged — the override is optional.

**Agent input-request decisions (grill session, 2026-08-07; spec §7.4):**

- *Trigger:* structured, machine-readable requests only — `question` (AskUserQuestion-style, options/multi-select/free text) and `permission` (restricted-mode prompt); nothing inferred from output text.
- *State:* new `awaiting_input`, **holds** its concurrency slot (live agent process idle mid-step); `answer` human action returns it to `running` in place; `cancel` valid from it.
- *Clocks:* step `timeout` pauses while waiting; new `input_timeout` (global default 24h in config `defaults`, overridable in workflow `defaults`/per step); expiry kills the process and fails the attempt (`input_timeout`) under normal retry/blocked policy.
- *API:* normalized `InputRequest` (kind, questions/options/multiSelect, adapter-native payload in `raw`) stored as task `pending_input`; durable `task.awaiting_input` event (kind + summary; full request via task GET); `POST /v1/tasks/{id}/answer` (409 outside `awaiting_input`). *(Amended by the PR F grill: no separate `task.awaiting_input` event type — the kind + summary ride `task.state_changed`'s payload, per PR D's one-event-per-transition rule; see the PR F block.)*
- *Opt-out:* `on_input: wait | deny` (workflow `defaults` + per step, default `wait`); `deny` = adapter auto-answers ("no user available — best judgment" / permission denied) so unattended workflows never stall.
- *Capability:* per-adapter `supports_input` — claude yes via bidirectional stream-json (`--input-format stream-json`, stdin kept open; control-message wire format is undocumented → pinned per detected CLI version, fixture-tested, degrades to `supports_input: false` if unverifiable); codex `exec` is strictly non-interactive → never enters `awaiting_input`. Unparseable control request = attempt fails `input_protocol_error`, never hangs.
- *Alerts:* TUI-level only (board pins needs-attention tasks + badge, header attention count, terminal bell on `task.awaiting_input`); OS desktop notifications stay post-v1.
- *Recovery/metrics:* crash while `awaiting_input` = `interrupted` (request discarded, fresh re-run may re-ask); request+answer transcripted as `vincent.*` lines; wait time in new `input_wait_ms` StepRun column, excluded from durations.
- *Phasing:* interface shape final in T1.7 (`InputRequest`/`InputResponse` types, `Respond()` on RunHandle, `RunSpec.OnInput`, `supports_input` in Detect — claude may report `false` until the engine lands); full engine as new T2.12 (incl. `pending_input_json`/`input_wait_ms` migration); TUI in T3.2/T3.4; fake agent grows an ask-question scenario.

- [x] **T1.1 — Config & platform dirs.** Platform-native config/data dir resolution (§12.2); `config.yaml` load with defaults + strict validation (§12.3); hot-reload via fsnotify. ✓ 2026-08-07
  *Done when:* unit tests cover defaults, overrides, invalid config rejection, and live reload. *(Verified: 28 unit tests — defaults, partial/full overrides, 13 invalid-config rejections, live reload incl. invalid-edit keep-last-good, per-OS data-dir table, generated default file loads back to `Default()`.)*
- [x] **T1.2 — SQLite store & migrations.** Pure-Go driver; WAL + busy_timeout; embedded migrations applying schema §14; typed store layer (CRUD + scheduler query) for projects, tasks, step_runs, events. ✓ 2026-08-07
  *Done when:* migration idempotence test + CRUD round-trip tests pass on all 3 OSes in CI. *(Verified: PR #7 matrix green on ubuntu/macos/windows — idempotence via re-migrate + reopen, pragma checks, CRUD round-trips incl. nullable columns, scheduler ordering, FK enforcement.)*
**T1.3/T1.4 decisions (grill session, 2026-08-07):**

- *Start handshake:* `daemon start` first probes for a healthy daemon (already running = success), then spawns the detached child and polls `daemon.json` + `GET /v1/health` for ~10 s; on failure it prints the tail of `daemon.log` and exits non-zero. No pipe IPC.
- *Status truth:* try-acquire `daemon.lock` — acquirable means no daemon (a leftover `daemon.json` is reported as stale); held means alive, then `/v1/health` decides responsiveness. Exit codes: 0 healthy · 1 not running · 2 alive-but-unresponsive. Human-readable output; `--json` deferred to T4.2.
- *Idempotency:* desired-state semantics — `start` on a running daemon and `stop` with none running both print what happened and exit 0.
- *Stop flow:* `daemon stop` POSTs `/v1/daemon/stop` (202; response completes before shutdown begins), then waits for the lock to become acquirable (~20 s, longer than the daemon's own kill grace); `--force` falls back to killing the recorded PID.
- *Foreground = daemon:* `vincent daemon` is the real daemon in both modes — always takes the lock, writes token/`daemon.json`, always logs to the rotating file; foreground additionally mirrors logs to stderr. Log level rides a `slog.LevelVar` so `log_level` applies on hot-reload; a `listen` change is warned and the effective value kept.
- *Middleware order:* recover outermost → request log → auth → handler (panics always logged and enveloped as 500; unauthorized requests still logged). `/v1/health` is auth-exempt and logged at debug so poll loops don't spam the log.
- *Error vocabulary (initial):* `unauthorized`, `not_found`, `method_not_allowed`, `invalid_json`, `validation_failed`, `invalid_state`, `internal`; ServeMux fallback 404/405 wrapped so every error body is the §13.1 envelope (405 carries `Allow`).
- *`/v1/info` (initial):* version/commit/built, pid, `started_at`, uptime, effective listen address, `max_parallel_tasks`; agent availability joins in T1.7 (additive).
- *`/v1/config`:* effective config as snake_case JSON mirroring `config.yaml`; durations rendered as Go duration strings.
- *Token:* 32 bytes `crypto/rand` as hex, written atomically (temp+rename) 0600 at first start, reused unchanged on later starts.

- [x] **T1.3 — Daemon lifecycle.** `vincent daemon` (foreground); `daemon start/stop/status` (detached start, graceful stop, status via `daemon.json`); single-instance lock file; bearer token generated 0600 at first start; structured logging with rotation. (§12.1, §12.2, §13.1) ✓ 2026-08-07
  *Done when:* start/status/stop cycle works on all 3 OSes; second daemon refuses to start; token/`daemon.json` created with correct permissions. *(Verified: PR #10 matrix green on ubuntu/macos/windows — binary e2e drives the full start/status/stop cycle incl. idempotency and exit codes; in-process tests cover second-instance refusal, API stop, context cancel, fatal invalid config; token 0600 asserted on POSIX, existence on Windows.)*
- [x] **T1.4 — API foundation.** `net/http` server bound `127.0.0.1`; bearer-auth middleware (health exempt); JSON error envelope with stable codes; `GET /v1/health`, `/v1/info` (agent availability wired later), `/v1/config`. (§13.1–13.2) ✓ 2026-08-07
  *Done when:* auth rejection, health-without-auth, and error-shape tests pass. *(Verified: PR #10 — httptest suite covers auth rejection (missing/wrong token), health without auth, 404/405/401/500 envelope shapes incl. Allow header, panic recovery, and the stop endpoint with and without auth.)*
**T1.5/T1.6 decisions (grill session, 2026-08-07):**

- *Repo shape:* strict toplevel only — registered path must equal `git rev-parse --show-toplevel`; bare repos and linked worktrees rejected; a subdirectory is rejected with an error naming the real toplevel (explicit beats magic).
- *Path identity:* absolute path required (client cwd ≠ daemon cwd); stored cleaned as-given; duplicates rejected at the handler by identity comparison (`os.SameFile` — sees through symlinks and Windows case aliases) against all existing projects, error names the existing project. No DB unique on `path` (a string constraint can't see aliases). Default-name collision on `name` UNIQUE → `validation_failed` suggesting an explicit name.
- *default_branch:* local branches only — every detection candidate and explicit value must resolve under `refs/heads/`; `origin/HEAD`'s short name counts only if a local branch of that name exists, else fall through (`main` → `master` → current HEAD branch → reject). Invariant: a stored `default_branch` always resolves locally at write time.
- *PATCH:* pointer-field JSON — absent = unchanged, explicit `null` clears (`default_workflow`, `max_parallel_tasks`); `null` on required fields is `validation_failed`. Path repoint re-runs full registration validation + duplicate check, and the effective `default_branch` (stored, or supplied in the same PATCH) must resolve in the new repo.
- *DELETE:* hard delete with explicit cascade in one tx (events → step_runs → tasks → project; FKs stay strict). Guard without `force`: 409 `invalid_state` with the non-archived count. `?force`: 409 if any task `running`; otherwise archives each non-archived task force-removing its worktree (force *is* the dirty confirmation), then cascades. Branches never deleted. Transcript-file cleanup joins in T1.8.
- *Dirty rule:* untracked files count — dirty = `git status --porcelain` non-empty, matching `git worktree remove`'s own refusal; force maps to `--force`. Remove is idempotent: worktree dir already gone → prune + succeed.
- *Create residue:* prune-then-fail — `git worktree prune` before `worktree add`; if the target dir still exists non-empty, typed `worktree_path_occupied` error. Nothing deleted without an explicit force.
- *Error surfacing:* one vocabulary — `worktree.Error.Reason` snake_case strings (`base_branch_missing`, `branch_exists`, `worktree_dirty`, `worktree_missing`, `worktree_path_occupied`, `project_path_missing`, `git_error`) double as future task `block_reason` values; HTTP layer reuses the existing code set (400 `validation_failed`, 409 `invalid_state`, 404 `not_found`) — no new codes.
- *gitx:* `internal/gitx` run/capture helper with typed exec errors; context-scoped timeouts (30 s queries, 5 min worktree ops); git version probed and logged at daemon start, warn below 2.31 (phase 1 decision, lands here as first git use).

- [x] **T1.5 — Projects API.** `GET/POST /v1/projects`, `GET/PATCH/DELETE /v1/projects/{id}`; registration validation (path exists, is git repo); `default_branch` auto-detection with fallback (§5.1); delete guarded by non-archived tasks. ✓ 2026-08-07
  *Done when:* endpoint tests against temp git repos cover happy path + each validation failure. *(Verified: PR #11 matrix green on ubuntu/macos/windows — happy path, every registration failure (relative/nonexistent/non-repo/bare/subdir/linked-worktree/branch-missing/zero-cap), alias-duplicate via os.SameFile, name conflict, full detection-chain table incl. detached/unborn rejection, PATCH null-clears + repoint matrix, DELETE guard/force matrix with real worktree removal.)*
- [x] **T1.6 — Worktree manager.** Create worktree + branch `vincent/{id}-{slug}` from base (§10); slug rules (§5.3); remove + prune on archive with dirty detection and `force`; error taxonomy for missing base branch, pre-existing branch, missing project path (§18). ✓ 2026-08-07
  *Done when:* integration tests with temp repos cover create/remove/dirty/each error case. *(Verified: PR #11 — lifecycle (branch survives removal, idempotent re-remove), slug/branch-name tables incl. empty-slug edge, all taxonomy errors, untracked + tracked dirty, project-gone force fallback, outside-root deletion refusal. New `internal/gitx` with version probe logged at daemon start.)*
**T1.7–T1.9 decisions (grill session, 2026-08-07):**

- *Adapter layout:* `internal/agent` = interface + shared types + adapter registry; `internal/agent/claude` = the Claude adapter (stream parsing, help probe, curated catalog, fixtures). Codex lands as `internal/agent/codex` in T2.9.
- *Stream granularity:* message-level only — no `--include-partial-messages`. Live tail updates per assistant turn; token-level deltas revisited at T3.3 if the tail feels chunky (additive flag + parser change).
- *Permission modes:* both mapped in T1.7 — full-auto = `--dangerously-skip-permissions`; restricted = `--allowedTools` with a minimal curated set (Read, Glob, Grep, Edit, Write, MultiEdit, `Bash(git:*)`) — no test runners in v1's restricted set (they execute arbitrary project code anyway); denials degrade per §9.4 until T2.12.
- *Kill:* tree-kill from day one — POSIX `Setpgid` + process-group kill; Windows Job object with KILL_ON_JOB_CLOSE + TerminateJobObject (auto-reaps the tree if the daemon dies). Shared helper; reused by T2.4 timeout-kill and T2.6 cancel.
- *Detect():* resolves path (config knob → `exec.LookPath`) + parses `claude --version`; `logged_in` is `*bool` and claude reports nil ("unknown") in v1 — no state-file parsing; `supports_input` false until T2.12. `Options()` stays an uncached ad-hoc probe (binary-identity caching belongs to T2.11's endpoint).
- *M1 retry:* none — the synthesized adhoc snapshot pins `max_retries: 0`, so first failure → `blocked` (snapshot stays truthful to actual behavior; T2.4 owns the real retry loop and the §8.4 failure block).
- *M1 timeout:* `defaults.agent_timeout` enforced via context deadline on the run; expiry tree-kills and blocks with reason `timeout` (T2.4 generalizes to per-step/per-type).
- *M1 stop/crash interim:* graceful stop cancels run contexts (tree-kill) and marks live step_runs `interrupted` + their tasks `blocked` (block_reason `interrupted`); a startup sweep applies the same to leftover `running` rows (crash case). No auto re-run until T2.8 replaces the sweep with real re-queue semantics.
- *Override validation (M1):* `agent` must name a registered adapter (only `claude` exists) — else `validation_failed`; `model`/`effort` are free text passed through verbatim, catalog warnings arrive with T2.11.
- *Worktree timing:* branch name computed at task creation (`branch_name` NOT NULL); worktree created at run start — a queued task that never ran leaves no worktree (`worktree_path` nullable, matching schema §14).

- [x] **T1.7 — AgentAdapter + Claude Code adapter.** Interface per §9.1 (incl. `Options()`, `RunSpec.Model`/`Effort`, and the §7.4 input-request surface shaped final: `InputRequest`/`InputResponse` types, `Respond()` on RunHandle, `RunSpec.OnInput`, `supports_input` in `Detect()` — claude may report `false` until T2.12 lands the engine); Claude implementation: prompt via stdin, `stream-json` event parsing, full-auto flag, `--model`/`--effort` passthrough, usage/cost extraction, kill support; `Options()` help-probe (effort enum + model aliases parsed from `--help`, merged with the curated catalog, §9.6); `Detect()` wired into `/v1/info`. Include a **fake agent** test binary emitting scripted stream-json so CI never calls a real API. ✓ 2026-08-07
  *Done when:* adapter unit tests against the fake binary cover success, error event, nonzero exit, kill, and usage parsing; options probe parses a captured `--help` fixture and falls back to curated-only on an unparseable one; the input-request interface compiles and is stubbed (behavior exercised in T2.12). *(Verified: PR #13 matrix green on ubuntu/macos/windows — 28 adapter/probe tests incl. a tree-kill test proving a spawned grandchild dies with the process tree (`internal/procx`: POSIX process groups, Windows Job objects). Spec addition: `RunHandle.PID()` for §12.4 journaling.)*
- [x] **T1.8 — Minimal task run.** `POST /v1/tasks` (validation, snapshot placeholder, immediate queue, optional `agent`/`model`/`effort` task-level override persisted to the `*_override` columns via a new migration) with a hardcoded single-agent-step execution path: worktree create → adapter run (agent/model/effort resolved per §8.6, recorded on the StepRun) → StepRun + transcript file → `done`/`blocked`; `GET /v1/tasks[,/{id}]`, `/steps`, `/steps/{run_id}/transcript`, `/diff` (§13.2). Depends: T1.2, T1.5–T1.7. ✓ 2026-08-07
  *Done when:* end-to-end test with fake agent produces correct DB rows, transcript file, diff output; an override round-trips into StepRun `model`/`effort`. *(Verified: PR #13 — e2e suite drives POST→done through real store/worktrees/runner/fakeagent, asserting step rows (model/effort override round-trip, tokens, cost, exit code), transcript contents + ranged reads, merge-base diff, branch creation; failure paths agent_error / nonzero_exit / timeout / stop-interrupt each land in `blocked` with the right reason. Migration 0002 adds the override + step model/effort columns.)*
- [x] **T1.9 — Phase gate (M1 acceptance).** Scripted curl flow per §19 M1 against the fake agent in CI; run once manually with real `claude` on Windows and record the result here. ✓ 2026-08-07
  *Done when:* script committed and green in CI; manual run noted. *(Verified: `scripts/m1-gate.sh` green in the PR #13 CI matrix on all three OSes. Manual run 2026-08-07: `VINCENT_GATE_AGENT=claude scripts/m1-gate.sh` with real claude 2.1.224 on Windows 11 — GATE PASS: register → task → done → branch/step/transcript/diff asserted, real agent edited README.md in the worktree.)*

## Phase 2 — Workflow engine (M2)

Milestone acceptance (§19 M2): a multi-step workflow (gate + command publish) runs unattended to the gate; an agent question round-trips awaiting_input → answer → resume; `kill -9` mid-step recovers correctly; caps honored under load.

**Phase 2 decisions (grill session, 2026-08-07):**

- *Delivery:* 7 PRs along dependency seams — **A** T2.1+T2.2 (registry + templates), **B** T2.3+T2.4 (FSM + step executors; the T1.8 interim admission loop stays alive), **C** T2.5+T2.6 (scheduler + human actions), **D** T2.7+T2.8 (SSE endpoints + crash recovery), **E** T2.9+T2.11 (codex + catalog/resolution), **F** T2.12 (input requests), **G** T2.10 (M2 gate). Durable event *writes* land early in B (the FSM is the only thing that changes state); only the SSE *endpoints* wait for D.
- *Engine shape:* actor per admitted task — one goroutine walking the snapshot steps, sole writer of that task's state. Grows `internal/taskrun` (existing daemon wiring) rather than a new package; `internal/workflow` (registry + templates) and `internal/scheduler` (admission) fill the reserved stubs.
- *Actor lifetime = one admission:* parking at `awaiting_gate`/`blocked`/`paused` persists state, releases the slot, and exits; approve/retry/skip/resume re-queue and a fresh actor resumes at `current_step`. `awaiting_input` is the sole exception — slot held, live process, actor stays (§7.4).
- *FSM:* pure `internal/taskstate` package (State type + transition table + `Can(state, action)`, exhaustively tested); `store` aliases the constants. Persistence is a compare-and-swap `TransitionTask(id, from, to, …)` with `WHERE state = ?`; zero rows → `ErrStateConflict` → HTTP 409 carrying the current state. No transition anywhere bypasses it.
- *Events:* `internal/events` broker. State change + `events` row in **one** SQLite transaction; publish to subscribers only after commit, row id = SSE `id:`. Live output never touches the table — per-task subscribers only; a slow live-output subscriber drops chunks, a slow state subscriber is disconnected (it resumes losslessly).
- *SSE:* `Last-Event-ID` → replay `id > cursor` from the table, then attach to the broker — subscription registered *before* the replay query so nothing slips through the seam (dedupe by id). `?types=` / `?project_id=` filtered server-side. 15 s `:heartbeat` comment. Per-task stream coalesces live output on a ~100 ms flush timer (§13.3's ~10 Hz); catch-up = fetch transcript, then follow.
- *Scheduler:* single goroutine — the only place `queued → running` happens, so caps can't race. Ordering + both caps in SQL (global count plus per-project counts; a project at its cap is skipped while the queue walk continues). Woken by any task state change, config reload, and project PATCH; 5 s safety tick. `awaiting_input` counts toward both caps.
- *Retry accounting:* `step_runs.attempt` is monotonic per (task, step_index) — history stays append-only and `{step_index}-{attempt}.jsonl` never collides. The retry *budget* counts consecutive failed attempts since the last human action (`retry` resets it, §6); `interrupted` runs are excluded entirely (§7.2). `.LastFailure` carries the previous *failed* attempt; the §8.4 failure block is appended for agent steps on any attempt > 1.
- *Templates:* `Option("missingkey=error")` — a typo'd field fails the step before spawn instead of rendering a hole into the prompt. Optional fields use `{{with}}`/`{{if}}`, as the §8.1 example already does.
- *Step rows for non-process steps:* a manual step writes a `running` step_run on entry (emitting `gate.waiting`) and terminalizes to `approved`/`rejected` on the human action, so gate wait time shows in the timeline; `skip` writes/terminalizes a `skipped` row. Every step index a task passes through has at least one row.
- *Shells:* POSIX `/bin/sh -c`; Windows probes `pwsh` at daemon start, falls back to `powershell`, logs the resolution once, re-probes on config reload. A step's `shell:` pin overrides; a pinned shell that isn't installed fails the step before spawn with reason `shell_unavailable` — never a silent fallback.
- *Action delivery:* engine keeps `map[taskID]*liveRun` (control channel + cancel func + RunHandle). Handlers validate via `taskstate`, persist with the CAS, then deliver; a missing live run is normal (task parked or already exited), not an error. `cancel` cancels the run context (procx tree-kill, 10 s grace, §6); `pause` sets a flag read at the next step boundary; `answer` forwards to `RunHandle.Respond`.
- *Crash recovery:* PID + `proc_started_at` journaled before spawn; on startup every `running` step_run → `interrupted`, and the process is tree-killed only when the PID exists **and** its start time matches (guards PID reuse). Owning task → `queued` at the same `current_step`, retry budget untouched; `awaiting_input` treated identically with `pending_input` cleared. Replaces T1.8's `SweepInterrupted`, which blocks instead of re-queuing.
- *`adhoc`:* becomes a **built-in registry entry** at lowest precedence (a real file named `adhoc` in either scope shadows it), so creation is one uniform path, `workflow` stays optional, and `scripts/m1-gate.sh` passes untouched. Keeps `max_retries: 0` so an ad-hoc run fails fast rather than doubling agent spend. **Spec addition** to §8.
- *Selection (§8.6):* pure resolver in `internal/agent`; catalog validation runs at workflow load (T2.1) *and* at `POST /v1/tasks` with the task override applied — known-invalid pair = 400 `validation_failed`, unknown value accepted with a warning. Resolution itself happens at step start and the resolved triple is written to the step_run.
- *edit+retry:* rewrites that step's `prompt`/`run` inside `tasks.workflow_snapshot` (the snapshot stays the single execution truth, §5.3), plus a migration adding `step_runs.prompt_override TEXT` and `step_runs.run_override TEXT` so the timeline can flag an edited attempt. **Spec addition** to §14. *(Amended by the PR C grill: two columns, not one — the wire body is `{ prompt_override?, run_override? }` and a command override stored under a column named `prompt_override` reads as a bug. The handler cannot write these directly; see the PR C block below.)*
- *Clocks:* real clock, shrunk durations in tests — timeouts are `context.WithTimeout` on real processes, so a fake clock buys mock fidelity, not determinism. Exception: T2.12's `input_wait_ms` accounting takes a `now func() time.Time` so the arithmetic is assertable without sleeping.
- *M2 gate:* new `scripts/m2-gate.sh` beside `m1-gate.sh` (which keeps guarding the spine) — scenario 1 multi-step workflow (agent → command → manual gate → command push to a local bare remote) with an input-request round-trip; scenario 2 hard-kill mid-step + recovery; scenario 3 cap stress. Windows hard-kill uses `taskkill /F /PID` (Git Bash `kill -9` can't reliably kill a native process); the script branches on `$OSTYPE`.

**PR C decisions (T2.5 + T2.6; grill session, 2026-08-07):**

- *Package seam:* `internal/scheduler` owns the admission goroutine, both caps, the ordering, **and** the only `queued → running` CAS in the codebase — the "caps can't race" guarantee above only holds if the CAS is inside the scheduler. It hands admitted tasks to `taskrun.Runner.Admit`, which spawns the actor and never blocks; `Runner.admit`/`loop`/`tickInterval` are deleted. `Admit` returns an error, and the scheduler applies `Interrupt` (`running → queued`) on failure, so a shutdown can never orphan a `running` row with no actor.
- *Wake plumbing:* one post-commit callback on the `Store`, fired by `TransitionTask` after the transaction commits, wired by the daemon to `scheduler.Wake`. Same shape the T2.7 broker needs ("publish only after commit"), so PR D rewires that single line to `broker.Publish` and the scheduler becomes a subscriber. Config reload and project PATCH wake explicitly.
- *Cap counting:* the slot-holding set from `taskstate.HoldsSlot`, never a hand-written `IN (…)`. `store`'s task-state constants become genuine aliases of `taskstate`'s — the two lists had already drifted (`store` was missing `awaiting_input` entirely), and a third copy must not become possible. `awaiting_input` exists for the scheduler in this PR even though its endpoint arrives in T2.12.
- *Admission query:* one query returns queued tasks in `priority DESC, created_at ASC`, each row carrying its project's slot count and cap; Go walks it maintaining live tallies, skipping capped projects while continuing. SQL cannot see its own in-flight admissions, so the incremental tally is Go by necessity — caps and ordering still live in SQL.
- *Startup sweep:* `SweepInterrupted` is hoisted out of `taskrun.Runner.Start` to an explicit daemon-startup call ahead of both components. Neither the scheduler nor the runner owns crash recovery, and T2.8 then replaces one call site instead of operating inside a package.
- *Action logic:* methods on `taskrun.Runner` (it already owns the live map, the step-row helpers and `Worktrees`); `internal/api` stays a transport shim — decode, call, map typed errors to 409/400/404.
- *Pause durability:* `tasks.pause_requested` is a persisted column. Engine transitions never clear it; every human action **except** `pause` clears it (each is a human saying "go"). A `queued` task carrying the flag is not admitted — the scheduler transitions it `queued → paused` and clears. Pause therefore survives a hard kill (which T2.8 re-queues) and a stale flag can never ambush a later admission.
- *Cancel:* handler CASes to `aborted`, then signals the live run's cancel func. The actor terminalizes its **own** step_run row as `interrupted` with `failure_reason: canceled` (new `ReasonCanceled`); when no actor is live — `queued`, `blocked`, `paused`, or `awaiting_gate` with its open manual row — the handler terminalizes it, same state and reason. §5.4 gains no `canceled` step state. Losing the race to admission returns 409 with the current state and **no** internal retry: silently converting "cancel a task that hasn't started" into "kill a live process" is a different act the human never saw.
- *Kill grace:* §6's "graceful term, then kill after 10 s" does not exist today — `procx` has only `Kill()` (SIGKILL / `TerminateJobObject`) and `steps.go` wires `runCtx.Done()` straight to it. PR C adds `Proc.Terminate()` — `SIGTERM` to the process group on POSIX; on Windows it falls through to `TerminateJobObject`, documented, since a Job has no graceful tree-term primitive and §6's `taskkill /T /F` is the same thing. Wired into the **cancel path only**: `Terminate` → 10 s → `Kill`. The step-timeout path keeps hard-killing (a step that blew its budget had its chance); T2.8 adopts the same helper for shutdown. One kill primitive, three callers, three PRs.
- *Retry budget reset:* `tasks.retry_cursor_at`, stamped by the `retry` handler and passed straight into the `since` parameter `store.CountStepAttempts` already takes.
- *edit+retry write path:* the handler holds the override text while the task is `blocked` — the next `step_run` does not exist yet, and the actor is sole writer of step-run rows. Bridge is `tasks.pending_override_json`: the handler rewrites `workflow_snapshot` **and** stashes `{"prompt":…,"run":…}`; the actor drains it onto the next attempt's two columns and clears it in the same write. Only the attempt the human actually edited is flagged, not every later auto-retry of that step — accurate, since the edit happened at a point in time even though the snapshot keeps it forever.
- *Gate/skip row mechanics:* from `awaiting_gate`, terminalize the open `running` row (`approved`/`rejected`/`skipped`) — no new row. From `blocked`, the `failed` row stays and `skip` appends a fresh `skipped` row at `attempt = Last+1`. `approve` and `skip` advance `current_step` in the **same CAS** as the state change (`TaskChange.CurrentStep`); `reject` holds the cursor and blocks with `block_reason: rejected`. Skipping the final step needs no special case: `current_step = len(steps)` → `queued` → the actor is admitted, the step loop body never executes, and it falls through to `complete`.
- *Archive:* 409 + `invalid_state` with `details.reason: worktree_dirty`, matching the project-delete precedent's status and code. `force` accepted in the body (§13.2) **and** as `?force` (symmetry with `DELETE /v1/projects/{id}`, and `hasForce` already exists). Worktree removal must succeed **before** the transition — unlike project delete the record survives here, and an `archived` task pointing at a live worktree is a lie; a removal failure is 409 and the task stays `done`/`aborted`.
- *Priority:* conditional `UPDATE … WHERE id = ? AND state IN ('queued','paused')`, zero rows → 409 with the current state, then a durable `task.priority_changed` event and an explicit scheduler wake. A reorder never touches `TransitionTask`, so without this it would fire neither — and a TUI watching a queue silently reorder under it is worse than one more event type.
- *Error envelope:* grows an `omitempty` `details` object — `{"error":{"code","message","details":{"state":"running"}}}` — so §13.1's "409 carrying the current state" is machine-readable instead of prose a client must regex. Additive: every existing response stays byte-identical. **Spec addition** to §13.1.
- *Task response:* gains `available_actions` (from `taskstate.HumanActionsFrom`, written and tested in T2.3 and called by nothing until now) and `pause_requested`. Both derived per response; the M3 TUI never reimplements the §6 table.
- *Migration 0003:* `tasks.pause_requested`, `tasks.retry_cursor_at`, `tasks.pending_override_json`, `step_runs.prompt_override`, `step_runs.run_override`. **Spec addition** to §14.
- *Scope:* PR C ships `cancel`, `pause`, `resume`, `retry`, `skip`, `approve`, `reject`, `archive` and `PATCH {priority}`. Not `/answer` (T2.12 owns the bidirectional stream, `pending_input` and the wait accounting) and not the SSE endpoints (T2.7). No stub routes — an unimplemented `/answer` gets the router's existing 404 rather than a 501 clients might learn to special-case.
- *Testing:* scheduler tests run against a **real store** (the caps and ordering are SQL — a fake store would test nothing) with a stub `Admitter` that blocks on channels: no processes, fully deterministic, proving the global cap, the per-project cap, capped-project-skipped-while-the-walk-continues, and `priority DESC, created_at ASC`. One `taskrun` fake-agent integration test proves the real seam wires up.
- *Commit order:* docs first — `spec.md` §6, §11, §13.1, §13.2, §14 plus the two amended decision bullets above — so the rest of the diff reads against a correct spec.

**PR D decisions (T2.7 + T2.8; grill session, 2026-08-08):**

- *Event vocabulary completed:* `task.created` written in the same transaction as the task INSERT; `project.created`/`project.updated`/`project.deleted` in-tx at the store layer — the events table has no FKs on `task_id`/`project_id`, so `project.deleted` is appended after the delete cascade in the same tx and survives it; `workflow.registry_changed` from the registry's change callback; `daemon.shutting_down` at stop request. `task.archived` is **dropped** (spec §13.3 amendment): archive already emits `task.state_changed` with `to: archived` through `TransitionTask`, and a second row per archive would break one-event-per-transition.
- *Broker:* `internal/events` — the daemon rewires `store.SetEventHook` to `broker.Publish` (post-commit, writer goroutine, non-blocking); the scheduler becomes an internal non-droppable func subscriber behind the same `WakeOn` filter. SSE subscribers ride buffered channels: a slow state subscriber is disconnected (it resumes losslessly via `Last-Event-ID`), a slow live-output subscriber drops chunks.
- *Cold start:* `GET /v1/events` without `Last-Event-ID` is live-only from now — no history replay (clients snapshot via REST, then follow). With a cursor: replay `id > cursor` from the table, subscription registered before the replay query, dedupe by id.
- *Per-task stream:* durable task events + live output interleaved; `Last-Event-ID` resumes the durable events only, live-output catch-up stays transcript-then-follow. Coalescing is one SSE event per chunk with `Flush` gated at ~100 ms (~10 Hz): ordering across mixed event types comes free and there is no array envelope for clients to parse.
- *Live-output tap:* `taskrun.Deps` gains the broker; agent events publish from the adapter event loop (`agent.output`/`agent.tool_use`/`agent.usage`), command/check lines from the output scanner (`command.output` with phase+stream), mirroring the transcript shapes.
- *Middleware:* `statusWriter` gains `Unwrap()` so `http.ResponseController` reaches the flusher through the logging wrapper; 15 s `:heartbeat` comment; `?types=`/`?project_id=` filters stay in SQL (`ListEvents` already takes them).
- *Recovery mechanics:* the bulk `SweepInterrupted` (which bypassed the `TransitionTask` CAS — a pre-existing T1.8 interim) is replaced by per-task recovery through the FSM's `Interrupt` action (`running → queued`, `awaiting_input → queued`, both already in the table): events fire, the scheduler wakes, the retry budget is untouched (`interrupted` runs are excluded, §7.2). Step rows terminalize `interrupted` with reason `interrupted`. Command steps gain the same PID + `proc_started_at` journaling agent steps already have (§12.4 says any step process). No migration — every column exists.
- *PID-reuse guard:* hand-rolled `procx` start-time reader via `x/sys` (already in the module graph): Windows `OpenProcess`+`GetProcessTimes`, Linux `/proc/<pid>/stat` field 22 + btime, macOS `sysctl KERN_PROC_PID`. Kill only when the PID exists **and** its start time is within ±5 s of the journaled spawn time — covers tick/btime conversion error while making a reuse collision practically impossible. Orphan kill: POSIX signals the process group (`Setpgid` makes pgid = pid); on Windows `KILL_ON_JOB_CLOSE` usually reaped the tree with the daemon, so a live match gets a belt-and-braces terminate.
- *Shutdown order:* stop request → `daemon.shutting_down` → scheduler stops → runner Terminates live trees and waits ≤15 s for actors to persist `running → queued` (visible on SSE) → Kill stragglers → broker close (streams end) → HTTP drain → exit. The step-timeout path keeps hard-killing. `cli` `stopTimeout` 20 s → 30 s: 15 s grace + drain no longer fit under 20.
- *Testing:* T2.7 — reconnect-resume proving zero missed state events across a dropped connection; coalescing observed on the per-task stream; vocabulary coverage per emitter. T2.8 — daemon-binary e2e: fakeagent hang scenario, hard-kill (`taskkill /F` on Windows, SIGKILL elsewhere), restart, assert orphan gone + task re-queued at the same step + runs to done (assertions converge on outcomes, so Windows' job auto-reap needs no branch); unit guard tests with real sleeper processes (PID dead → no kill; alive + match → tree-killed; alive + mismatch → left alone); graceful path in-process (Terminate honored inside the grace).

- [x] **T2.1 — Workflow registry.** Strict YAML decode with unknown-key rejection (§8.1–8.2); validation with file/line errors; global + project scopes with shadowing (§5.2); fsnotify reload keeping valid entries when one file is broken; `GET /v1/workflows`, `POST /v1/workflows/validate`. ✓ 2026-08-07
  *Done when:* table-driven validation tests + scope-shadowing and broken-file-isolation tests pass. *(Verified: 23-case validation table incl. per-type field rejection and located line numbers, scope shadowing across builtin/global/project, broken- and unparsable-file isolation, duplicate-name-in-scope, project re-point and removal, live-reload tests covering create/edit/break/delete, a directory created after startup, a project registered after startup, and non-YAML files not churning the registry. Spec additions recorded: built-in scope in §5.2, entry shape in §13.2.)*
- [x] **T2.2 — Template engine.** Context assembly per §8.4 (`.Task`, `.Project`, `.Workflow`, `.Step`, `.Steps`, `.Worktree`, `.LastFailure`); render-fails-before-spawn; command/check env vars (§8.5); retry failure block appending (§8.4). ✓ 2026-08-07
  *Done when:* unit tests cover every context variable, missing-field failure, and the retry block. *(Verified: every §8.4 variable asserted individually, strict-render failures for struct-field typo / map-key typo / unknown root, the defensive `index` idiom, all ten §8.5 env vars, and the failure block incl. 200-line truncation. Spec §8.1/§8.4 updated: `missingkey=error` means an optional field reads `{{ with index .Task.Fields "ticket" }}`.)*
- [x] **T2.3 — Task state machine.** Full FSM §6 with persisted transitions (incl. `awaiting_input`, §7.4), guarded invalid transitions (409), workflow snapshot at creation replacing T1.8's placeholder, `block_reason`, timestamps. ✓ 2026-08-07
  *Done when:* exhaustive transition-table test (every state × every action, incl. `answer`) passes. *(Verified: `internal/taskstate` holds the table; the test restates §6 independently and walks all 9 states × 16 actions, plus slot/terminal/human-action classification. Persistence is `store.TransitionTask` — compare-and-swap on the from-state with the `task.state_changed` event written in the same transaction; a conflicted transition writes neither row. Task creation now snapshots the registry entry. The 409 surface itself arrives with T2.6's action endpoints — `StateConflictError` already carries the current state for it.)*
- [x] **T2.4 — Step executors.** Agent step (any adapter; agent/model/effort resolved per §8.6 via T2.11), command step (platform shell selection + `shell:` pin, §8.3), manual gate, `check` execution, per-step timeouts with kill, retry loop honoring `max_retries`, transcript capture for all output. (§7) ✓ 2026-08-07
  *Done when:* integration tests cover each step type, check pass/fail, timeout kill, retry-then-blocked, and Windows + POSIX shells in CI. *(Verified: engine integration tests drive the real runner against real repos and the fake agent — multi-step agent→command run with §8.5 env, manual gate parking (row at entry, actor exits), retry-then-blocked with per-attempt transcripts, check failure recording `check_exit_code`, timeout tree-kill, template error failing before spawn, `.Steps` results flowing into a later step, and the §8.4 failure block reaching the retry prompt. Scripts are written per platform so the same tests exercise sh and pwsh in CI. §8.6 resolution has its own precedence/inheritance table test; T2.11 moves it into `internal/agent` and adds catalog validation.)*
  *Carried forward:* the retry budget currently counts every failed attempt of a step; `store.CountStepAttempts` takes a `since` cursor so T2.6's human `retry` can reset it. In-process interruptions now re-queue the task (§12.4), but the startup sweep is still T1.8's blocking one until T2.8.
- [x] **T2.5 — Scheduler.** Global + per-project caps counting the slot-holding states (`running` + `awaiting_input`, per `taskstate.HoldsSlot`); admission by priority DESC, created_at ASC; re-evaluation on every state change and config reload (§11). ✓ 2026-08-07
  *Done when:* concurrency tests prove both caps and ordering under simultaneous task load. *(Verified: `internal/scheduler` owns the loop, both caps and the only `queued → running` CAS; `taskrun.Runner` implements its `Admitter`. Tests run against a real store — the caps are SQL — with a stub Admitter blocking on channels, covering ordering, the global cap, the per-project cap, both counting pre-existing slot holders, capped-project-skipped-while-the-walk-continues, `awaiting_input` holding a slot while `awaiting_gate`/`blocked`/`paused` do not, a pending pause parking instead of admitting, and a refused handoff re-queuing rather than orphaning a `running` row. Re-evaluation arrives through one post-commit event hook on the Store, which T2.7's broker will publish from; config reload and project PATCH wake explicitly.)*
- [x] **T2.6 — Human actions API.** `cancel` (process kill incl. Windows tree-kill), `pause` (step-boundary), `resume`, `retry` with prompt/run overrides recorded, `skip`, `approve`, `reject`, `archive` (+dirty `force`), `PATCH` priority (§6, §13.2). ✓ 2026-08-07
  *Done when:* each action tested from every valid state plus one invalid-state 409 each. *(Verified: actions are methods on `taskrun.Runner`, which owns the live runs they reach and the step_run rows they close; `internal/api` maps them onto HTTP. The action table test derives its cases from `taskstate` itself rather than listing them, so every action × every valid state is covered by construction, and each action is also driven from an invalid state at both layers — 409 with `details.state` over HTTP. Side effects covered individually: deferred pause, the pause flag cleared by every other action, gate rows closed in place vs. a fresh `skipped` row from blocked, `approve`/`skip` advancing the cursor in the same CAS while `reject` holds it, skipping past the last step, the retry cursor, snapshot rewriting plus a single-drain pending override, override/step-type mismatches, cancel closing rows a departed actor left open, archive removing the worktree before transitioning and refusing a dirty one without force, and priority emitting `task.priority_changed`. §6's 10 s cancel grace is newly real: `procx.Terminate`.)*
  *Carried forward:* `/answer` and both SSE endpoints are deliberately unrouted (404) — T2.12 and T2.7 own them. The step-timeout path still hard-kills; T2.8 adopts `Proc.Terminate` for graceful shutdown.
- [x] **T2.7 — Events & SSE.** Durable state events in `events` table; `GET /v1/events` with `Last-Event-ID` resume and type/project filters; per-task stream with live agent/command output, coalesced ~10 Hz (§13.3); transcript-then-follow catch-up documented behavior. ✓ 2026-08-08
  *Done when:* reconnect-resume test misses zero state events; output streaming test shows coalescing. *(Verified: `TestEventsResumeMissesNothing` drops the connection mid-stream, commits events while disconnected, resumes via `Last-Event-ID` and asserts the exact id sequence across the replay-to-live seam; `TestTaskStreamCoalescesOutput` pushes 50 chunks and counts flushes well below one per chunk with order preserved. Vocabulary completed: `task.created`/`project.*` in-tx, `workflow.registry_changed` from the registry hook, `daemon.shutting_down` at stop request; `internal/events` broker replaces the direct scheduler-wake hook.)*
- [x] **T2.8 — Crash recovery.** PID + start-time journaling before spawn; startup orphan detection/kill; `interrupted` runs re-queued without consuming retries; graceful shutdown (admission stop, 15 s term-then-kill) (§12.4). ✓ 2026-08-08
  *Done when:* test kills the daemon hard mid-step and asserts full recovery; graceful path tested. *(Verified: `TestCrashRecoveryE2E` runs the real binary, SIGKILLs it mid-agent-step, restarts, and asserts the journaled process is gone, the task re-ran from the same step with no retry consumed, and finished done; `recover_test.go` covers the guard — dead PID tolerated, matching start time tree-killed, mismatched start time spared; `TestStopGracefulRequeuesRunningTask` covers the graceful path. Command steps now journal PIDs too; `taskrun.Recover` replaces the bulk sweep and goes through the FSM per task.)*
**PR E decisions (T2.9 + T2.11; grill session, 2026-08-08):**

- *Codex invocation (pinned against codex-cli 0.142.5 on this machine):* `codex exec --json`, prompt piped via stdin (no prompt argument), cwd via `cmd.Dir`. Full-auto = `--dangerously-bypass-approvals-and-sandbox`; restricted = `--sandbox workspace-write` (closest analog of claude's allowlist; the linked-worktree `git commit` caveat is documented in the adapter — vincent never needs commits). Model via first-class `-m` (**spec §9.3 amendment** — the flag exists as of 0.142.x); effort via `-c model_reasoning_effort=`. No `--ephemeral` (codex sessions aid post-mortems, mirroring claude keeping its own), no `--skip-git-repo-check` (worktrees are git repos).
- *Fixtures from reality:* success / tool-use / failure JSONL captured from real `codex exec --json` runs on 0.142.5 (approved spend). Failure capture came free: an account/config mismatch produced genuine `error` + `turn.failed` lines. Event mapping: `agent_message` → output; `item.started` for command/tool/file/web items → tool_use; `turn.completed` → usage + terminal result (result text = last `agent_message`, `input_tokens` verbatim like claude, cost nil); `turn.failed`/`error`/error-items → error; stream end without a terminal event = error result, mirroring claude.
- *Codex curated catalog:* efforts `minimal, low, medium, high, xhigh` (**spec §9.3 amendment** — the operator's own config runs codex at `xhigh`; omitting it would make a working value a claude-only "error"). **No curated models**: live probing proved model ids are account-dependent (the same id accepted on one plan, 400 on another), so the catalog never advertises a model that might be rejected — free text + CLI default only.
- *Known-invalid rule (cross-catalog, **spec §8.2 amendment**):* value ∈ resolved adapter's catalog = valid; value only in *another* adapter's catalog = validation error (claude's `sonnet`/`max` on a codex step); value in no catalog = warning, pass through. Efforts and models treated uniformly.
- *Probe policy:* one `CatalogCache` in `internal/agent`, keyed by binary identity (resolved path + mtime; version recorded). Only `GET /v1/agents` (and `?refresh=true`) ever probes; validation paths (registry load, `/validate`, `POST /v1/tasks`) read cache-when-primed else curated — never a subprocess. Merge is monotonic (probe only adds to curated), so verdicts can soften but never harden. Daemon primes the cache asynchronously at startup.
- */v1/info freshness:* availability served from the same cache (stat-check per request, re-probe only on binary change) — installing/upgrading a CLI is visible without daemon restart, and the two endpoints can't disagree. Replaces the startup-snapshot interim.
- *Warnings surface (**spec §13.2 amendment**):* structured `warnings[]` beside `errors[]` on registry entries and the validate response; `warnings[]` strings on the task-creation 201 body; slog warn at registry load. All additive (`omitempty`).
- *Resolver move:* `agent.Resolve` over neutral per-level structs — `internal/agent` imports neither `workflow` nor `store`; `taskrun` adapts its types and deletes its local copy.
- *Fake agent codex dialect:* selected by argv shape (first arg `exec`), `--version` prints `codex-cli 0.142.5 (fake)`, scenarios emit the captured JSONL shapes.
- *No migration, no new events:* every column and event type PR E touches already exists.

- [x] **T2.9 — Codex adapter.** Per §9.3, behind the same interface; model/effort via `-m` / `-c model_reasoning_effort=`; `Options()` serves the curated catalog (the CLI enumerates nothing); nil cost handling; fake-binary tests mirroring T1.7. Depends: T1.7 (interface only — can run parallel to T2.1+). ✓ 2026-08-08
  *Done when:* same test matrix as the Claude adapter passes. *(Verified: the claude matrix mirrored against the fakeagent codex dialect (argv-sniffed `exec`) — buildArgs full-auto/restricted/`-m`/`-c` mapping, Detect version parse + missing binary, success/turn.failed/nonzero-exit/kill/context-cancel/Respond-rejected — plus fixture-driven stream tests pinned against JSONL captured from real codex-cli 0.142.5 runs (success, tool-use, failure incl. advisory error items). Engine e2e drives a codex step through the real runner: resolved triple on the step row, prompt via stdin, tokens from turn.completed, nil cost.)*
- [x] **T2.11 — Agent option catalog & selection resolution.** `GET /v1/agents` (§9.6): per-adapter availability + provenance-tagged model/effort options; cache keyed by binary identity (resolved path + mtime + version) with `?refresh=true`; probe-failure fallback to curated with `probe_error`. §8.6 resolution engine (step > task > defaults > adapter default, agent-scoped inheritance) consumed by T2.4; catalog validation wired into T2.1 workflow checks and `POST /v1/tasks` (§8.2: known-invalid = error, unknown = warning). Depends: T1.7, T2.9. ✓ 2026-08-08
  *Done when:* resolution table-tests cover every precedence and inheritance case incl. agent-switch resets; cache test proves invalidation on binary swap and a hit otherwise; known-invalid pair rejected while a free-text model passes with a warning. *(Verified: the §8.6 table moved with the resolver into `internal/agent` (neutral Level structs; taskrun keeps a wrapper-mapping test) incl. all agent-switch resets; `CatalogCache` tests prove a hit, mtime-swap re-probe, `?refresh` force, missing-binary degrade to curated with `probe_error`, and a never-probing `Catalogs()` view via probe counters. Cross-catalog rule table: own = valid, other adapter's = error naming the owner, unknown = warning; `xhigh` valid on both. Wired end-to-end: workflow Parse locates findings at the supplying level and dedupes defaults, the validate endpoint returns `warnings[]`, POST /v1/tasks 400s a codex-only effort on a claude step and creates with a warning for an unknown model, and /v1/info rides the same cache. Interface additions recorded: `Adapter.Path()` + `Adapter.Curated()`.)*
**PR F decisions (T2.12; grill session, 2026-08-08):**

- *Wire protocol (pinned against claude 2.1.226 via a capture-first spike on this machine, ~$0.12 spend):* input mode = `--input-format stream-json` + `--permission-prompt-tool stdio` (undocumented flag — the SDK's `canUseTool` maps to it; it is also what makes AskUserQuestion exist in `-p` mode); the prompt becomes one `{"type":"user",…}` JSONL line on a kept-open stdin. Requests: `control_request`/`can_use_tool` — `tool_name: AskUserQuestion` → `question` (labels from `options[].label`, `multiSelect` honored), any other tool → `permission`. Responses: `control_response`/`success` wrapping `{"behavior":"allow","updatedInput":input+answers}` (answers keyed by question text; arrays for multi-select) or `{"behavior":"deny","message":…}`. Deny-mode canned answer for questions = `updatedInput.response` free text (verified live: the agent proceeds on its own); permission deny = behavior deny (verified: tool_result error, agent continues). Full-auto: regular tools bypass the callback, questions still arrive; restricted: allowlisted tools auto-approve, every other tool falls through as a permission request — §9.4's hard denial becomes a §7.4 request when input is live. **Spec §9.2 rewritten** with the pinned facts.
- *Version gate:* `supports_input` = claude version in `[2.1.0, 3.0.0)`; unparseable → false. A ceiling because enabling the protocol changes invocation for the whole run (prompt-as-JSONL): a major bump degrades visibly to today's non-input behavior instead of risking every claude step; the pin moves only with re-captured fixtures.
- *Deny is engine-side (spec §7.4 wording amended):* the adapter always emits `EventInputRequest` when the protocol is live; the engine resolves `on_input` — `deny` calls `Respond()` with the canned answer and never transitions; `wait` transitions. One policy point, one translation path shared with human answers; `RunSpec.OnInput` stays on the interface for adapters that need CLI-level mapping.
- *FSM (spec §6 amended):* new engine action `input_closed`: `awaiting_input → running`, fired when a wait ends without an answer but execution continues — `input_timeout` expiry, agent process death with retries remaining, or a `control_cancel_request` withdrawing the pending request. Retries exhausted keep `Fail → blocked`.
- *No `task.awaiting_input` event (spec §13.3 amended; phase-1 bullet above amended):* entering the state is `task.state_changed` with `to: awaiting_input` carrying `{kind, summary}` via `TaskChange.EventPayload` — one event per transition, PR D's rule holds.
- *`input_timeout` is per request:* each `awaiting_input` entry starts a fresh window (the staleness of *this* question is what an operator cares about); `input_wait_ms` accumulates the attempt's total and takes a `now func()` for assertable arithmetic. The step clock becomes an actor-managed timer over `context.WithCancelCause` (a baked-in `WithTimeout` deadline can't pause): stopped on `awaiting_input` entry, resumed with the remaining budget on answer; cancel causes distinguish `timeout`/`input_timeout`/`canceled` in `classifyAgent`.
- *`pending_input` invariant store-enforced:* `TransitionTask` sets `pending_input_json` via `TaskChange.PendingInput` on `request_input` and unconditionally NULLs it on any transition whose from-state is `awaiting_input` — "non-null iff `awaiting_input`" lives in one place; T2.8's recovery clear comes free. Migration 0004 adds `tasks.pending_input_json` + `step_runs.input_wait_ms` (shapes already in spec §14).
- */answer validates strictly against the pending request:* every pending question answered (keyed by text), single-select exactly one value, multi-select one-or-more, values free text (options are suggestions, never an enum); `permission` requires `allow` and rejects `answers`; mismatch = 400 `validation_failed` naming the offense; wrong state = 409 from the FSM CAS. Delivery: the handler CASes `awaiting_input → running` through `humanAction` (pause flag cleared by the PR C every-action-but-pause rule), then hands the `InputResponse` to the actor over a new answer channel on `liveRun` — only the actor touches `Respond()`; answer/death/timeout races serialize on the CAS.
- *Protocol errors:* unknown `control_request` *subtypes* and unparseable `can_use_tool` payloads fail the attempt `input_protocol_error` (never hangs, §18); unknown *top-level* stream types stay tolerated `EventUnknown` (existing rule). New reasons: `input_timeout`, `input_protocol_error`.
- *Fake agent:* three scenarios mirroring the captured shapes — `ask-question` (emits a real-shape control_request, blocks reading stdin for the control_response; doubles as the timeout and deny vehicle), `ask-permission` (allow/deny round-trip), `bad-input-request` (malformed control request). All reusable by T2.10's gate script.
- *Verification:* claude stream fixtures distilled from the spike captures (codex JSONL precedent); CI stays fake-agent-only; one manual full-stack round-trip with real claude recorded in the T2.12 note below. Config catches up with spec §12.3: `defaults.input_timeout` (24h default, strict-parse addition).

- [x] **T2.12 — Interactive input requests (§7.4).** Claude adapter bidirectional stream-json input (stdin kept open, `--input-format stream-json`; question/permission control messages normalized to `InputRequest`; `Respond()` translation back; wire protocol pinned against the detected CLI version with fixtures, degrading to `supports_input: false` when unverifiable); `awaiting_input` FSM wiring (slot retained, step clock paused, `input_wait_ms` accounting); `input_timeout` enforcement; `on_input: wait|deny` resolution (deny = adapter auto-answer, no state change); migration adding `pending_input_json` + `input_wait_ms`; `POST /v1/tasks/{id}/answer`; durable `task.awaiting_input` event; request/answer transcript lines; fake-agent ask-question scenario (blocks until answered on stdin). Depends: T1.7, T2.3, T2.4, T2.6, T2.7. ✓ 2026-08-08
  *Done when:* fake-agent test drives request → `awaiting_input` (slot held, clock paused) → answer → resume → done; `input_timeout` expiry fails the attempt with the right reason; deny mode auto-answers with no state change; unparseable control request fails `input_protocol_error`; answer in wrong state returns 409. *(Verified: wire protocol captured live from claude 2.1.226 (`--permission-prompt-tool stdio` + prompt-as-JSONL; fixtures in `internal/agent/claude/testdata/`); the whole claude adapter matrix now runs through input mode by default (the fake reports 2.1.224) with version-gate degrade tests; Respond goldens cover single/multi answers, the canned response, and permission allow/deny. Engine e2e drives request → `awaiting_input` (pending_input on the task, `task.state_changed` carrying `{kind, summary}`) → answer → resume → done with `input_wait_ms` > 0 and `vincent.input_request`/`vincent.input_response` transcript lines; `input_timeout` expiry blocks with reason `input_timeout` through the new `input_closed` transition; deny mode auto-answers both kinds without ever leaving `running`; `bad-input-request` is killed rather than waited on and blocks `input_protocol_error`; wrong-state answers 409 with `details.state`, mismatched bodies 400 naming the offense. Manual run 2026-08-08: real claude 2.1.226 (haiku) on Windows 11 through the full stack — AskUserQuestion parked the task, curl answered "Blue", the run resumed in place and wrote color.txt, done; `input_wait_ms=1492`. Interface additions recorded: `agent.EventInputCanceled` + `InputResponse.Response`.)*
**PR G decisions (T2.10; grill session, 2026-08-08):**

- *Script shape:* one `scripts/m2-gate.sh` (m1-gate conventions: bash + curl + jq, `hostpath`, env-override dirs), one shared build, then **per-scenario isolation** — each scenario gets fresh config/data/repo dirs and its own daemon start/stop, so a leftover task can never distort another scenario's assertions. `FAKEAGENT_SCENARIO` is daemon-env-bound, which forces the per-scenario daemon anyway. `VINCENT_GATE_SCENARIO=1|2|3` runs one scenario for local debugging.
- *Scenario 1 diff source:* fakeagent's `ask-question` scenario now honors `FAKEAGENT_EDIT_FILE` after the answer arrives (mirroring `success`) — the published branch carries agent-made work, and the same workflow doubles for the manual real-`claude` leg (the prompt tells the agent to ask, then write the answer to the file). Second question via `FAKEAGENT_ASK_MULTI=1` so `/answer` multi-select is exercised over the wire.
- *Scenario 1 workflow:* global scope (`{config_dir}/workflows/`), agent (ask + edit) → command (`git add -A && git commit`) → manual gate → command (`git push publish {{.Task.BranchName}}` to a `git init --bare` remote). Asserts: `pending_input` shape + `available_actions`, strict `/answer` round-trip, resume, `input_wait_ms > 0`, `vincent.input_request`/`input_response` transcript lines, gate row `approved`, branch + agent-edited file present in the bare remote.
- *SSE in the gate:* light replay assert only — after completion, `curl -N` `/v1/events` with `Last-Event-ID: 1` and a max-time, grep the replay for `task.state_changed` `to: awaiting_input` carrying `{kind, summary}`. Cursor 1, not 0: ids start at 1 and a 0/absent cursor is live-only by design (PR D cold-start rule), so 1 is the earliest real resume point. Proves the durable contract over real HTTP; reconnect/coalescing stay Go-tested (T2.7).
- *Scenario 2 (hard-kill):* adhoc task, `FAKEAGENT_SCENARIO=hang` + `FAKEAGENT_SPAWN_CHILD=1`; the child PID is read from the transcript (unknown-type events are transcripted verbatim); daemon killed hard (`taskkill //F //PID` on Windows, `kill -9` elsewhere), restarted with `FAKEAGENT_SCENARIO=success`. Asserts: orphaned child dead post-restart, attempt-1 `interrupted` + attempt-2 `succeeded` on the same step, task `done`. adhoc pins `max_retries: 0`, so the re-run existing at all proves §7.2's interrupted-excluded-from-budget on real binaries.
- *Scenario 3 (cap stress):* one-step command workflow (`run: sleep 2` — valid in POSIX sh and pwsh alike), two projects with per-project cap 2, global `max_parallel_tasks: 3` (both constraints bind: 3 < 2+2 and 4 tasks/project > 2), 8 tasks; poll `/v1/tasks` ~200 ms tracking concurrent `running` per project and globally. Asserts: global ≤ 3, per-project ≤ 2, parallelism ≥ 2 observed at least once (not accidentally serial), all 8 `done`. No agent processes — deterministic durations, no scenario-env coupling.
- *CI:* own `m2-gate` job mirroring `m1-gate` (3-OS matrix, checkout + setup-go + script). Parallel to m1-gate; a failure names the milestone that broke.
  *2026-08-13 — superseded, deliberately.* The three gates are now steps of one `gates` job per OS, not three jobs. What this decision was protecting is intact: each gate is still its own named step, so a failure still names the milestone that broke. What it cost, once M5 made it three jobs per OS, was nine checkouts and nine toolchain setups for scripts that run in 11–25s — the setup dominated the work it was setting up. Safe because each script already isolates itself completely (own `mktemp -d` dirs via `VINCENT_CONFIG_DIR`/`VINCENT_DATA_DIR`, own ephemeral port from `daemon.json`, own `trap cleanup EXIT`); verified by running all three back to back on Windows before the change landed. **Note for whoever updates branch protection:** the required check names changed from `m1-gate (os)`/`m2-gate (os)`/`m5-gate (os)` to `gates (os)`.
- *Manual leg:* `VINCENT_GATE_AGENT=claude` runs **scenario 1 only** against the real CLI with `model: haiku` pinned (T2.12 spend precedent); scenarios 2/3 stay fake even then — killing a paid run and 8× cap spend prove nothing extra. Result recorded in the T2.10 note.

- [x] **T2.10 — Phase gate (M2 acceptance).** Automated: multi-step workflow (agent → command → gate → command publish to a local bare remote) with fake agents, incl. an input-request round-trip (§7.4); kill -9 recovery; cap stress test. Manual: one real-workflow run with real `claude`, result recorded here. ✓ 2026-08-08
  *Done when:* acceptance script green in CI; manual run noted. *(Verified: `scripts/m2-gate.sh` green in the PR #21 CI matrix on ubuntu/macos/windows — scenario 1 drives ask → `awaiting_input` → scripted `/answer` built from `pending_input` → resume → commit → gate approve → push, asserting `input_wait_ms > 0`, `vincent.input_request`/`input_response` transcript lines, the approved gate row, the agent's README.md edit in the bare remote, and the `awaiting_input` `{kind}` event over an SSE replay from cursor 1; scenario 2 hard-kills the daemon mid-hang (`taskkill //F` on Windows) and asserts the orphaned grandchild reaped after restart plus interrupted-attempt-1/succeeded-attempt-2 rows under adhoc's `max_retries: 0` (§7.2 budget exclusion on real binaries); scenario 3 observed caps at exactly 3/3 global and 2/2 per-project across 8 tasks, all done. fakeagent's `ask-question` now honors `FAKEAGENT_EDIT_FILE` post-answer. Manual run 2026-08-08: `VINCENT_GATE_AGENT=claude scripts/m2-gate.sh` with real claude 2.1.226 (haiku) on Windows 11 — GATE PASS: AskUserQuestion parked the task, the scripted answer resumed it in place, claude's README.md edit was committed, gate-approved, and pushed to the bare remote.)*

## Phase 3 — TUI (M3)

Milestone acceptance (§19 M3): the full loop — register project, author workflow, run 3 parallel tasks, approve a gate, archive — without leaving the TUI.

**Phase 3 decisions (grill session, 2026-08-08):**

- *Delivery:* 7 PRs — H: T3.1 foundation · I: T3.2 board · J: T3.3 detail timeline/tail · K: T3.4 detail diff/actions · L: T3.5 new-task flow · M: T3.6+T3.7 projects/workflows/daemon views · N: T3.8 gate. Each independently green in CI; tasks.md updated per PR; each PR opens with its own grill block (Phase 2 pattern).
- *Framework:* Bubble Tea **v2** (`charm.land/bubbletea/v2` + `charm.land/bubbles/v2` + `charm.land/lipgloss/v2`) — greenfield TUI, v2 is the stable, maintained line; choosing it now avoids a v1→v2 migration later. No v1-only third-party widgets (task table = bubbles v2).
- *API client:* new shared `internal/apiclient` — typed methods over §13.2, token + `daemon.json` discovery, SSE subscriber with `Last-Event-ID` reconnect and §13.3 snapshot-then-stream catch-up. The client owns its wire structs (server DTOs stay unexported); drift can't ship unnoticed because client and server live in one binary and the client is integration-tested against the real handlers. T4.2's CLI subcommands reuse this package.
- *Testing:* TUI covered by pure Update/View unit tests (feed msgs, assert on state + rendered strings — no terminal emulation, deterministic on the 3-OS matrix); apiclient integration-tested against an `httptest` server running the real API; daemon auto-start covered by binary e2e like the Phase 1 daemon tests. No teatest/golden frames. Manual polish checks stay in the T3.8 gate.

**PR H decisions (T3.1; grill session, 2026-08-08):**

- *Auto-start UX:* bare `vincent` always enters the TUI immediately; a connect state runs `daemon.StartDetached()` + the phase-1 health poll as a `tea.Cmd` behind a "starting daemon…" screen; failure = in-TUI error screen (daemon log path, `r` retry, `q` quit). The same state doubles as the reconnect screen if the daemon dies mid-session.
- *Shell shape:* one root model owning view routing (`1..6`), global keys, and the `?` help overlay; views are sub-models. PR H ships stub views for board/detail/new-task/projects/workflows/daemon; view-specific action keys land with their views in later PRs.
- *SSE wiring:* apiclient subscription goroutine → `Program.Send` msgs; the home stub renders connection state + the last-received event line, which is how the done-when ("reflects an externally-made state change live") is demonstrated and unit-tested.

- [x] **T3.1 — TUI foundation.** Bubble Tea shell; API client (token + `daemon.json` discovery); daemon auto-start when unreachable (§12.1); `/v1/events` subscription driving re-render; view routing, global keys, help overlay (§15). ✓ 2026-08-08
  *Done when:* TUI connects, auto-starts a stopped daemon, and reflects an externally-made state change live. *(Verified: PR #22 matrix green on ubuntu/macos/windows — apiclient integration tests against the real handlers (live delivery, disconnect + Last-Event-ID resume with cursor assert, server-side type filter, auth envelope, SSE spec framing, discovery); TUI Update/View unit tests (auto-start state machine, failure + retry, routing, help, quit, event line, reconnect); `TestAutoStartRealDaemon` e2e drives the built binary probe-miss → auto-start → live re-render on an external `project.created` → graceful stop.)*
**PR I decisions (T3.2; grill session, 2026-08-08):**

- *Board data comes from the server, not N+1 joins:* `GET /v1/tasks` grows a list-only `listTaskResponse` embedding `taskResponse` with `project_name` (LEFT JOIN projects), `step_total`, `step_name`, `cost_usd`, `input_tokens`, `output_tokens`. §15's board columns exist nowhere in today's DTO, and the alternative — `/v1/projects` + `/v1/tasks/{id}/steps` per row — is 22 calls per refresh for 20 rows *and* forces the TUI to parse `workflow_snapshot` YAML just to count steps. The detail endpoint gets none of these: it already serves `steps[]` with per-attempt cost, and an `omitempty` float would hide a legitimate `0.00`. **Spec addition** to §13.2.
- *Cost is the sum of every attempt,* not the surviving one. A step that failed twice before succeeding rolls up all three `step_runs` rows — §17 says "aggregate cost across attempts", and the number a human opens the board for is what the task actually cost, which includes the attempts that went wrong. Summing only the latest attempt would under-report spend precisely on the tasks that burned it.
- *`step_total`/`step_name` ride a lazy snapshot cache:* `map[taskID]parsed` behind a mutex, filled on miss. Parsing per row per call re-parses 50 YAML docs on every debounced refresh for data that is immutable — §18 already establishes the snapshot never changes after creation, so the cache key can't go stale and no invalidation path exists to get wrong. Denormalizing into columns was rejected: it duplicates the snapshot into state that can drift and costs a migration.
- *Archived tasks are excluded by default:* new `?archived=` on `GET /v1/tasks` (`TaskFilter` gains the field). `ListTasks` has no such filter today, so the board would accumulate archived rows forever; `state=archived` still serves the explicit case. **Spec addition** to §13.2.
- *`task.step_advanced` is a new durable event.* `taskrun/engine.go:126` advances the cursor through `SetTaskProgress`, which emits nothing — a 6-step task shows `1/6` for its entire run. `SetTaskProgress` becomes a transaction (UPDATE + event insert) with a post-commit `s.notify(ev)`, mirroring `TransitionTask`, and fires **only** when `currentStep != nil` so the worktree-path-only call stays silent. Payload is `{"current_step": N}` — §13.3's established "ids plus what changed, not the full object". Routing the advance through `TransitionTask` as a `running → running` self-transition was rejected: every valid-transition guard would have to special-case an edge the FSM otherwise treats as invalid. No scheduler change needed — `scheduler.go:214`'s `WakeOn` already filters to `state_changed`/`priority_changed` and its comment anticipated exactly this ("the rest — step progress, gate waits — would just spin the loop"). **Spec addition** to §13.3.
- *Refresh is snapshot-then-events, never polling:* full `GET /v1/tasks` refetch debounced ~150 ms on task events, plus a 1 s tick that **only re-renders** so `elapsed` counts up without touching the daemon. Patching rows in place from event payloads was rejected — the TUI would re-derive `available_actions` and the rollups the DTO already computes, and drift silently. A refetch that fails while the stream is healthy keeps the last-good rows behind a dim header error line; it never escalates to the shell's reconnect screen, whose `r` would tear down a working connection over one 500.
- *Widget:* `bubbles/v2/table` (flips from `// indirect`). `renderRow` (`table.go:437`) truncates with `ansi.Truncate` and `Inline(true)`, so state colors and the needs-attention badge survive as pre-styled cell strings. Two constraints found by reading it: line 445 wraps the whole row in `styles.Selected` and lipgloss does not rewrite nested sequences, so **selection must be a background** (a foreground silently loses to per-cell color); and `SetRows` only clamps the cursor *index*, so **selection is tracked by task id and re-derived after every refresh** — otherwise a re-sort under the cursor reselects a different task.
- *Sort is five bands, client-side over the fetched page* (the server keeps `id DESC`): needs-attention (`awaiting_input`/`awaiting_gate`/`blocked`, oldest wait first) → `running` → `queued` in true scheduler order (`priority DESC, created_at ASC, id ASC`, matching `store/tasks.go:188`) → `paused` → terminal newest-first. Pin-plus-flat-`id DESC` was rejected: it hides which queued task runs next, which is the question the queue exists to answer.
- *`elapsed` is wall clock* from `started_at`, computed client-side. §17's active-time rule (excluding `input_wait_ms`) governs per-step detail metrics where it is diagnostic; on a board, a task idle 35 of 40 minutes on your input displaying "5m" would read as "just started" for the row most urgently needing you. The pin and badge already explain the why. **Spec amendment** to §15 so the two sections cannot drift on the word "elapsed".
- *Filter:* `/` opens a client-side substring match over title/project/state. Instant, no round-trip per keystroke; the server's `?project_id=`/`?state=` stay available for PR L. Discrete pickers were rejected partly on key budget — §15 already assigns `s` to *skip*.
- *Bell:* `\a` written straight to `os.Stdout` from a `tea.Cmd`. `tea.Printf`/`Println` (`bubbletea/v2@v2.0.8/renderer.go:70,86`) print *above* the program as unmanaged scrollback and are documented no-ops under altscreen — wrong tool, and it would leave a blank line per ring. Rings only on an `EventNote` carrying `to: awaiting_input`, never from the initial snapshot, deduped by event id, rate-limited to 1/s. A `Last-Event-ID` resume delivering a genuinely missed transition *should* ring (it happened while you were disconnected); id-dedupe is what keeps a re-replay silent. No config key in v1 — a `tui.bell` toggle means a `configResponse` field, a §13.2 amendment and a reload path for a complaint nobody has made.
- *Selection hand-off:* `enter` emits `selectTaskMsg{id}`; the root intercepts it, switches to view 2 and forwards the id, so the stub renders "task 42 — detail lands in PR J" and PR J's real view receives the identical message with no board changes. Widening the three-method `view` interface (`views.go:26`) with a `selectedTask()` accessor was rejected — every stub would implement something meaningless.
- *Degradation:* under ~100 columns the board drops whole columns by priority (cost → step name → project) rather than truncating all eight proportionally, which turns every column into an unreadable stub. Empty states are distinct: "no tasks yet — press 3 to create one" vs "no tasks match `foo` — esc to clear", since they are different problems with different exits.
- *Scope:* action keys `p`/`c`/`a`/`r`/`s`/`A` stay out — PR K owns the action bar. Help documents `r` as connection-retry only. **Known collision for PR K to resolve:** the global `r` (retry connect, `root.go`) versus §15's `r` (retry task); settling it now would commit the action-key design before the PR that owns it has looked at the full key set.
- *Header:* `GET /v1/info` fetched once per successful connect (and per reconnect) for `max_parallel_tasks` and agent availability — that data rides a daemon-side cache that only moves when a CLI is installed mid-session. Running and needs-attention counts are derived client-side from the fetched list, which is correct only because the fetch is unbounded (loopback SQLite, tasks in the tens-to-hundreds; revisit if the M3 gate feels slow).
- *Testing:* the `live_test.go` pattern (real store + broker + API handlers + apiclient driving the root model) proves a committed step advance moves `k/n` live — the done-when's "without polling" clause end-to-end. Unit Update/View tests cover filter, the five-band sort, pinning, empty states and column dropping; `internal/api` handler tests cover the new DTO surface including the three-attempt cost sum and `?archived=`; one store test asserts `SetTaskProgress` stays silent on a worktree-path-only write, the regression a later refactor would introduce. The bell gets an injectable `bell func()` on the board model (default: the stdout write) so tests count rings — same event twice → one ring, two events 100 ms apart → one ring — because the dedupe and rate limit only misbehave during reconnect replay, when nobody is watching.
- *Commit order:* spec (§13.2, §13.3, §15) → store → api → apiclient → tui → help + tasks.md. Docs first per the PR C precedent, and strict dependency order so every intermediate commit builds.

- [x] **T3.2 — Board view.** Task table (id, project, title, state color-coded, step k/n, elapsed, cost), filters, scheduler-order sort; header with daemon status, agent availability, running/cap counts, needs-attention count; needs-attention tasks (`awaiting_input`/`awaiting_gate`/`blocked`) pinned on top with a distinct badge; terminal bell on `task.awaiting_input` (§7.4, §15). ✓ 2026-08-08
  *Done when:* board renders live updates from SSE without polling; filters work; an awaiting-input fake-agent task visibly alerts (pin + badge + bell). *(Verified: PR #23, all 9 checks green — ci + m1-gate + m2-gate on ubuntu/macos/windows. `TestBoardRendersLiveFromSSE` drives the shell against the real handlers and asserts `k/n` moves 1/3 → 2/3 on a real `task.step_advanced` — the event this PR added, without which the counter froze for a whole run. `TestBoardPinsAndBadgesAwaitingInput` asserts the pin ordering, the badge and the header count; `TestBoardEnterOpensDetail` proves the `selectTaskMsg` hand-off. Bell covered by unit test through the injected ring counter (replayed event id silent, burst collapsed, window reopens) rather than by a fake-agent run — the board only ever sees the state, and the fake-agent input path is already covered in `internal/taskrun`. `TestBoardColumnsFitWidth` caught a real overflow in the first cut of the narrow-terminal degradation.)*
  *Carried forward:* the `r` collision is **unresolved by design** — `r` is currently retry-connect (global, `root.go`) while §15 assigns `r` to retry-task. PR K owns the action bar and must settle it; the help overlay documents `r` as connection-only until then. *(Settled by PR K: `root.go` already gated the global `r` on the disconnected screen, so `r` is retry-task whenever connected and no key had to move.)*
**PR J decisions (T3.3; grill session, 2026-08-09):**

- *Transcript rendering is server-side:* `?format=normalized` on `GET …/steps/{run_id}/transcript` runs the owning adapter's parser over the file and emits the same shapes the live chunks already use, so scrollback and the live tail share **one** render path in the TUI. Adapters export `NewLineParser() func([]byte) agent.Event` — claude's `parseLine` is stateless, codex's is a method on a stateful `stream`, and the constructor form fits both. Parsing both dialects in the TUI was rejected: it puts adapter knowledge outside `internal/agent` and grows a dialect per future adapter. **Spec addition** to §13.2.
- *The normalized vocabulary is a lossless superset:* the four live chunk types plus `agent.result`, `agent.error`, the `vincent.*` kinds under their own names, and `agent.raw` for any line the parser calls `EventUnknown`. The live stream drops results and errors deliberately (`taskrun/steps.go:492`) because they surface as step outcomes — but scrollback of a *failed* attempt is precisely where "why did this die" is the question, and a transcript that silently omits lines is not the lossless record §12.2 promises.
- *`X-Next-Offset` snaps to a line boundary:* it is the file size today (`api/tasks.go:539`), which on a concurrently-appended file lands mid-record and hands the next fetch half a line. The body now ends at the last `\n` and the header reports that boundary. The endpoint's unit is a JSONL record; returning half of one is the endpoint lying. Nothing had exercised this — §13.3's transcript-then-follow had no client until this PR. **Spec addition** to §13.2.
- *`?tail=<bytes>`, 256 KiB from the TUI:* §18 admits an agent can emit gigabytes and the per-run cap is T4.3, so PR J must survive one unaided. `tail` counts raw file bytes and normalization applies after; mutually exclusive with `offset` (400 `validation_failed`); follow-ups resume from the returned `X-Next-Offset`, so there is one resume path rather than two. The client-side size probe (`?offset=<huge>` → empty body + size) was rejected: a round trip per open, and the boundary rule re-derived outside the server. *(Amended during implementation: the window snaps **backwards** into the record its byte count lands in, not forward to the next record start. Forward avoids a partial record but returns **nothing** whenever the last record is larger than the window — the handler test hit this on its first run, a `vincent.step_finished` line being longer than a small tail. Backwards avoids the partial record just as well, cannot come back empty, and is the same "last boundary at or before X" function the end cursor already needed, so the endpoint has one rule instead of two.)*
- *The catch-up seam is exact, not approximate:* every live chunk carries `run_id` (its `step_runs` row) and `offset` (the file position **after** its line was written — write-then-publish already holds at `taskrun/steps.go:105→109` and `417→418`). The client subscribes, buffers, fetches, then drops buffered chunks where `run_id` matches and `offset <= X-Next-Offset`. Offset alone is wrong: offsets restart at zero in every attempt's file, so a step rollover or retry would compare positions across two different files and discard the new attempt's output entirely. **Spec addition** to §13.3.
- *One fetch, debounced:* `GET /v1/tasks/{id}` already embeds `steps[]` (`api/tasks.go:462`), so the whole timeline is one call — no `/steps` round trip. Refetch is debounced ~150 ms on events for *this* task id, plus once on connect, mirroring the board (`tui/board.go:24`).
- *Three DTO gaps closed:* `step_name` on `stepRunResponse`, from the `snapshotCache` PR I already built (`api/snapcache.go:19`), so the timeline shows workflow step names instead of ids; `step_total` moved **up** into `taskResponse` and deleted from `listTaskResponse` (declaring it in both puts one JSON tag on two fields at different embed depths — Go resolves that silently in favor of the outer one, which is a landmine, and the list DTO's JSON stays byte-identical through the embed); `prompt_override`/`run_override` exposed as booleans, not text. PR C added those two columns "so the timeline can flag an edited attempt" and nothing has ever read them — this is the first consumer.
- *Timeline row:* `step · attempt · state · duration · tokens · cost`, with `failure_reason` under a failed row, `model/effort` on agent rows, and `✎` on an edited attempt. Duration is §17 active time (`finished_at - started_at - input_wait_ms`) with `+Nm waiting` appended, so the excluded time stays **visible** rather than silently subtracted. This is deliberately the opposite of the board's wall-clock rule (PR I): there the number is an alarm and must not read as "just started"; here it is diagnostic, and a step that spent three minutes waiting on a human is not a slow step.
- *Layout:* stacked — timeline on top (~40%), output below behind a tab strip PR K extends with Diff. Selecting an attempt *is* how scrollback is navigated, so hiding either pane behind a tab breaks the primary gesture, and PR K's action bar needs a home visible from every tab. Narrow or short terminals collapse the timeline toward the selected row rather than dropping a pane.
- *Pane rendering:* `agent.output` plain · `agent.tool_use` as `▸ Tool  arg` · `command.output` stream-prefixed with stderr dimmed-red · `vincent.input_request`/`input_response` highlighted (the moment the run stopped for you) · `agent.error` and the `error`/`input_timeout`/`input_protocol_error` notes in the error style · `agent.result` as a closing summary line · `agent.raw` dim and collapsed behind a count, so a chatty adapter cannot drown the pane. `agent.usage` is fetched and **not** rendered: the timeline row already carries tokens and cost, and adapter-native usage JSON is noise at the moment you are reading output.
- *Follow mode:* on by default for the live attempt, unavailable for a terminal one (a "following" badge on a finished run is a lie). Any upward scroll drops it; `f` or `G` re-arms and jumps to the bottom; the pane header states `▼ following` or `⏸ paused · N new`, because dropping follow silently while output keeps arriving is how a user concludes the run stalled. Across a rollover, selection advances to the new attempt only if the cursor was already on the live one — live movement never overrides browsing (same rule as the board's id-tracked selection). Chunks for a `run_id` the client has not fetched yet trigger an un-debounced refetch and are **held**, never dropped: dropping costs the first ~150 ms of every step, which is where the interesting part is.
- *Keys:* `tab` toggles focus timeline↔output · `↑↓`/`jk` move attempt or scroll by focus · `pgup/pgdn`, `g`/`G` jump · `f` toggles follow · `esc` returns to the board. `1..6`, `?` and `q` stay global. PR J claims none of §15's action keys (`p`/`c`/`a`/`r`/`s`/`A`), so the `r` collision above stays PR K's to settle.
- *Five distinct states:* no task selected · queued with no attempts · task refetch failed (keep last-good rows behind a dim line, never escalate to the shell's reconnect screen, per PR I) · transcript pruned or missing (`api/tasks.go:523`) · step row with no transcript at all (manual-gate and skipped rows, which render their own metadata and fetch nothing). The last two both produce "an empty pane" today and call for opposite reactions from the user.
- *Testing:* apiclient integration — `StreamTask` yields `OutputNote` **and** durable events (`readFrames` discards the `event:` field today, `apiclient/events.go:188`, so an id-less live-output frame currently decodes into a zero-valued `Event`), plus normalized mode, `?tail=` and resume offsets. API handler tests drive normalization through the **existing** captured fixtures in `internal/agent/{claude,codex}/testdata` rather than new ones, and cover boundary snapping and the DTO additions. TUI unit Update/View tests cover timeline rows, selection under refresh, follow drop/re-arm, the truncation marker and all five states. One `live_test.go`-style end-to-end over the real store + broker + API + apiclient + root asserts a runner-published chunk reaches the tail with **no duplicate and no gap across the catch-up seam** — the seam this PR invents, and the one behavior a unit test cannot honestly prove.
- *View lifecycle:* the root gains `viewActivatedMsg`/`viewDeactivatedMsg` broadcast on view switch, so the detail view opens its per-task stream only while it is on-screen with a task and cancels on leave. An always-on tail for a task nobody is watching burns a connection and unbounded memory for output the transcript already holds; PR K and PR L need the same signal.
- *Commit order:* spec (§13.2, §13.3, §15) → `agent` (exported line parser) → `taskrun` (chunk `run_id` + `offset`) → `api` → `apiclient` → `tui` → help + tasks.md. Docs first per the PR C/I precedent, strict dependency order so every intermediate commit builds.

- [x] **T3.3 — Task detail: timeline & tail.** Step timeline with attempts/durations/tokens/cost; live output tail with follow mode; scrollback into past transcripts via ranged transcript endpoint. ✓ 2026-08-09
  *Done when:* a running fake-agent task shows live tail; historical attempts browsable. *(Verified: `TestDetailTailJoinsTranscriptWithoutGapOrDuplicate` drives the shell against the real store, broker, handlers and client — it fetches an attempt's transcript, then publishes one chunk the fetch already covered and one past it, and asserts the covered line renders exactly once while the new one arrives. That seam is what this PR invents and the only part a unit test cannot honestly prove. Unit tests cover the timeline (active duration with the excluded wait shown, the `✎` edited-attempt flag, tokens/cost/failure reason), selection under refresh (a step advance moves the cursor only when it was on the live attempt), the buffered-chunk dedupe in both directions, an unknown `run_id` forcing an un-debounced refetch, follow drop/re-arm with its unread counter, all five empty states, raw-line collapsing and the record cap. `internal/api` covers the boundary rule, `?tail=`, `format=normalized` against the captured claude fixtures, and the DTO additions; `internal/apiclient` covers `StreamTask` delivering `OutputNote` and durable events, and the tail/resume round trip.*
  *Carried forward:* the API's `?tail=` snaps backwards rather than forwards (see the amended PR J bullet above — snapping forward returns nothing when the last record exceeds the window). The test pump now runs commands concurrently like the runtime; serialized, a blocking subscription command starved everything queued behind it, which no earlier PR had noticed because no view held its own subscription.
**PR K decisions (T3.4; grill session, 2026-08-09):**

- *One PR, four features:* the action bar is the spine the diff tab, the gate decision, the answer form and edit+retry all hang off. Splitting would ship a bar that hides `answer` and `retry` and then re-touch every gating test.
- *Actions live on the board too, not just the detail view:* §15 lists `p`/`c`/`a`/`r`/`s`/`A` as **global** keys, and triage is a board activity — approving three gates through enter/esc round trips is the workflow this view exists to avoid. One component driven by `available_actions`, rendered as a bar in the detail view and a dimmed footer line on the board. Gate reject needs a key §15 never assigned (`r` is retry, `a` is approve): `x`. **Spec addition** to §15, together with `E` (edit+retry) and `d` (output/diff toggle).
- *The `r` collision is resolved by scope, not by a new key* (PR I carried it forward): `root.go:145` already gates the global `r` on `phaseFailed`/`phaseReconnecting`, so `r` is retry-task whenever connected and retry-connect only on the screen where no task is reachable. Nothing to renumber.
- *`apiclient` grows a write path:* it is GET-only today. One `post` helper plus a typed method per action returning the updated `Task`, and `Error` gains `Details map[string]any` so PR C's `details.state`/`details.reason` are machine-readable — the details object exists precisely so a client doesn't regex prose. A 409 forces an immediate refetch: the row was stale, and reporting the error without correcting the state invites the same keypress again.
- *Confirmation is for `cancel` and `archive` only,* inline in the bar (`y`/`n`/`esc`) — nothing else in this TUI is modal. Cancel tree-kills a live agent; archive removes the worktree. Dirty archive fires unforced first, and the 409 `details.reason: worktree_dirty` becomes "uncommitted changes will be lost — force? y/n", reusing the server's own dirty confirmation rather than inventing a second one. Everything else fires immediately; all of it is recoverable.
- *Post-action state comes from the response body,* then the existing debounced refetch reconciles. Optimistic prediction would re-derive the FSM the TUI deliberately does not own (PR I's rule), and waiting for SSE alone makes a keypress feel dead for a round trip.
- *Diff is fetched on tab activation and on an explicit refresh, never from the event stream:* the endpoint shells out to `git diff`, so an event-driven refetch is a subprocess per event. Rendering is hand-rolled structural coloring (file headers, hunks, `+`/`-`) in lipgloss — `chroma`'s diff lexer colors the same four things, and per-language highlighting *inside* a diff needs gutter-stripping plus per-file lexer detection it will not do for you; it is in the module graph only as a golangci-lint indirect and would become a direct runtime dependency for ~60 lines of styling. Render capped at 5000 lines with a marker (T4.3 owns real caps). The endpoint's two 409s render distinctly — "no worktree yet" (queued) and "worktree removed" (archived) are different situations.
- *Gate instructions need no new field:* `enterGate` renders them through the template engine and stores them in `step_runs.result_summary` (4096 cap), which `stepRunResponse` already exposes. The approve/reject prompt reads the open manual row.
- *edit+retry gets typed step text, not raw YAML:* the detail response already carries the whole `workflow_snapshot` (`api/tasks.go:476`), so the text exists on the wire — but reaching it means parsing workflow YAML inside the TUI, and §15 defines the TUI as a pure API client while PR H put wire structs in `apiclient`, which would then have to import `internal/workflow`. A typed array keeps format knowledge on the server, where the parse is already happening and already cached, and T4.2's CLI gets it for free. Exposing it also surfaces a live invariant break: PR I's `snapshotCache` is documented "never needs invalidating: a task's snapshot is written once at creation and is immutable", but PR C's edit+retry **rewrites `tasks.workflow_snapshot`**. Harmless so far (step count and names don't change on an edit), a stale-prompt bug the moment step text is cached. So: `workflow_steps[]` (index, id, type, prompt, run, instructions) on the **task-detail** response only, and `forget(taskID)` called from the override write, making the documented invariant true instead of true-by-luck. **Spec addition** to §13.2.
- *edit+retry always opens the task's `current_step`* — the only step `retry` re-runs — and the step type picks the field (agent → `prompt`, command/check → `run`), so the client never sends the mismatched pair the server would reject. A blocked **manual** step has neither: `E` is simply absent there and plain `r` stays. `E` rather than `r` for the editor path because the common move after a flaky command is "run it again", not "open an editor".
- *`$EDITOR`:* `tea.ExecProcess` over a temp file, `$VISUAL` → `$EDITOR` → `vi`/`notepad`. Unchanged content sends a plain retry with **no** override — flagging the attempt `✎` when nothing was edited would lie to the timeline. An emptied buffer aborts and sends nothing: an empty prompt is never what someone meant.
- *Answer form renders above the tail,* not as a floating overlay — §7.4 questions can be long and an overlay that clips one is worse. *(Amended during implementation: it is its own block between the timeline and the pane rather than content scrolled inside the pane. Inside the viewport the cursor can scroll out of sight while the keys still drive it, which is the one thing a form must not do; as a block it takes at most half the pane and windows around the cursor, the way the timeline already does.)* Typed `InputRequest`/`InputResponse` in `apiclient` (server DTOs stay unexported, PR H). The client enforces §7.4's rules to disable submit with a reason; the server stays sole authority and its 400 renders in the form. Options are suggestions, never an enum, so free text sits beside them.
- *Focus and capture:* `tab` cycles timeline → pane → form; `detail.capturesInput()` flips from its hardcoded `false` so the form and the confirm prompts own the keyboard, reusing the hook the board's filter established (`root.go:129`). Action keys fire from any focus except those two.
- *`set priority` stays out* (**recorded in §15**): it is a §6 action with no `available_actions` entry — it is not FSM-gated — and no §15 key. The bar's entire contract is "render exactly what the server says", and priority would be its one hand-written exception; PR L sets priority where it belongs, at creation.
- *Testing:* exactly one end-to-end, extending PR J's `detaillive_test.go` with a real `taskrun.Runner` and the fakeagent `ask-question` scenario — request → form → submit → `running` → tail continues, which is the round trip this PR invents. Bar gating derives its cases from `taskstate.HumanActionsFrom` (the T2.6 pattern) so a future FSM edit cannot leave a dead action on screen, with one hand-written case the FSM cannot express (blocked on a manual step shows `r`, not `E`). The editor path runs through an injected exec hook (edited → override on the wire; unchanged → plain retry; emptied → nothing sent). The diff renderer is unit-tested against a synthetic multi-file diff, while `apiclient` covers the real `text/plain` body and both 409s, which no synthetic string can produce.
- *Commit order:* spec (§13.2, §15) → `api` → `apiclient` → `tui` → help + tasks.md, per the PR C/I/J docs-first precedent.

- [x] **T3.4 — Task detail: diff & actions.** Diff tab (syntax highlighted); action bar offering exactly the state-valid actions; gate approve/reject with rendered instructions; answer form for `awaiting_input` (question/permission rendered with options, multi-select, free-text entry; submits `POST /answer` and the run resumes in place, §7.4); edit+retry through `$EDITOR` (§6, §15). ✓ 2026-08-09
  *Done when:* every action reachable and functional from the detail view; invalid actions never shown; a fake-agent question is answerable from the detail view and the run resumes live. *(Verified: `TestDetailAnswersLiveAgentQuestion` drives the real runner, the real fake-agent binary, the real handlers and the real client — the agent process is blocked on stdin, so the answer this form submits is what unblocks it, and the task runs to `done` with the form gone. `TestBoardApprovesGateFromTheRow` proves the same bar works from the board. The gating test derives its cases from `taskstate.HumanActionsFrom` across all nine states, so no state can render a dead action or hide a live one. Unit tests cover the two confirmations, the dirty-archive re-ask with `force`, a 409 reporting the state it found, form navigation/selection/free text/permission decisions/blocked submit, diff classification (the `+++`/`---` markers are file headers, not one add and one remove per file), the diff's five empty states, and the editor path in five shapes — edited prompt, edited command on a command step, unchanged (plain retry, no `✎`), emptied file (nothing sent), failed editor. `internal/api` covers `workflow_steps[]` and the snapshot-cache invalidation; `internal/apiclient` covers every action against the real handlers.)*
  *Carried forward:* `set priority` (§6) remains unbound — it is not in `available_actions`, so the bar cannot gate it; §15 now records that. The diff refreshes only on entering the tab, since the endpoint shells out to git per call.
**PR L decisions (T3.5; grill session, 2026-08-09):**

- *No server change.* Every input this flow needs is already on the wire: `GET /v1/projects` (default branch, default workflow), `GET /v1/workflows?project_id=` (description, steps with their §8.6 level-1/3 agent, validity and errors), `GET /v1/agents` (availability plus provenance-tagged model/effort catalogs and defaults), and `POST /v1/tasks` (which already accepts `fields`, `base_branch`, `priority`, `agent`, `model`, `effort` and returns `warnings[]` on the 201). This is the first Phase 3 PR that is purely client-side, so it breaks the docs-first precedent for the honest reason that §15 and §13.2 already describe it exactly.
- *`apiclient` gains four read paths it never had:* `ListProjects`, `ListWorkflows`, `ListAgents`, `CreateTask`. PR K gave it a write path but only for task actions; `CreateTask` needs its own request struct because the existing `Override` is retry-scoped (`prompt_override`/`run_override`) and shares nothing with the creation triple.
- *One screen, not nine.* §15 writes the flow as a chain (`project → workflow → title → … → create`), but that chain is field *order*, not screen count. The form is a row list where the focused row expands into its editor or picker. A wizard makes back-navigation and review-before-submit into features that have to be built; a form gets both free, matches how `board` and `detail` are already modelled (one sub-model, one `update`), and is testable through a single Update loop instead of nine hand-offs.
- *Base branch is free text, prefilled, validated by the daemon.* §13.2 has no branch-listing endpoint and adding one means the daemon shells out to git every time the flow opens. `handleTaskCreate` already resolves the branch authoritatively and 400s with `base_branch %q does not resolve to a local branch`; a client-side pre-check would only drift from it. The 400 re-focuses the branch row carrying the server's message rather than dropping the draft.
- *Broken workflows are listed, not hidden.* The registry deliberately returns invalid entries with their `errors[]` so a client can show them (`workflows.go`), and hiding a workflow someone just typo'd is how you get "my workflow disappeared". They render with a badge and their first error, and cannot be selected — create would 400 anyway. `adhoc` is an ordinary entry.
- *The unavailable-agent flag has a hole, and it stays open.* `workflowStepResponse.Agent` is §8.6 levels 1 and 3 only; when neither the step nor `defaults` names an agent the field is empty, meaning "adapter default", and the API cannot say which adapter that resolves to. Those steps render "adapter default" **unflagged**. Flagging them would require either a resolution field the server does not expose or the TUI re-implementing §8.6, and PR K's rule is that format and resolution knowledge stays server-side.
- *One plain `GET /v1/agents` per flow open, `R` to force a probe.* The endpoint is binary-identity cached — a plain GET costs a path resolve and one stat per adapter, and re-probes on its own when a binary actually changed — so it is already live enough for "populates from a live probe" without spawning an adapter subprocess every time someone presses `n`. Forcing `?refresh=true` on open would make the flow's latency scale with adapter count for no new information.
- *Changing the agent clears model and effort.* §8.6's agent-scoped inheritance exists so a claude alias like `sonnet` can never reach codex; the picker has to enforce the same thing or it will hand the server a triple the resolver would have rejected. All three default to "unset — use workflow defaults", options carry a dim `cli`/`curated` tag from `agent.Option.Source`, and a `(free text…)` row accepts anything the catalog does not list. A static line records that the override replaces workflow defaults and never a step's explicit pin.
- *Validation splits by what the client can actually know.* Create is blocked locally only on an empty title, no project, or an invalid workflow — three things decidable without the daemon. Base branch and unknown model/effort go to the server and its `error.message` lands on the row it names. Re-deriving the daemon's checks in the TUI is how the two drift.
- *Fields are a row list, not a text buffer.* `fields` is an open string map with no schema anywhere in §5.3, so there is nothing to validate against; `a` adds a pair, `enter` edits key then value, `d` deletes, a duplicate key overwrites in place with a warning. A raw `k=v` buffer defers every parse error to submit time and cannot distinguish an empty value from a missing key.
- *Description is a `textarea` with `e` for `$EDITOR`,* reusing the idiom the answer form already established for free text. `editorCommand()` and the injected `execFunc` carry over from PR K, but `writeEditorFile` is hard-bound to `(taskID, WorkflowStep)` and gets generalised to `(basename, text)` — the description belongs to no task and no step yet.
- *Priority is the raw integer,* default 0, `+`/`-` nudging while focused. The scheduler sorts on `priority DESC, created_at ASC`; naming tiers "high/normal/low" would invent a scale the admission order does not have.
- *`esc` while editing a row leaves edit mode; `esc` on the body abandons the draft* with an inline confirm only when something was touched, matching PR K's rule that confirmation is reserved for destructive-and-irrecoverable. `capturesInput()` is true only while a text row is in edit mode, so the global single-key bindings survive everywhere else — the hook the board's filter established. `ctrl+s` submits from anywhere, `enter` submits from the Create row.
- *After the 201 the flow lands on the new task's detail view* and drops the draft; the response `warnings[]` render as a status line there. Creating a task is the beginning of watching it, and warnings are advisory (catalog-unknown model or effort) — the task exists either way, and leaving them on a board row is how they get missed.
- *Nothing is sticky between opens.* Project comes from the board's cursor row (or the only project when there is one), workflow from that project's `default_workflow` else `adhoc`, branch from `default_branch`, priority 0 — all re-derived each open. A half-remembered draft from twenty minutes ago is a worse default than the context you are standing in.
- *Testing:* one end-to-end (`TestNewTaskFlowCreatesRunnableTask`) driving the real handlers, real registry and the fakeagent binary — fill the form through `Update`, submit, assert the created task reaches `done`, which is the round trip this PR invents. Unit tests cover the unavailable flag against a catalog with one adapter missing, invalid-workflow rows refusing selection, the agent switch clearing model and effort, the `$EDITOR` description path in PR K's five shapes, a `base_branch` 400 re-focusing its row, warnings surfacing after the 201, and the fields editor's duplicate-key overwrite. `apiclient` covers the four new calls against real handlers.
- *Commit order:* `apiclient` → `tui` → `n` binding and help overlay in `root.go` → tasks.md. No spec commit — §15 and §13.2 already specify this flow.

- [x] **T3.5 — New-task flow.** Project picker → workflow picker (description, step list, unavailable-agent flags) → title → description (inline or `$EDITOR`) → fields → base branch → priority → optional agent/model/effort pickers fed by `GET /v1/agents` (provenance-tagged options, free-text entry, note that step pins are not overridden) (§15, §8.6). ✓ 2026-08-09
  *Done when:* task created through the flow runs end-to-end; unavailable agent visibly flagged; override pickers populate from a live probe and accept free text. *(Verified: `TestNewTaskFlowCreatesRunnableTask` drives the shell against a real daemon — real registry, scheduler, worktree and the fake-agent binary — pressing `n`, picking the registry workflow, typing a title, adding a `ticket` field and submitting with `ctrl+s`; the created task reaches `done` with the fields, workflow and prefilled branch the form sent, which is the only way to show the body it builds is one the engine accepts. The same test reads the unavailable flag off the live probe: the harness registers codex at a path that does not exist, and the option renders flagged while claude resolves to the fake binary and populates the model catalog. Unit tests cover the invalid-workflow refusal with the entry's own finding, the agent switch clearing model and effort and swapping the catalog, free-text model entry, the request omitting every untouched field, client-side blocking limited to project/title/workflow, a `base_branch` 400 landing on its row and moving the cursor there, an unroutable error staying a form error, the fields editor's duplicate-key overwrite and its dropped keyless row, capture on and off, both discard paths, priority nudging and omission at 0, the `$EDITOR` path in three shapes (edited, failed, seeded from the draft), and the 201's warnings reaching the detail view as a non-error status.)*
  *Carried forward:* two shell bugs fixed in passing — `esc` was swallowed globally by `root.updateKey`, which had quietly made the detail view's documented "esc back to the board" dead code since PR J, and the new `n` binding is suppressed while the form is up because there `n` is "no" to the discard prompt. A step whose agent resolves only at §8.6 level 4 still cannot be availability-checked; it renders "adapter default", unflagged. **A pre-existing data race surfaced on CI** (ubuntu + macos `-race`, windows passed on timing): `Runner.emit` built its event as `TaskID: &task.ID`, publishing a pointer into the runner's live task to every SSE subscriber while `transition` rewrote that struct on each state change. This end-to-end test is the first to hold a task-scoped subscription while the runner drives the same task, so it is the first to interleave them. Fixed by copying the ids at all three construction sites (`taskrun.emit`, `store.CreateTask` — whose task the API mutates immediately afterwards to assign the branch name — and `store.TransitionTask`); `TestPublishedEventsDoNotAliasTheCallersTask` pins the invariant deterministically rather than leaving it to the detector catching the right interleaving.
**PR M decisions (T3.6; grill session, 2026-08-09):**

- *Both views in one PR.* §15's views 4 and 5 share a shape (list → row → `$EDITOR`), share the refetch-on-event plumbing, and the Workflows view's scope grouping comes from the project list. Split, the projects fetch gets built twice and the "which project's registry" question has no answer until the other half lands.
- *One spec commit.* §15 documents global keys and a detail-view key line but nothing per-view for views 4 and 5, and PR K's precedent is that key decisions — including the negative ones — belong in §15 rather than only here. So: a Projects/Workflows key line, plus a record that **creating a workflow from the TUI is out of v1**. Creation needs a server-exposed global workflows directory (`GET /v1/config` does not expose one, `api/server.go:267`), a filename prompt and a starter template; §15 view 5 asks for list + `e` + live reload and nothing more. A carried-forward note here is invisible to someone reading the spec to find out whether the feature exists.
- *Everything else is client-side.* `GET/POST/PATCH/DELETE /v1/projects` all exist with a tri-state PATCH (`api/projects.go:150`), `GET /v1/workflows?project_id=` already returns `scope`, `file`, `errors[]` and `warnings[]`, and the fsnotify watcher → `Registry.OnChange` → durable `workflow.registry_changed` chain is wired end to end (`daemon/daemon.go:229`). `apiclient` gains `CreateProject`/`PatchProject`/`DeleteProject` plus an exported `Opt[T]` marshalling absent/null/set — without it "clear the default workflow" is unexpressible and re-sending every field on each edit would stomp a concurrent change.
- *The project form is purpose-built, not extracted.* `newtask.go` has row-list + focus + edit-mode machinery, but it is welded to the create-task draft and the shapes genuinely differ: new-task validates three rules client-side and submits once, project edit PATCHes against server-side path and branch validation. Reuse `picker` (for `default_workflow`) and the editor helpers; two instances is not enough evidence for a framework.
- *Validation splits the PR L way.* Block locally on an **empty path only**. Is-it-a-git-repo, does-the-branch-resolve, is-this-repo-already-registered and name uniqueness are all already server-side (`api/projects.go:75–100`), three of them need filesystem access the TUI should not assume, and every 400 lands on the row its message names, reusing PR L's `base_branch` routing.
- *Add sends only what was touched.* Path alone in the common case: the daemon derives `name` from `filepath.Base(path)` and detects `default_branch` itself, and the created row shows what it chose. The path field prefills with the TUI's process cwd — people usually launch it from the repo they mean, and a wrong prefill costs one `ctrl+u` while an empty field costs a hand-typed absolute path with no completion behind it.
- *Delete has two 409s and only one of them is a question.* `project has N non-archived task(s); pass ?force …` (`projects.go:277`) becomes PR K's dirty-archive idiom — unforced first, the server's own count in the prompt, retry with `?force`. `task N is running; cancel it before deleting the project` (`projects.go:284`) is **not** force-able and renders as a plain inline error: a `y/n` whose "yes" cannot succeed is the worst prompt in the app. Delete also confirms unconditionally first — the cascade drops task rows and removes worktrees.
- *The two caps are independent, and the view has to say so.* `scheduler.admit` applies the global `config.MaxParallelTasks` always and `c.ProjectCap` additionally only when non-null (`scheduler/scheduler.go:114,147`); the API agrees — `"max_parallel_tasks must be at least 1 (null = unlimited)"`. A null cap does not mean "capped at the global figure", it means the project competes for the whole pool, so rows read `2 running · cap 2` or `2 running · no project cap (global 3)`. The global figure earns its place because it is the ceiling that actually held a task back. `apiclient.Project`'s doc comment says the cap "falls through to the global one" — the same wrong reading, fixed in this PR.
- *Cap editing is where the tri-state bites:* empty input sends `null`, anything else sends `set` — including `0`, whose 400 lands on the row like every other server message. Not intercepting `0` client-side is deliberate: the daemon's message explains the rule better than a form hint, and it is one fewer check to drift.
- *The Workflows view merges all scopes, N+1.* `GET /v1/workflows` with no `project_id` lists global only, so a scope-badge column is pointless unless the view assembles `ListWorkflows(0)` plus one call per project. Projects are human-sized. The merge has to key on (scope, project_id, name): a global entry shadowed inside project P comes back **twice** — once `scope: global`, once `scope: project` — and renders as one row carrying a "shadows global *X*" note, not two rows that look like a duplicate. Rows group by scope (global block first, then projects in list order, alphabetical within), and an invalid entry stays in place with a badge rather than sorting to the top: a workflow that moves when you break it is harder to find, not easier.
- *A per-project fetch failure degrades that block only* (`registry unavailable: <err>`), while a failed global call is the view's load error and keeps last-good rows behind a dim line, PR I/J's rule. One unreadable project must not blank the registry.
- *`e` edits the real file in place and then does nothing.* No temp copy, no `POST /v1/workflows/validate` on the buffer — the view waits for `workflow.registry_changed`. That is what makes `e` and an external `vim` provably the same path, which is exactly what the done-condition asks; a second verdict from `/validate` could disagree with what the daemon actually loaded. `e` is absent (not an error) on an entry with no `file` — the built-in `adhoc` — which renders "built-in", the same rule PR K used for `E` on a blocked manual step. The editor helpers gain a sibling `openEditorPath(path)` rather than a flag: it touches a file it must not create, must not truncate and never reads back.
- *Liveness is refetch-on-broadcast, no owned connection.* `workflow.registry_changed` carries an empty payload, so it can only mean "refetch everything"; `project.*` events refetch the project list and, since a new project adds a registry scope, the workflow set too. Both debounce like the board (`board.go:175`) and both refetch on `viewActivatedMsg` so a view that was off-screen through a burst does not open stale. Unlike PR J's tail these hold nothing open — they read the broadcast the root already delivers — so they stay subscribed while hidden and merely skip the fetch.
- *Keys are free here:* the action keys live in `actionbar.go`, owned by board and detail only, so nothing collides with root's `q ? esc 1-6 n r`. Projects: `a` add · `enter`/`e` edit · `d` delete (confirmed) · `/` filter. Workflows: `e` edit · `enter` expand steps · `R` force refetch. Both implement `projectHinting` so `n` opens the new-task form on the row under the cursor, and `capturesInput()` is true only while a text row is in edit mode — PR L's rule.
- *Nine named states, none modal:* projects — no projects registered · refetch failed (last-good behind a dim line) · delete refused by a running task; workflows — registry empty · a project block with no entries · a project block whose fetch failed · an invalid entry · a file-less built-in. "An empty pane" covered three different situations in PR J; naming them is what stops that.
- *Testing:* exactly one end-to-end, `TestWorkflowsViewReflectsFileEdit` — real registry with `Watch` running, real handlers, real client, real shell: write a valid YAML into the watched directory and assert the row appears with its status **without a restart**, then rewrite it broken and assert the row flips to invalid carrying the registry's own finding. That chain (fsnotify → debounce → reload → durable event → SSE → broadcast → refetch) is the seam this PR invents and the only part a unit test cannot honestly prove. **No second end-to-end for projects** — create/patch/delete invent no cross-process seam, so they go in `apiclient` tests against real handlers, including an `Opt` round trip proving absent ≠ null ≠ set and both 409 bodies. Unit tests cover: add sending only touched fields · empty path blocked locally and nothing else · a 400 landing on its named row and moving the cursor · the unforced-delete confirm and its forced retry · the running-task 409 rendering with no prompt · cap set / cleared / `0` rejected · cwd prefill · both cap-column shapes · the shadowed-pair merge · scope grouping and within-block order · an isolated per-project fetch failure · a global failure keeping last-good · `e` absent on a file-less entry · an invalid entry badged in place · refetch on `viewActivatedMsg`.
- *Files:* `projects.go` + `projectform.go`, `workflows.go` + `workflowrender.go` — the model/render split both existing views already use once a view has more than one rendered state.
- *Commit order:* spec (§15) → `apiclient` (project writes, `Opt[T]`, corrected cap doc comment) → `tui` projects → `tui` workflows → help overlay + tasks.md. Docs-first per PR C/I/J/K, strict dependency order so every intermediate commit builds.

- [x] **T3.6 — Projects & Workflows views.** Project list/add/edit/remove with per-project cap; workflow registry with scope badges + validation status, `e` opens `$EDITOR`, live reload visible (§15). ✓ 2026-08-09
  *Done when:* editing a workflow file externally or via `e` updates the view without restart. *(Verified: `TestWorkflowsViewReflectsFileEdit` runs the whole chain for real — a real fsnotify watcher on a real registry, the real `OnChange` → `workflow.registry_changed` wiring the daemon uses, real handlers, real client, real shell. It writes a workflow into the watched directory and asserts the row appears without a restart, then rewrites it broken and asserts the row flips to invalid carrying the registry's own finding. Nothing below the API layer had ever been exercised through the TUI, and no unit test can stand in for it. Unit tests cover the projects view — both cap-column shapes, the three empty states, a failed refresh keeping last-good rows, the delete confirmation re-asking only when the daemon named `?force` and never after a forced request, the add form omitting every untouched field, client-side blocking limited to an empty path, a PATCH carrying only what changed with null distinguished from absent, a zero cap sent for the daemon to reject, a 400 landing on the row its message names and an unroutable one staying a form error, invalid workflows listed but unselectable, and capture following the text fields — and the workflows view: the shadowed-pair merge rendering once with its note, scope grouping with alphabetical order inside, a broken entry in place with its finding, an isolated per-project failure, a failed global fetch keeping last-good, `e` refused on a file-less built-in, `e` opening the real path and changing nothing until the reload arrives, the cursor skipping headers, and refetch on activation and on registry/project events but not task ones. `internal/apiclient` covers create/patch/delete against real handlers, including an `Opt` round trip proving absent ≠ null ≠ set and both 409 bodies.)*
  *Carried forward:* the grill session found the **cap semantics documented backwards**: `apiclient.Project` said an unset `max_parallel_tasks` "falls through to the global one", but `scheduler.admit` applies the two caps independently — the global always binds and the project's binds additionally when set, so a null cap means the project competes for the whole pool. Comment corrected and the column renders the two distinctly. `handleProjectDelete`'s two 409s are also genuinely different: only the non-archived-tasks one is forceable, so the confirmation keys on the server's own "pass ?force" phrase rather than on the status code — a `y/n` for the running-task case would have offered a "yes" that cannot succeed. **Creating a workflow file from the TUI is out of v1** (now recorded in §15): it needs a server-exposed global workflows directory, which `GET /v1/config` does not expose. `workflow.registry_changed` carries an empty payload, so any reload refetches every scope; a scope-tagged payload would let one project block refetch alone. A step whose agent resolves only at §8.6 level 4 still renders "adapter default", unflagged. One test-harness trap worth remembering: `httptest.Server.Close` blocks on a live SSE subscription, so the shell's context must be created *after* `t.Cleanup(ts.Close)` — cleanups run LIFO, and the reverse order hangs the test rather than failing it.
**PR N decisions (T3.7; grill session, 2026-08-09):**

- *The log tail is a local file read, not an endpoint.* §15 opens with "the TUI is a pure API client", but that line is about domain state: the TUI already reads `{data_dir}/daemon.json` and the token file just to connect (`apiclient.Discover`), and already renders `daemonLogPath()` on the failure screen. Decisive point is that the log matters most when the daemon is **dead**, and a `GET /v1/logs` cannot serve it then. §15 records the exception and its reason rather than leaving it implied.
- *Which means view 6 has to be reachable while disconnected.* `root.body()` gates every view behind `phaseConnected`, so as it stands that argument buys nothing — "daemon unreachable, press r" next to a path you must quit the TUI to open. `body()` special-cases `viewDaemon`: `6` renders the log pane and the three paths while down, `1-5` stay gated, and the failure screen gains `press 6 for the daemon log`.
- *The tail seeks from the end and never holds a handle.* `daemon.LogTail` reads the whole file (`os.ReadFile`, up to lumberjack's 10 MB) — fine for its one caller, wasteful for a pane that re-reads on a ticker. A bounded ~64 KB read from the end, open→read→close, is also the only rotation-safe shape: lumberjack rotates by **renaming**, and on Windows renaming a file another process holds open fails, so a `tail -f`-style open handle would have broken the daemon's own log rotation whenever this view was on screen. New `internal/daemon/logtail.go`; `LogTail` is rewritten on top of it so `daemon start` gets the bounded read too.
- *Two cadences, because two rates of change.* Info and config refetch on `viewActivatedMsg` and `R` only — config hot-reloads emit no event (`store/events.go` has just `workflow.registry_changed` and `daemon.shutting_down`), and polling `/v1/config` every second spends a request on a screen nobody stares at. The log alone rides a 2 s ticker, started on activate and stopped on deactivate, because you open this view *because* something is happening. Uptime is ticked locally from `started_at` rather than incrementing the fetched `uptime_seconds`, so it cannot drift. A durable `daemon.config_reloaded` event is the better long-term answer and is carried forward, not built here.
- *The log pane is a `viewport` with the detail view's follow semantics* — scrolling away drops follow, `f`/`G` re-arm, 500-line buffer. `f`/`G` already mean "back to live" in the output pane; making them mean something else here, or nothing, is the cost of not reusing it.
- *The first-run flag is TUI-local and fails open.* `{data_dir}/tui.json`, an open JSON object so later prefs join without a format change, holding a plain bool. Not the DB: a UI acknowledgment is not daemon domain state, and keying it there would require a live daemon to decide whether to warn. Not version-keyed: §16 says *once*, and re-warning each upgrade teaches people to dismiss the box unread. Missing, corrupt, unreadable and unwritable all mean **show the notice** — a security warning that suppresses itself on a JSON parse error has failed in the wrong direction. A failed write still dismisses for the session and reports as a dim status line.
- *The flag is written on acknowledgment, never on display,* so a `ctrl+c` two seconds in does not permanently bury a notice nobody read. The overlay renders at startup ahead of and independent of the connect flow (the connect `Cmd`s run behind it) — the warning is about what the daemon will do, so it must not wait on the daemon being reachable. That moves dirs resolution out of `probeCmd` and into `Run()`, resolved once and passed to both `newRoot` and the connector.
- *The overlay swallows everything except `enter` and `ctrl+c`.* Deliberately **not** `esc` or `q`: those are the keys people hit reflexively to make a box go away, and this is the one screen where that is the failure mode. `ctrl+c` stays live because the TUI must always be killable, and it exits without writing the flag.
- *Notice text is a const in `firstrun.go`,* ~8 lines compressed straight from §16's bullets so the two cannot drift: full-auto agents run arbitrary commands **as your user**, not confined to the worktree; worktrees isolate collisions, not privileges; mitigations are per-workflow/step `restricted` mode, full transcripts, and nothing merging or pushing unless a step does it. No file read — the fail-open rule above makes every I/O failure *show* the notice, so the text itself must have no failure mode at all.
- *The quit reminder is a line, not a prompt.* §15 says it *reminds*; quitting is non-destructive by construction, and PR K's rule reserves confirmation for destructive-and-irrecoverable. `p.Run()` returns the final model, so `Run()` prints after the alt screen tears down and the line survives in the scrollback — which is the whole point of "on exit". `running` only, off the board's already-fetched list, so quitting never blocks on a request; `queued` is doing nothing worth reporting and `awaiting_input` is waiting on *you*, which the bell already covers. Nothing printed at 0, and nothing when the board never loaded — "0 tasks running" from a TUI that never reached the daemon is a false statement.
- *View 6 reports, it does not act.* `POST /v1/daemon/stop` exists and stays unused: §15 view 6 lists five things to show and no verb, `vincent daemon stop` already owns it, and stopping the daemon from a TUI that auto-started it at launch leaves the app staring at the corpse it just made. Recorded in §15 as a negative, per PR M's precedent for TUI workflow creation.
- *Keys:* `R` refresh all three sources (same letter and meaning as the workflows view) · `f`/`G` re-arm follow · arrows/`pgup`/`pgdn` scroll. No `/` — nothing here is a list. `capturesInput()` is permanently false; this view has no text entry.
- *Identity block carries the three paths* — data dir, config dir, log path — because someone reading this view is usually about to `cd` to one of them. Plus version/commit/built, pid, uptime, listen. Config block: `max_parallel_tasks`, the three `defaults.*_timeout`, `transcript_retention_days`, `log_level`, both agent paths. Adapters: name, availability, resolved path, version, `supports_input`, error. `apiclient.Info` moves out of `tasks.go` into a new `apiclient/daemon.go` — `Info` describing the daemon has no business in the tasks file — widened with `commit`/`built`/`started_at`/`listen`, joined by a new `Config`.
- *Eleven named states, PR M's rule:* all three fresh · info fetch failed (identity and adapters keep last-good behind a dim `last updated Ns ago · <err>`) · config fetch failed, independently · activated but nothing fetched yet ("loading", not a blank frame) · log missing or unreadable (`(no daemon log: <err>)` in the pane alone) · log present but empty (a different fact from unreadable) · no adapters in the catalog · disconnected (log and paths live; identity/config `stale · daemon unreachable` if we ever had them, `unavailable` if we never did) · notice shown and unacked · ack write failed (dismissed anyway) · flag file corrupt (shown, fail-open, distinguishable in a test from a first run).
- *Testing:* one end-to-end, `TestDaemonViewReflectsLiveDaemon` — real in-process daemon, real client, real shell: activate view 6 and assert version/pid/listen/`max_parallel_tasks`/adapters equal what `/v1/info` and `/v1/config` actually return, and that the pane contains a line the real daemon really wrote. That is the done-condition verbatim and the only assertion proving two endpoints, the locally-derived path and the render agree about one running process. Two real-I/O tests that are not end-to-end but cannot be faked: a real `lumberjack.Logger` with `MaxSize` shrunk, written past the limit to force the rename, asserting the reader follows the new file and holds no handle (the Windows hazard, and CI runs all three OSes); and first-run across restarts — two shells over one temp data dir, then a third with the flag unreadable. Unit tests cover the bounded reader on files under/over/exactly the window and a last line with no newline, each of the eleven states, `R` refreshing all three, the ticker starting on activate and stopping on deactivate, follow dropped by scroll and re-armed, the quit line at 0 / at N / board never loaded, the overlay swallowing `esc` and `q` while passing `ctrl+c`, and `6` reachable while disconnected with `1-5` still gated.
- *Files:* `tui/daemon.go` + `daemonrender.go` (the model/render split every multi-state view uses), `tui/firstrun.go` (flag store, notice text, overlay — one concern, and none of it is a view), `daemon/logtail.go`, `apiclient/daemon.go`.
- *Commit order:* spec (§12.2 `tui.json`; §15 key line, the read-only negative, the local-file-read exception, view 6 reachable while disconnected; §16 where the ack persists and that it fails open) → `internal/daemon` bounded tail → `apiclient` → `tui` first-run → `tui` daemon view → root wiring (dirs in `Run`, gate exception, quit line, help overlay) → tasks.md. Docs-first per PR C/I/J/K/M, strict dependency order so every intermediate commit builds.

- [x] **T3.7 — Daemon view & first-run warning.** Version/uptime/config/adapters/log tail; one-time full-auto risk notice on first run (§16); quit reminder with running-task count. ✓ 2026-08-09
  *Done when:* warning shows exactly once (persisted flag); view reflects live daemon info. *(Verified: `TestDaemonViewReflectsLiveDaemon` drives the view against a real in-process daemon — real store, real broker, real adapter registry with one binary that resolves and one that does not, real handlers, real client, real shell — and asserts the rendered frame carries the version, pid, listen address, cap, log level and timeouts that `GET /v1/info` and `GET /v1/config` independently return, that uptime renders from the daemon's `started_at` rather than from the seconds since the view opened, and that both adapters appear with the availability a live probe gave them. The daemon's logger is wired to the real rotating `{data_dir}/logs/daemon.log`, so the pane is asserted to show lines that very server wrote — a seam with no HTTP in it, which nothing else in the suite crosses. The persisted flag is proven across restarts: two shells over one data dir, notice then no notice, and a third with the flag file unreadable showing it again. Unit tests cover the eleven named states, `R` re-reading the log with no daemon to ask, the ticker starting on activation and stopping off-screen, follow dropped by scroll and re-armed by `f`/`G`, the overlay swallowing `esc`/`q`/`1`/`n`/`?` while passing `ctrl+c` without writing the flag, a failed ack dismissing anyway and saying it will return, the state file keeping fields this build does not know, view 6 reachable while disconnected with 1–5 still gated, and the quit line at zero, at one, at several, and with a board that never loaded. `internal/daemon` covers the bounded reader on files under, over and at the window, an unterminated last line, empty versus missing, and a tail taken across a real lumberjack rotation.)*
  *Carried forward:* the rotation case is the reason the tail opens and closes on every read rather than following a handle — lumberjack rotates by **renaming**, and on Windows that rename fails while another process holds the file open, so a `tail -f`-shaped reader would have broken the daemon's own log rotation for as long as this view was on screen. CI runs that test on all three platforms. Two smaller findings: `apiclient.AgentStatus` was silently dropping the `path` the server had been sending all along (the board never rendered it, so nothing noticed), and `apiclient.Info` had been living in `tasks.go`. **There is still no `daemon.config_reloaded` event** — config hot-reloads are invisible to clients, so the view refetches on activation and `R` rather than following a reload live; a durable event is the better answer and is the natural companion to §12.3's hot-reload. `tea.Tick` cannot be cancelled, so the log ticker stops by not re-arming and one stray tick can arrive after the view goes off-screen; it is a no-op, but it is not a cancellation. The first-run overlay renders before the connect flow, so a launch whose directory resolution failed shows the notice and cannot record it — reported inline and shown again next time, which is the fail-open rule working as intended. `stubView` is gone: all six §15 views are real. The three `tui` commits landed as one — spec, `internal/daemon`, `apiclient` and `tui` stayed in dependency order and every commit builds, but the first-run, daemon-view and root-wiring split recorded above was not kept.
**PR N decisions (T3.8; grill session, 2026-08-09):**

- *The gate seeds and stops; a human drives.* M1/M2 are bash+curl in CI because an HTTP API has a scriptable surface; a TUI does not, and the Phase 3 decision already ruled out teatest and golden frames. So `scripts/m3-gate.sh` keeps the m1/m2 conventions (`hostpath`, env-override dirs, one build, temp everything) for **setup only** — build, two git repos, a bare remote, workflows, config — then prints a banner and exits. `docs/gates/m3-gate.md` carries the checklist and the results. No CI job: there is nothing here CI can assert that the Go suite and the two existing gates do not already.
- *The script never `exec`s `vincent`.* On Windows it runs under Git Bash, which hands a native console app a pipe rather than a console; a Bubble Tea alt-screen app in that host is a coin flip, and a gate that fails on its own terminal host proves nothing about the TUI. Printing a paste-ready launch line instead makes the terminal host a **recorded variable** (Windows Terminal + pwsh vs. Terminal.app) rather than an artifact of how the script was invoked, keeps both OS runs comparable, and lets the human relaunch after the deliberate reconnect kill without re-seeding.
- *`$EDITOR` is never overridden,* only reported. `editorCommand()` resolves `VISUAL` → `EDITOR` → `notepad`/`vi` (`tui/editor.go:32`); pinning it would test the script's own export instead of the product, and the Windows failure mode worth catching is precisely "the human's real editor does something on exit the TUI did not expect". The banner prints the resolved argv so the results record which editor was actually exercised.
- *Nothing is pre-registered and no daemon is pre-started.* Registration is leg 1 of the §19 loop and auto-start is an M3 deliverable — doing either in the script deletes a checklist item. `.vincent/workflows` written into a repo before registration is fine: `ReloadProject` reads it when the project appears and `watch.go:28` handles a directory created later. Two repos, so the Projects view is never a one-row list and delete-refused-by-a-running-task has somewhere to happen.
- *§19 M3 is amended, not satisfied.* Its acceptance says "author workflow … without leaving the TUI"; PR M recorded in §15 that **creating** a workflow from the TUI is out of v1. Two spec sections contradicting each other is a defect whichever one we act on, so §19 gets a footnote: "author" means edit-in-place via `e` → `$EDITOR`, creation is a filesystem act, see §15. PR N is a gate and does not grow the feature to make the older sentence literal.
- *Two workflows, and the author-edit is load-bearing.* `m3-loop` (global): agent ask+edit → command commit → manual gate → command push to the bare remote — m2-gate scenario 1's shape, with `model: haiku` pinned in the YAML where the spend decision is reviewable rather than typed into a form. `m3-parallel` (global) **plus a project-scoped shadow** in `{repo}/.vincent/workflows` that differs in step name and delay: PR M's "shadows global *X*" row is a claim about precedence, and only a visible difference makes the board's step column report which file actually won instead of the UI grading its own note. The walkthrough edits `m3-parallel` *before* creating the parallel tasks, so the human then watches their own edit render on three live rows — the fsnotify → reload → `workflow.registry_changed` → refetch chain reaching the screen.
- *Split adapters, one session.* `agents.claude.path` → real claude, `agents.codex.path` → fakeagent; the new-task form's per-task `agent` override (`tui/newtask.go:605`) picks. m2-gate's `VINCENT_GATE_AGENT=claude` re-run cannot demonstrate "without leaving the TUI" — it makes the human leave the TUI mid-loop to re-run a script. `VINCENT_GATE_AGENT=fake` survives as a zero-spend rehearsal. Real `claude` takes the question leg, where fakeagent's protocol can diverge and where §7.4 over a *TUI form* is untested today (m2-gate scripts `/answer` over curl). Its availability is preflighted **before** seeding: failing at leg 4 for an environment reason costs the whole walkthrough and reads, in the moment, exactly like a product bug.
- *`FAKEAGENT_DELAY_MS` is new.* The success path returns immediately, so three tasks can finish before the board paints them side by side, and a live tail that already ended is not a live tail. m2-gate's cap-stress dodged this with `run: sleep 2` and no agent at all — which renders none of the board columns PR I added a DTO for (step name, step total, cost, tokens), the exact columns on trial here. The knob gets an `agenttest` assertion: an untested delay fails silently as "the gate felt fast", which is indistinguishable from passing. Cap 3 with **four** tasks — three run, the fourth sits `queued` and admits on the first completion, which is the only M3 check that looks at admission at all.
- *Two checklist sections, one pass bar.* The loop is mandatory and its failure fails the gate. A sweep section takes one item per phrase quoted verbatim from §19's M3 contents column — six views, live tail, diff view, all actions, input-request alerts + answer form, `$EDITOR`, auto-start — plus the deliberate `daemon stop --force` (PR H made connect and reconnect the same state, so one kill proves both), a dirty archive answered `n` then `y` (the only destructive confirmation in the TUI, and the parallel tasks end dirty for free), and a third deliberately invalid workflow file. Quoting the spec is what stops the sweep from shrinking on the second OS run when the answer is already known.
- *Grading rule fixed before the walkthrough:* blocks the loop → gate fails, fixed in PR N; merely looks wrong → a new Phase 4 task ID under the "newly discovered work" rule. Decided in advance so a finding cannot be argued down to "cosmetic" after the fact because the gate is otherwise green.
- *Windows 11 + macOS.* Linux gets the full suite and both gates in CI on every PR; a third hand-run buys the least, and macOS is the machine whose terminal rendering can actually be judged. Recorded as a stated choice, not an omission. Both runs happen **on the branch** before merge — under the alternative the PR closing T3.8 merges before the task's own evidence exists, and a gate-fail would land a broken gate on `master` first. Any loop-blocking fix invalidates prior runs and both OSes are re-walked: `m3-gate.md` records a commit SHA per run, and two runs against two builds make that record a lie.
- *Results shape:* per run a header block (date, OS + version, terminal host, vincent commit, `claude --version`, resolved `$EDITOR`), a pass/fail/n-a table keyed to item IDs, and findings each carrying a Phase 4 task ID or "fixed in PR N" — Q6's rule only works if a finding is written next to its disposition, or "deferred" and "forgotten" become the same artifact.
- *Commit order:* tasks.md `[~]` + this block → spec (§19 footnote, §15 cross-reference) → `cmd/fakeagent` delay knob + test → `scripts/m3-gate.sh` → `m3-gate.md` checklist → walkthroughs → results + tasks.md `[x]`. Docs-first per PR C/I/J/K/M.

- [x] **T3.8 — Phase gate (M3 acceptance).** ✓ 2026-08-10 Manual scripted walkthrough of the §19 M3 loop on Windows + one POSIX OS, using fake agents for parallelism and real `claude` for one task; results recorded here.
  *Done when:* walkthrough passes on both; notes committed.
  *2026-08-09:* deferred behind T3.9–T3.13 (see the TUI refactor decisions below). The walkthrough runs **once**, against the panel UI, and never against the one being replaced; its sweep section grows the items listed in T3.13. `scripts/m3-gate.sh` and section A of `m3-gate.md` carry over unchanged — the seed is UI-agnostic and the §19 loop is spec-derived.
  *2026-08-10:* first Windows pass surfaced four findings before any run was recorded; graded per the rule above (none block the loop). Fixed pre-run: console windows flashing for every daemon child (procx/gitx `CREATE_NO_WINDOW`), the takeovers' duplicate key rows and missing frames (both runs will judge the fixed build). Deferred: naming what "(workflow default)" resolves to in the new-task form → T4.7.
  *2026-08-10 (first full Windows walkthrough):* Section A **passed** — the §19 loop completed without leaving the TUI — with nine findings, all fixed pre-record so the recorded runs judge one build:
  1. **The task table stopped updating** (`0/3 running` with four tasks queued; only a restart showed them). Not cosmetic: `root` routed every non-input message to the *active* view, so the board's refresh debounce fired into the new-task form, `refreshPending` stayed true and every later task event was ignored. The elapsed ticker died the same way. Input is now routed, background work is broadcast. This predates the refactor; the fused screen is what made it visible.
  2. Clicking blank space below the last row selected the last task (the move clamps) — clicks now land only on rendered rows.
  3. The Projects table ignored the bubbles cell padding and overflowed by a column's worth, cutting `running / cap`; the path column also grew unbounded. Widths now degrade like the board's (squeeze the name, shed workflow then branch) with the path capped, and the row builder shares one column set with the header.
  4. `?` listed all eight surfaces' sections; it is now the focused surface's keys plus the globals, framed and titled with the surface name.
  5. `?` showed the underlying surface's footer while open; it now carries its own key row.
  6. The palette's group headings looked like its entries; sections are now styled and ruled, with navigation in its own **views** section.
  7. "(workflow default)" named no agent. The registry already reports each step's agent (§8.6 levels 1 and 3), so the form now reads "(workflow default → claude, adapter default)" from the server's own report. Model and effort stay unnamed — the API does not report them per step, which is exactly T4.7's remaining scope.
  *2026-08-10 (first full macOS walkthrough, Ghostty, real claude):* Section A **passed** again — the §19 loop completed without leaving the TUI — with three findings, none loop-blocking, all fixed pre-record so the recorded runs still judge one build. Written up in `m3-gate.md` under "Shakeout findings"; in short:
  1. Clicking a task row selected the row two above it (S14). The click origin borrowed `board.chromeLines()`, a *height* budget that also counts the action line rendered **below** the table; the lines above the rows are now their own `board.firstRowLine()`. The old test agreed with the bug because it recomputed the same formula — the regression test now finds the target line in the rendered frame.
  2. Paste did nothing anywhere, so L2's "paste the seeded app-repo path" was not performable. `tea.PasteMsg` is neither a key nor a mouse event, so the root broadcast it and every view dropped it; `ctrl+v` failed separately because bubbles answers it with an unexported message only textinput understands. Paste now follows the *key* routing to the one field holding the keyboard, and `ctrl+v` reads the system clipboard into that same path. A paste with no field under it is dropped rather than replayed as keystrokes — on the board that would fire `a`/`c`/`r` as task actions.
  3. The gate banner's `export` lines outlive the walkthrough, and the next `go test ./...` in that shell inherits `FAKEAGENT_DELAY_MS` and `FAKEAGENT_SCENARIO_CODEX` — `cmd/fakeagent` and `internal/agent/codex` fail looking exactly like real regressions. The POSIX launch block is one-shot `env VAR=… vincent` now, and teardown tells pwsh how to clear its session.
  Also fixed while enumerating paste targets: the new-task form's key/value editor was missing from `capturesInput()`, so typing `q` into a field name quit the TUI.
  *2026-08-10 (recorded runs — **GATE PASS**):* both walkthroughs re-walked on `master` at `40fbffe`, the first build carrying every shakeout fix, so the commit-SHA-per-run rule holds with one build for both. **Windows 11** (Windows Terminal/pwsh, plus Git Bash/mintty for S14's second host) and **macOS 26.5.2** (Ghostty 1.3.1), real `claude` 2.1.226 on the question leg, platform-default editor on each. Section A L1–L10 pass on both — the §19 loop completes without leaving the TUI — and Section B S1–S18 pass, with macOS reading S14 as its Ghostty equivalent since the two Windows hosts are the Windows run's item. No new findings in either recorded run; every earlier finding was re-checked against this build. Results tables in `docs/gates/m3-gate.md`. Phase 3 closes at 13/13; T4.7 carries the only deferred item (naming what "(workflow default)" resolves to for model and effort, which the API does not report per step).

**Phase 3 TUI refactor decisions (grill session, 2026-08-09):**

The M3 TUI reached T3.8 as six full-screen views routed by `1..6`, with 42 distinct
keystrokes bound and 45 help rows across nine groups behind `?` (measured in
`tui-friction.md`). It works and it is tested; it is
also unlearnable without the overlay open. The refactor keeps every capability and
changes how it is reached. T3.1–T3.7 stay `[x]` — they are not undone, they are
superseded in layout and routing by T3.10–T3.13.

- *Refactor first, then gate — the outgoing UI is never walked at all.* T3.8 is acceptance evidence, and evidence for a UI about to be deleted is worth nothing: a two-OS walkthrough with real `claude` costs hours of human time and pins a commit SHA that the next PR invalidates. Nothing is discarded by skipping it, because nothing was recorded — `m3-gate.md` holds a template with no run in it, so T3.8 simply stays `[~]` until T3.13 lands. The gate's *artifacts* do carry over: `scripts/m3-gate.sh` never touches the TUI (it seeds two repos, the workflows and a bare remote, then prints a paste-ready launch line) and section A is quoted from §19, so only section B's key-specific sweep items are rewritten, in T3.13.
- *The friction record is derived, not walked.* `tui-friction.md` still exists and is still what the refactor is graded against, but its provenance changes: it is read off the current binding surface — 42 bound keystrokes against 45 help rows in nine groups, the six views behind `1..6`, `actionbar.go`'s hints being task-scoped only — rather than harvested from a session at a terminal. The derivation is reproducible and the measurement method is stated in the file, which is what stops the "before" figure being renegotiated once the "after" is known. The friction is already legible in the source; spending a human hour to rediscover it buys nothing that reading `help.go` does not. What matters is that the list is written down **before** the refactor, so "is this better?" is settled against a fixed record instead of taste at review time.
- *Bubble Tea v2 stays.* lazygit's feel comes from layout and contextual menus, not from gocui; tview and gocui would both discard ~7k lines, every `*live_test.go`, and the Phase 3 framework decision to buy nothing the current stack cannot do. bubbletea v2 has mouse and focus, lipgloss v2 has borders and joins. No goal below needs a different engine.
- *Hybrid, not full lazygit.* The daily loop (board → detail → back) fuses into one persistent screen; projects, workflows, daemon and new-task stay full-screen takeovers, reached from the command palette instead of `1..6`. A full lazygit port would force §15's eight-field new-task wizard into a popup, which is worse than what exists. Config surfaces visited twice a week do not earn permanent screen width.
- *Three panes, accordion.* Top: the full-width task table, keeping all eight §15 columns — a 30%-wide left rail would delete five of them, and cost and step `k/n` are spec'd and load-bearing. Bottom: timeline (left) beside output/diff (right), which satisfies §15's "neither half can hide behind the other" as well as the stacked layout did. The focused panel expands and the others collapse to title + one line; the task table never drops below 5 rows because it is the navigation spine. Below **80×20** the shell falls to single-panel mode (focused panel full screen, `tab` swaps); below **60×15** it renders an explicit `terminal too small (58×14, need 60×15)`. A silently illegible layout is worse than a stated floor, and the floor is testable.
- *Discoverability is three mechanisms, landed in order:* the `:` command palette, then the contextual footer, then mouse. `:` has no collision in the current bindings, reads as "command" to anyone, and leaves `x` (reject gate) alone. The palette lists task actions valid **right now**, navigation to the four takeover screens, and panel-local commands — each with its direct key beside it, so it teaches shortcuts instead of replacing them. Invalid task actions are omitted rather than greyed, preserving §15's existing invariant that an invalid action is never on screen. Navigation living in the palette is what retires `1..6` without substituting another memorized key.
- *The footer never wraps.* One line: focused-panel keys (max 5, priority-ordered) · valid `available_actions` · pinned right and never truncated, `: commands  ? help  q quit`. `:` is the escape hatch that makes every other key optional, so a narrow terminal truncating it away would fail exactly when the user is most lost.
- *One binding registry.* `bindings.go` declares key, label, scope (global / panel / task-action) and priority; the footer, the palette and the `?` overlay all render from it. Three hand-maintained lists drift within two PRs and the drift is invisible until a user presses a key the help promised. The registry also gives the footer its ordering for free and turns "is every key discoverable?" into a test.
- *Keys are unhidden, not cut.* Only `1..6` dies; one key is born (jump to next attention task). 45 → ~40 is not the win — memorization pressure dropping to zero is. Cutting the surface further means deleting capability, which is a different and more destructive project, and was explicitly considered and rejected in this session.
- *Panels are the existing sub-models.* The `view` interface is already `render(width, height)`, which is what a panel needs; it is renamed to `panel`, a new `shell.go` owns layout, focus and the accordion, and `board.go`/`detail.go` survive as sub-models rendering into a smaller box. Most render assertions in `board_test.go`/`detail_test.go` hold because the output did not change, only the box. A greenfield rewrite would discard working tested code for the same pixels. `internal/tui` stays one package: panels share unexported style vars, msg types and the client handle, and splitting would buy an export surface for no isolation.
- *No teatest, no golden frames — still.* Layout and hit-testing become **pure functions** instead: `layout(w, h, focus) → []box` and `hitTest(x, y, []box) → panelID`, table-tested without rendering a frame. Golden frames across the 3-OS matrix is how the Phase 3 decision predicted flaky CI, and nothing has changed. Mouse tests stay in the established pattern: feed `MouseClickMsg{x,y}`, assert focus.
- *The fuse creates an SSE bug, so it gets an acceptance criterion.* The detail view subscribes per task for the live tail; once the panels track the cursor, holding `down` across twenty rows opens and tears down twenty streams. Selection→subscribe is debounced (~250ms settle) and only fires for a task that is actually running, with an explicit unsubscribe on move. Tested by holding `down` and asserting one subscription — untested, it ships as "the TUI felt laggy during the gate" and gets misfiled as rendering.
- *`esc` cancels one layer per press,* by an explicit stack written into §15: popup (palette / confirm / answer form) → takeover screen → active filter → no-op. It never quits. "Back to board" disappears as a meaning because you are always on the board. `/` becomes panel-local and filters whichever list has focus; `tab` **commits** the filter and moves focus, leaving it applied and visible in the panel title, and only `esc` clears it. The shell consults `inputCapturing` on the focused panel only.
- *The answer form is an interrupt, so it is a popup that never steals focus.* §15 already treats `awaiting_input` as an interrupt — pinned to top, terminal bell — and a tab you have to go find is the wrong shape for one. A popup also gets full width for multi-select options and free-text, which a quarter-pane does not. Auto-opening mid-keystroke is how input is lost, so it arrives as a badge plus a footer hint and the human presses the key. One new key jumps to the next attention task, surfaced in the footer only when the count is non-zero.
- *Mouse ships on by default,* with `M` to toggle and the toggle in the palette. Terminal text-selection loss is real, but every terminal has a modifier override (shift-drag; option-drag on Terminal.app/iTerm) and the toggle covers the rest; off-by-default makes the discoverability feature undiscoverable. Scope: click-to-focus, click-row-select, wheel scroll in the focused panel, click a footer hint to fire it, click a tab. Not drag, not right-click.
- *Disconnection stops hiding the screen.* Panels stay rendered with contents marked stale behind a banner and `r` to retry; nothing force-navigates. PR H already made connect and reconnect the same state, and the last-known task table is information as long as it is labelled. `:` still reaches Daemon. This **amends §15's "views 1–5 stay behind the connection"** and lands in the T3.9 docs PR rather than arriving with the code.
- *Fixed palette, no theme knob.* Borders, focus colour, the existing state colours. Degrades under `NO_COLOR` and on 16-colour terminals (lipgloss v2 handles the downgrade; T3.10 asserts it). A theme setting is config surface, a docs section and a support burden for zero acceptance value — Phase 4 material if anyone asks.
- *§15 is amended in place; §19 is not touched.* v0 is unreleased and `docs/versions/` holds only `v0/`, so forking a v1 spec for it would produce two documents that disagree with no reader able to tell which is live. §15 keeps its numbered 1–6 list as the **capability** contract — all six surfaces still exist, only routing and layout change — and only its layout and key prose is rewritten. T3.8's sweep item quoting "six views" therefore survives verbatim and §19 collects no second footnote.
- *No feature flag, no coexistence.* Two UIs means maintaining two, testing two, and a gate that must record which one it walked; v0 has no user base a flag would protect. `master` stays coherent because T3.10 fuses board and detail in one PR rather than half-migrating.
- *Five PRs, docs first, each independently green* — O: T3.9 · P: T3.10 · Q: T3.11 · R: T3.12 · S: T3.13. Each opens with its own grill block, per the Phase 2/3 pattern.
- *Success is measured against the "before".* Every item in `tui-friction.md` becomes a checkbox re-checked during the T3.8 walk, each carrying its disposition — fixed, deferred to a Phase 4 task ID, or won't-fix with a reason. Without that, "is it easier now?" is settled by whoever argues hardest in the PR thread, which is how 45 keys happened.

- [x] **T3.9 — Refactor spec & friction record (docs only).** ✓ 2026-08-09 Rewrite §15's layout and key prose for the three-pane panel shell (`:` palette, footer, `esc` stack, panel-local `/`, disconnected banner, terminal floors), keeping its numbered 1–6 list as the capability contract and leaving §19 untouched. Add `docs/history/v0-tui-friction.md`, derived from the current binding surface (`internal/tui/help.go`, the `1..6` routing in `views.go`, `actionbar.go`'s task-scoped hints) — the outgoing UI is not walked.
  *Done when:* §15 describes the target UI with no stale key table; `tui-friction.md` lists concrete friction items, each phrased so T3.8 can re-check it.
**PR P decisions (T3.10; grill session, 2026-08-10):**

- *The answer form becomes the §15 popup in this PR, not later.* The fused layout has no inline slot — the form used to take half the output pane, and boxing that into a quarter-height panel is exactly the shape the refactor decisions called wrong for an interrupt. Building it inline now means building it twice. It opens on a human key press only — `enter` on an `awaiting_input` row — and `esc` closes it; it never auto-opens and never steals focus. The announce surfaces stay where they were planned (jump key T3.11, footer hint T3.12); until then the row badge and the bell are the announcement, which is what they already are today.
- *The disconnected code lands here.* §15's amended "Disconnected" prose (T3.9) is implemented by the same PR that rewrites root's body rendering: `reconnecting`/`failed` with a loaded board renders the panels marked stale behind a banner with `r` to retry. The initial probe/start keeps the current connect screens — before the first connection there is no "last known task table", so a stale empty shell would be a lie, not information. Takeover screens keep the connection gate; the daemon view exception is unchanged.
- *The task fetch rides the same settle as the subscribe.* The refactor decision pinned only the SSE subscription to a ~250ms settle; the `GetTask` + transcript fetch join it rather than firing per cursor move. One mechanism, one test, and a 20-row hold-down costs one fetch instead of twenty. Cursor move = immediate unsubscribe + placeholder; settle = fetch + subscribe iff the task is running.
- *The accordion is vertical, between bands.* "Collapse to title + one line" applies to the unfocused band, not to the timeline/output pair individually: collapsing the output while navigating the timeline would defeat the reason they sit side by side — selecting an attempt *is* how scrollback is navigated (§15). Focus on tasks → the bottom band is two title+line strips; focus below → the tasks panel sits on its 5-row floor and the band keeps its 40/60 split.
- *`detail` stays one sub-model rendering two boxes* (timeline, output|diff). Splitting it into two models would divorce the timeline cursor from the transcript state it drives, for no testing gain; the shell's focus maps onto its internal focus.
- *Two action lines is the accepted interim.* Action keys route to the focused panel's bar — board's inside the tasks panel, detail's below the band — both gated on the same `available_actions` for the same selected task. T3.12's footer is the PR that unifies them; dragging that work forward would make this PR two PRs.
- *`1`/`2` collapse onto the fused screen* (`2` focusing the timeline) and `3..6` keep their meanings until T3.11 retires the digits — capability parity during the interim, no new keys born early.

- [x] **T3.10 — Panel shell; board and detail fused.** ✓ 2026-08-10 `panel` interface (renamed from `view`), `shell.go` owning layout/focus/accordion, `layout.go` as a pure `layout(w, h, focus) → []box`. Three panes (task table · timeline · output|diff tabs), focused-panel borders, single-panel mode below 80×20, explicit too-small below 60×15. Debounced running-task-only SSE subscription. `NO_COLOR` and 16-colour downgrade. *Depends:* T3.9.
  *Done when:* `layout` is table-tested across sizes and focus targets; holding `down` across the table asserts one subscription, not one per row; existing board/detail render assertions pass against the boxed sub-models.
  *2026-08-10:* landed per the PR P decisions above. Also fixed in passing: resizing across a board column breakpoint crashed bubbles' table (stale wider rows re-rendered against the narrower column set) — found by the new single-panel-mode test, fixed in `board.render`.
**PR Q decisions (T3.11; grill session, 2026-08-10):**

- *`!` is the attention-jump key.* §15 named the behavior but not the key; `!` reads as the attention marker itself, sits on shift+1, and collides with nothing. The jump moves the table cursor to the next attention row (wrapping, in pinned order) and opens it immediately — a jump is deliberate, so it skips the settle window exactly like `enter`.
- *The palette executes by synthesizing the entry's direct keypress.* One execution path means the palette can never diverge from the shortcut it teaches, and confirmations still confirm because the synthetic key walks the same code the real one does. Keyless entries — navigation to projects/workflows/daemon — emit the view switch directly; retiring `1..6` without substituting new memorized keys is the point, so those three deliberately have no key. New task keeps `n` (§15 global).
- *The esc stack is §15's, with takeover-internal layers inside the "takeover" entry.* Coarse order: popup (palette · confirmation · answer form) → takeover screen → active filter → no-op, and it never quits. A takeover's own layers (leave the field → discard prompt → its local filter) sit inside its stack entry, exactly as the new-task flow already behaves; projects/workflows/daemon gain the missing bottom layer, esc-leaves-to-home. T3.10's interim esc-focuses-the-table is dropped — `tab` is the focus key.
- *`tab` commits the board filter and moves focus; `enter` still commits in place.* The committed filter renders in the Tasks panel title and only esc clears it. `/` on the timeline/output panels is a no-op: §15 names the filterable lists — tasks, projects, workflows — and those two panes are not lists.
- *`[`/`]` land here as the output pane's tab-switch keys,* `d` kept as an alias (§15 keys): the registry is the PR where keys become discoverable, so the aliases arrive with it.
- *Digits become no-ops and every mention is reworded:* the failure screen's "6 for the daemon log" becomes "`:` reaches the daemon view", the empty board's "press 3 to create one" becomes "press n", and the interim root footer gains `: commands` ahead of T3.12's real footer.
- *`?` renders from the registry,* grouped by scope and context, carrying the same prose the hand-written help had. The reachability test opens the palette in each owning context and asserts every registry entry surfaces — "is every key discoverable?" becomes a test, per the refactor decision.
- *While disconnected the palette omits task actions* (nothing can act on a task the daemon cannot see) but keeps navigation and panel commands — `:` is how §15 says the daemon view stays reachable.

- [x] **T3.11 — Command palette; `1..6` retired.** ✓ 2026-08-10 `bindings.go` registry (key, label, scope, priority) as the single source for palette, footer and `?`. `:` opens a searchable list of valid task actions + navigation to the four takeover screens + panel-local commands, each showing its direct key; invalid task actions omitted. `esc` stack, panel-local `/`, `tab`-commits-filter. Jump-to-next-attention key. *Depends:* T3.10.
  *Done when:* `1..6` is gone and every takeover screen is reachable from `:`; a test asserts every registry entry is reachable from the palette; the `?` overlay renders from the registry, not a literal list.
  *2026-08-10:* landed per the PR Q decisions above; `TestPaletteReachesEveryRegistryEntry` is the reachability sweep.
**PR R decisions (T3.12; grill session, 2026-08-10):**

- *Confirmations and statuses live in the footer.* The task text says "generalize `actionbar` into the footer", and that includes everything the bar carried: a pending y/n replaces the footer's left segments outright — it owns the keyboard anyway, and the pinned chrome survives it — while result statuses ride after the action hints and are the first thing left-truncation eats. Relocating confirms to a popup was considered and rejected: more motion for a y/n question, and a second popup layer to stack esc through.
- *The event line is removed.* T3.1 needed visible proof that external events reach the TUI; the live panels are that proof now. §15's layout sketch has no such row, stream liveness is already the connection badge and the stale banner's job, and every layout — the 80×20 floor included — gains a content row. The T3.1 live tests keep their teeth by asserting the event-driven board refresh renders the row itself, which is a stronger claim than echoing the event id.
- *One `actionBar`, owned by the shell.* PR P accepted two bars as interim; the footer is where they merge. Board and detail share the shell's instance (their standalone constructors keep private ones so unit tests stand alone), so a confirmation started from any panel is the same pending question wherever the eye lands.
- *The registry grows a short `hint` per footer-worthy row.* The palette and `?` read the long labels; a one-line footer cannot. Rows without a hint stay out of the footer; priority orders the five that fit.
- *The chrome shrinks by two rows* (event line, the shell's interim bar line): `shellChromeH` drops from 4 to 2 and the §15 floors stay stated in terminal cells — the layout tests shift automatically because they are written against the derived constants.
- *Truncation is literal §15:* the composed left content is cut from the left with a leading `…` when the pinned segment would not fit; the pinned `: commands  ? help  q quit` is never touched. `r retry` joins the left content while the connection is down, and the `!` hint carries its count and exists only when that count is non-zero.

- [x] **T3.12 — Contextual footer.** ✓ 2026-08-10 Generalize `actionbar` into a one-line, never-wrapping footer: focused-panel keys (max 5, priority-ordered) · `available_actions` · right-pinned `: commands  ? help  q quit`. Left-truncates with `…`; the pinned segment never truncates. Attention-jump hint appears only when the count is non-zero. *Depends:* T3.11.
  *Done when:* narrow-terminal tests assert the pinned segment survives and the line never wraps.
  *2026-08-10:* landed per the PR R decisions above; the event line is gone and the T3.1 live proofs now assert the event-driven refresh renders the change itself.
**PR S decisions (T3.13; grill session, 2026-08-10):**

- *The wheel scrolls the focused panel, not the hovered one* — §15 verbatim. Hover tracking is drag-adjacent machinery for a behavior §15 does not ask for.
- *A click on a row selects it and rides the settle window,* exactly like cursor movement — §15 says "click a row to select it", and a click that opened would make selecting without opening impossible. Opening stays `enter`'s job.
- *Row mapping reads the rendered frame, not bubbles' private scroll state.* The table's scroll offset is unexported and its clamp logic is not a contract; the clicked line's distance from the styled cursor row drives `MoveUp`/`MoveDown` — the same path as key movement, so the table's own bookkeeping stays consistent.
- *Footer hints fire by synthesizing the hint's key* — the palette's one-execution-path rule again; the hit ranges are computed from the same segments the footer renders, truncation included, so a hint that was cut cannot be clicked.
- *The palette and the answer popup ignore clicks.* §15 scopes the mouse to focus, select, scroll, footer and tabs; popup interaction stays keyboard, and a stray click must not answer a question.
- *The mouse rides `tea.View.MouseMode`* — bubbletea v2 sets it per frame — so `M` is a flag flip: on by default per §15, the toggle in the registry and therefore the palette and `?`.
- *Takeover screens get wheel delegation for free and no click surfaces.* Nothing §15 lists exists there; their lists stay keyboard until someone misses it at the gate.
- *The T3.8 sweep grows five items and three are reworded:* S1/S8/S11 were written against the retired digits and the old connect screen; they now walk the palette route and the stale-panels banner. New items cover mouse on both Windows hosts, resize across both floors, palette completeness, the accordion at 24 rows, and `NO_COLOR`.

- [x] **T3.13 — Mouse.** ✓ 2026-08-10 Click-to-focus, click-row-select, wheel scroll in the focused panel, click a footer hint to fire it, click a tab. `hitTest(x, y, []box) → panelID` as a pure function. On by default, `M` toggles, toggle listed in the palette. Grow the T3.8 checklist: mouse on Windows Terminal + Git Bash, resize across both floors, palette reachable and complete, accordion at 24 rows, `NO_COLOR`. *Depends:* T3.12.
  *Done when:* `hitTest` is table-tested; `MouseClickMsg` tests assert focus and selection changes; `m3-gate.md`'s sweep carries the new items.
  *2026-08-10:* landed per the PR S decisions above; the sweep gained S14–S18 and S1/S8/S11 were reworded off the retired digits.

## Phase 4 — Polish (M4)

Milestone acceptance (§19 M4): fresh machine → first completed task in under 10 minutes on each OS.

**Phase 4 decisions (grill session, 2026-08-10):**

- *Delivery:* 7 PRs, one per task, lettered T–Z (Phase 3 ended at S) — **T:** T4.7 · **U:** T4.2 · **V:** T4.3 · **W:** T4.1 · **X:** T4.5 · **Y:** T4.4 · **Z:** T4.6. Two real dependencies set the order: `workflow validate [file]` (U) is what proves the shipped examples (Y), and the README's install instructions (Y) can only be honest once packaging exists (X). T4.2 and T4.4 stay apart despite touching the same surface — one is code with integration tests, the other is prose, and a mixed review serves neither. Each PR independently green in CI; tasks.md updated per PR; each opens with its own grill block (Phase 2 pattern).
- *Service install (W):* hand-rolled per platform, `golang.org/x/sys/windows/svc` for the Windows leg. `sc.exe create` pointed at today's binary would die with error 1053 — SCM expects control-code replies — so `vincent daemon` gains an `svc.IsWindowsService()` branch whose `Stop` handler cancels the very context `daemon.go:139` already drains on; nothing about shutdown is reimplemented. `x/sys` is already in the module graph (`go.mod:17`), so the Windows leg adds no dependency. launchd plist and systemd user unit are ~15 lines of template each in `_darwin.go`/`_unix.go`. Rejected: `kardianos/service` (it would own the one platform seam this repo has always kept in build-tagged files — the same bias that chose the git CLI over go-git and a hand-rolled data-dir resolver over xdg), and a logon Scheduled Task instead of a Service (contradicts §12.1's wording).
- *CLI (U):* human table by default, `--json` on every subcommand. **No auto-start** — `vincent task ls` errors with a pointer to `vincent daemon start`; bare `vincent` keeps auto-start because a TUI is an interactive session you asked for, while a `task ls` inside a shell loop silently spawning a daemon is a surprise. Exit codes 0/1 plus **2 for an unreachable daemon**, so scripts can tell "no daemon" from "your request was rejected"; no finer taxonomy. `workflow validate [file]` is local `internal/workflow` parsing and runs with no daemon at all; `workflow ls` serves the merged registry and therefore requires one.
- *Retention & limits (V):* `transcript_retention_days` already exists (`config.go:54`) — pruning was simply never written, so retention needs no new config. The size cap is the missing knob: new top-level `transcript_max_bytes` (default 512MB) beside it — **spec addition** to §12.3 and `configResponse` — not under `defaults:`, which is timeouts only, and not a hardcoded constant, because the done-when demands tests at shrunk thresholds and a constant forces a test-only override hatch. Pruning runs at daemon start **and** on a 24 h ticker: a daemon that survives reboots — the entire point of W — would otherwise prune only on the restarts it no longer has. The cap's failure joins the shared vocabulary as `ReasonTranscriptLimit` = `transcript_limit`. Log rotation already ships (`logging.go`, 10 MB via lumberjack); V adds its test, not its code.
- *Packaging (X):* goreleaser, cross-compiled from a single ubuntu runner — `modernc.org/sqlite` is pure Go and no `CGO_ENABLED` appears in the magefile or CI, so a 3-OS *build* matrix buys nothing; per-OS runners exist only to smoke-test the produced artifacts. **Signing is descoped** to cosign keyless signatures + checksums + GitHub build attestations: Authenticode and Apple notarization are recurring cert purchases (~$99/yr Apple Developer ID, hardware-token OV for Windows) that v1 does not make. §19's "signed binaries" carries a dated descope note, and the README documents the Gatekeeper and SmartScreen prompts — including `xattr -d com.apple.quarantine` — instead of pretending a user won't meet them.
- *Distribution (X):* GitHub Releases only — archives plus checksums. No Homebrew tap, scoop bucket, or winget manifest in v1: each is a second repo and a release-time publish step maintained forever, and the quickstart they would shorten is already "download, unzip, run".
- *§8.6 reporting (T):* new **`POST /v1/resolve`**, taking a workflow identifier plus the task-level override triple and returning each step's resolved `{agent, model, effort}` tagged with its source (`step`/`task`/`workflow`/`adapter`). Chosen over widening the workflow DTO because the two callers ask different questions: the workflow list asks what a step resolves to *as written*, while the new-task form asks what it resolves to *under the overrides currently being typed*, which have no server-side existence yet. One endpoint with an empty override body answers both. This honors the PR L decision rather than relitigating it — resolution stays server-side, and §8.6's agent-scoped inheritance rule keeps exactly one implementation.
- *Docs (Y):* README grows install + quickstart + a prominent §16 full-auto risk section; the authoring guide lands as `docs/workflows.md`; `examples/` ships three (`feature-pr`, `fix-and-test`, `docs-update`), each validated by `vincent workflow validate` in CI so a broken example fails the build rather than a reader.
- *M4 gate (Z):* follows the `m3-gate.md` precedent — a human walkthrough doc hand-run on Windows 11 and macOS, Linux covered by CI. "Fresh machine" is a clean VM with **no Go toolchain**, which is what proves the released artifact self-sufficient. The ten-minute clock starts at downloading that artifact and stops at the first completed task; it **includes** Gatekeeper/SmartScreen friction, which is vincent's own cost to pay, and **excludes** installing and authenticating the agent CLI, which is a documented prerequisite — §19's promise is about vincent's install path, not about Anthropic's or OpenAI's onboarding, and a gate whose clock is dominated by a third-party login measures the wrong thing.

- [x] **T4.1 — Service install.** ✓ 2026-08-12 `vincent service install/uninstall`: Windows Scheduled Task, launchd agent, systemd user unit (§12.1).
  *Done when:* install → reboot-survival → uninstall verified on each OS (manual, recorded here).
  *2026-08-11:* code complete per the PR W decisions. `internal/service` with one build-tagged backend per platform plus an `_other.go` that returns `ErrUnsupported` so vincent still builds off the three CI OSes. `vincent service install/uninstall/status`, all cross-compiling clean for windows/darwin/linux. *(The Windows backend here is the LocalSystem service **T4.19 replaced** on 2026-08-12; the rest stands.)* **`vincent daemon` gained the `svc.IsWindowsService()` branch** (`internal/daemon/svc_windows.go`): its Stop handler cancels the context `Run` already drains, so §12.4's shutdown is not reimplemented, and it waits for that shutdown before reporting `Stopped` — reporting early would let the SCM kill the process mid-teardown.
  *Decisions made while building:* the resolved config/data dirs are **written into the unit**, because a service does not inherit the installing shell and would otherwise silently use a different database than the CLI. `KeepAlive`/`Restart` are conditional on a *failed* exit on both POSIX platforms — unconditional restart would make `vincent daemon stop` impossible. systemd needs `loginctl enable-linger` to survive logout; the installer attempts it and, on failure, reports the exact command as a **warning** rather than failing an install that otherwise worked.
  *Found while testing:* on Windows `mgr.Connect()` requests `SC_MANAGER_ALL_ACCESS`, so `service uninstall` with nothing installed reported "access is denied" to an unelevated user who had nothing to uninstall. `query` now opens the SCM with `SC_MANAGER_CONNECT` and the service with `SERVICE_QUERY_STATUS`, and `uninstall` checks existence through it first — absence is answerable without privileges.
  *Verified:* unit/plist rendering (build-tagged, incl. the plist parsed as XML), the dirs-capture decision, label stability, and a CLI e2e leg covering `service status`/`uninstall` with nothing installed on all three OSes. **The done-when itself was never automatable:** install → reboot → uninstall by hand on each OS — the table below, filled in over 2026-08-11/12. Windows once additionally needed an elevated prompt, which CI runners do not give; T4.19's Scheduled Task removed that, which is why the closing walk needed no Administrator prompt at all.

| OS | Installed | Survived reboot | Uninstalled | Notes |
|---|---|---|---|---|
| Windows 11 | ✓ | ✓ | ✓ | 2026-08-12, owner, **re-walked** against the Scheduled Task backend — the first walk's ✓✓✓ was against a LocalSystem service and proved nothing about what replaced it. Three defects came out of the two walks: **T4.19** (a LocalSystem daemon no client could see), **T4.20** (a console window whose close button stopped the daemon) and **T4.21**/**T4.22** (hiding that window lost a race; a timed-out probe cached forever). The middle column now means *starts at the next logon* |
| macOS | ✓ | ✓ | ✓ | 2026-08-11, owner. Clean on all three legs — but the running service could not see any agent CLI: **T4.15**, re-verified against a real launchd on 2026-08-11 |
| Linux | ✓ | ✓ | ✓ | owner, reported clean on all three legs. `loginctl enable-linger` was not reported either way, so surviving **logout** — as opposed to the logon start all three platforms now promise — rests on the installer's warning path and its unit tests, not on this walk |

  *2026-08-12 — closed by the owner: three OSes, three legs.* The Windows row was re-walked end to end against the backend T4.19–T4.22 left behind, and reported clean. That re-walk carries **T4.21**'s remaining leg with it: the middle column *is* a real logon, with the cold Windows Terminal start whose ordering defeated T4.20's `ShowWindow` hide and which is the only place `FreeConsole` could have been disproved. No per-command transcript was captured, so what is recorded here is the owner's sign-off on the checklist as written — install unelevated, the daemon up at the next logon, `task ls` and the TUI reaching it with nothing started by hand, and an uninstall that leaves nothing installed — rather than pasted output per command.

  *2026-08-12 — first Windows walk (superseded above, kept because the findings hang off it).* Install and uninstall clean from an elevated prompt, and then the TUI kept starting a daemon of its own: the SCM had run the service as **LocalSystem**, whose profile is not the user's, so the daemon served a database in `C:\Windows\System32\config\systemprofile\` that no client would ever probe. Filed and fixed as **T4.19** — the backend became a Scheduled Task running as the invoking user, which also removed the elevation requirement this row was blocked on.

  *2026-08-11 — macOS leg walked by the owner.* Install, reboot-survival and uninstall all clean. The leg found one defect the automated tests could not: an installed service resolved **no** agent CLI, while the same daemon started by hand found claude and codex. Filed and fixed as **T4.15**; the macOS row stays green for the three things this task asserts, and the fix was re-verified against a real launchd by the owner the same day.
- [x] **T4.2 — CLI subcommands.** ✓ 2026-08-11 `project add/ls`, `task add/ls/show/cancel`, `workflow ls/validate` as thin API clients with table/JSON output (§12.1).
  *Done when:* CLI integration tests against a live daemon pass in CI.
  *2026-08-11:* landed per the PR U decisions. Human table by default, `--json` on every subcommand. **No auto-start** — `client()` discovers the daemon and returns exit 2 with a pointer to `vincent daemon start`, and additionally health-probes it, so a stale `daemon.json` from an unclean shutdown gives the same "no daemon" answer instead of a transport error at an arbitrary later point. Exit codes 0/1/2, with 1 meaning the daemon answered and said no (a 409 from the §6 FSM, a missing id) and 2 meaning nothing answered. `workflow validate` parses locally against the **curated** catalogs — no daemon, no probe, no installed CLI — which is what makes it usable from CI and a pre-commit hook. `TestCommandsAgainstLiveDaemon` drives the real binary against a real detached daemon: both output modes, empty results rendering as a header-only table and `[]` rather than `null`, exit 1 on rejection, exit 2 with no daemon, and validate's own 0/1 split.
- [x] **T4.3 — Retention & limits.** ✓ 2026-08-11 Transcript pruning for archived tasks past `transcript_retention_days`; per-run transcript size cap failing the step past the limit; daemon log rotation verified (§17, §18).
  *Done when:* pruning and cap behavior covered by tests with shrunk thresholds.
  *2026-08-11:* landed per the PR V decisions. **Config:** new `ByteSize` type so `transcript_max_bytes: 512MB` reads as a size rather than a magic integer, top-level beside its retention sibling (not under `defaults:`, which is timeouts only), surfaced on `GET /v1/config` and in the daemon view. **Cap:** the transcript latches past the limit, drops later writes, and the executors kill the tree — the agent path through the existing cancel-cause seam (`errTranscriptLimit` → `classifyAgent`), the command path by cancelling its run context from the stream goroutine, since a process that never stops producing is exactly what must not be waited on. The tripping line is written **whole** (a half line would turn a size failure into a parse failure for every later JSONL reader) and `NoteOverLimit` bypasses the cap so the file records *why* it ends. **Pruning:** `TranscriptPruner` runs at start and on a 24 h ticker — the ticker is what makes retention work on a daemon that survives reboots (T4.1) rather than only on the restarts it no longer has. Retention is measured from `archived_at`, `0` disables it, and rows are never deleted.
  *Verified at shrunk thresholds:* prune tests at 7-day retention with tasks aged either side of it, idempotency, disabled-by-zero, and context cancellation; transcript tests for latching, whole-line writes, disabled-by-zero, and the surviving annotation; `TestEngineTranscriptLimitFailsTheStep` drives a new `flood` fakeagent scenario against an 8KB cap end to end and asserts the block reason, the step reason, the annotation and that the file stayed bounded. **The idempotency test earned its keep immediately:** `os.IsNotExist` does not unwrap, `dirSize` wraps with `%w`, so the already-gone branch never matched and every pass re-counted deleted tasks as freshly removed.
  *Log rotation* had shipped since phase 1 and was never asserted; `TestLoggerRotates` drives the real `newLogger` past 10 MB and checks a backup appears and the live file drops back under the cap.
- [x] **T4.4 — Docs & examples.** ✓ 2026-08-12 README (install, quickstart, prominent full-auto risk section §16); workflow authoring guide; 2–3 example workflows shipped in `examples/` (e.g. `feature-pr`, `fix-and-test`).
  *Done when:* a reviewer can follow the quickstart cold; examples validate via `vincent workflow validate`.
  *2026-08-11:* landed per the PR Y decisions. `docs/workflows.md` is the authoring guide — scopes and reload, the three step types, checks as the answer to an agent grading its own homework, the template table, retries, the adapter capability matrix, permission modes, and a pre-commit checklist. `examples/` ships the three the decision named plus `cursor-review` from T5.6: **feature-pr** (agent → check → gate → push), **fix-and-test** (two agent steps, the first with an *inverted* check so a red test is the deliverable — one agent asked to "fix it and add a test" reliably writes a test that passes against its own fix), and **docs-update** (`restricted` mode, and the only workflow with no compiler to hide behind). All four validate through the real CLI in CI.
  *README:* a Quickstart section walking register → workflow → task → approve, with the **full-auto warning as a callout at the point of first run** rather than only in the Security section near the bottom — a reader following Install → Quickstart meets the agent before they would ever reach it. Install landed with T4.5.
  *Walking my own quickstart found two real defects*, which is what the exercise is for: `vincent workflow ls` does not show project-scoped workflows without `--project` (the quickstart said otherwise), and **`vincent task show` printed a blank project** — `project_name` was on the list response only, so every client's `TaskDetail.ProjectName` was silently empty. Moved to the shared `taskResponse`, with a handler test asserting both endpoints agree and a CLI e2e assertion on the rendered row.
  *2026-08-12 — the docs became a tree.* The README's phase-by-phase **Project Status** section is gone: it was the last "implementation in progress" surface in a repo whose v0 scope is complete, and a phase narrative is what `tasks.md` is for. In its place the README carries a one-paragraph statement of what is shipped and a **Documentation** index, and `docs/` grew from two loose files into a navigable set with its own hub at [`docs/README.md`](../README.md): `getting-started/{installation,quickstart,concepts}`, `guides/{workflows,agents,tui,scripting,running-at-login,troubleshooting}`, `platforms/{windows,macos,linux}`, `reference/{cli,configuration,files,workflow-schema,task-lifecycle,api}`, plus `security-model.md` and `faq.md`. **The authoring guide moved** — `docs/workflows.md` is now [`docs/guides/workflows.md`](../guides/workflows.md); earlier notes in this file name the old path because that is where it landed at the time.
  *Written against the source, not from memory:* the config table comes from `internal/config/config.go` and its bootstrap file, the CLI page from the cobra tree, the API page from the route table in `internal/api/server.go`, the key tables from the TUI's one binding registry, and the block reasons from the `Reason*` constants in `internal/taskrun` and `internal/worktree` — so a page that disagrees with the code is a bug in the page, findable by grep.
  *2026-08-12 — done-when met.* **The owner confirms the quickstart reads correctly** against the README's Install → Quickstart path (asked as `OUTSTANDING.md` §4, since retired — this file is the record now). No gaps, backtracks or filler were reported, so there is nothing further to fold in — the sign-off is the finding. The examples half of the done-when has been machine-verified since 2026-08-11: `TestCommandsAgainstLiveDaemon` runs `vincent workflow validate` over every file in `examples/` through the real binary on all three OSes, so a broken example fails the build rather than a reader.
  *What the sign-off does and does not cover.* It is the README path — the same prose the M4 gate was walked against, in its post-revamp form. The longer [`docs/getting-started/quickstart.md`](../getting-started/quickstart.md) is that sequence with the surrounding detail a reader who wants it can follow; it is not separately cold-read, and does not need to be, since the done-when names the quickstart a newcomer meets first.
- [x] **T4.5 — Release packaging.** ✓ 2026-08-11 goreleaser (or equivalent): versioned, signed binaries for all 3 OSes; checksums; install instructions per OS.
  *Done when:* tagged pre-release produces working artifacts installed and smoke-tested on each OS.
  *2026-08-11:* landed per the PR X decisions. `.goreleaser.yaml` builds six archives (linux/darwin/windows × amd64/arm64) from **one ubuntu runner** — the binaries are pure Go with `CGO_ENABLED=0`, so a build matrix would produce identical artifacts three times — plus `checksums.txt`, cosign **keyless** signatures over it, and `actions/attest-build-provenance` over every archive. `cmd/fakeagent` is deliberately excluded: it is test scaffolding and must never ship. The ldflags match the mage Build target's three symbols, so a released binary and a locally built one answer `vincent version` the same way. Distribution is GitHub Releases only — no tap, bucket or winget manifest.
  *`.github/workflows/release.yml`* runs on `v*` tags, with a `workflow_dispatch` dry run that builds a snapshot without publishing or signing. The per-OS `smoke` job is the done-when's other half: it unpacks the **real archive** and runs the **real binary** — `version` must report the tag rather than `dev`, `workflow validate` must accept the shipped example, and `task ls` must exit **2** with no daemon. Unpacking is what catches a broken archive layout, a missing execute bit, or a binary that will not start on the OS it was cross-compiled for.
  *Verified locally on Windows:* `goreleaser check` passes; a full `--snapshot` build produced all six archives; the Windows archive contains exactly `LICENSE`, `README.md`, `examples/cursor-review.yaml` and `vincent.exe`; the produced binary reports its injected version, validates the shipped example, and exits 2 with no daemon — the same three assertions the CI smoke job makes. (`checksums:` was the wrong key, caught by `goreleaser check` rather than at release time.)
  *README* gained the install section this task owes: per-OS unpack instructions, the `cosign verify-blob` incantation, and the Gatekeeper/SmartScreen prompts a user **will** meet, including `xattr -d com.apple.quarantine` — §19's ‡ note descoped OS code signing, so documenting the consequence is part of the deal.
  *2026-08-11 — done-when met.* **`v0.1.0-rc1`** cut by the owner ([run 31484132218](https://github.com/lezli01/vincent/actions/runs/31484132218)), the first execution of `release.yml` and therefore of the signing and attestation steps, which no PR build can reach. Verified independently of the report: the release carries **9 assets** (6 archives, `checksums.txt`, `.sig`, `.pem`), `isPrerelease: true` — so `prerelease: auto` read the `-rc1` suffix correctly — the run reports `release: success` plus **all three smoke jobs green** (ubuntu, windows, macos), and the Windows archive's digest resolves to an `application/vnd.in-toto+json` build attestation. Nothing unexpected reported.
  *This unblocks T4.6:* the artifacts its ten-minute clock starts from now exist and are downloadable.
- [x] **T4.6 — Phase gate (M4 acceptance).** ✓ 2026-08-11 Fresh-machine (VM) timed test per §19 M4 on each OS; results recorded here.
  *Done when:* all three runs under 10 minutes; notes committed. **v1 complete.**
  *2026-08-11 — done-when met.* Walked by the owner on a clean VM per OS with **no Go toolchain**, against the `v0.1.0-rc1` artifacts: download → unpack → `vincent project add` → copy an example workflow → `vincent task add` → task completes. The clock includes Gatekeeper/SmartScreen friction (§19 †) and excludes installing and authenticating the agent CLI.

  | OS | Time | Under 10 min? |
  |---|---|---|
  | Windows 11 | 5:00 | ✓ |
  | macOS | 4:30 | ✓ |
  | Linux | 3:35 | ✓ |

  *Every run under half the budget,* with the slowest OS the one carrying SmartScreen — so the descoped code signing costs roughly the difference against Linux, not the gate. **No breakdown of where the time went and no deviations from the README quickstart were reported**; the walkthrough script is the README's, so "deviating from it is itself a finding" produced none. The reader-facing half of the same prose is T4.4's remaining item, which this gate does not settle — the walker here wrote it.
  *M4's acceptance is met.* Phase 4 is not yet closed, but **nothing in it is waiting on a human any more**: T4.1's per-OS reboot walk and T4.4's quickstart read both closed on 2026-08-12, and what remains (T4.11, T4.17, T4.18, T4.23) was appended after this gate was written and is ordinary work.
**PR T decisions (T4.7; grill session, 2026-08-10):**

- *The endpoint is `POST /v1/resolve`, not a wider workflow DTO.* The two callers ask different questions: the registry listing asks what a step resolves to *as written*, the new-task form asks what it resolves to *under the overrides being typed* — and those overrides have no server-side existence until the task is created. A DTO field answers only the first. One endpoint with an empty override body answers both. **Spec addition** to §13.2.
- *Provenance rides the resolver, not the handler.* `agent.Resolve` becomes a wrapper over a new `ResolveWithSources`, so the engine and the endpoint share one implementation of the precedence *and* one of the attribution. A handler that re-derived "which level won" would be the second §8.6 implementation this task exists to prevent.
- *Level 4 is read from the adapter catalog,* through the same binary-identity cache `GET /v1/agents` uses, with `refresh=false` — resolving must never spawn a probe, because the form resolves on every override change. No adapter reports a `DefaultModel` today, so model and effort legitimately come back empty with source `adapter`; the form renders that as "CLI default" rather than inventing a model name. The seam is wired now so the day an adapter reports one, the form names it with no further change.
- *Non-agent steps keep their index with null fields.* A resolution lines up positionally with the registry's own step list, which is what lets both renderers zip the two together without matching on ids.
- *The TUI drops its registry-scanning `workflowAgents` entirely* rather than keeping it as a fallback. Two code paths producing the agent summary — one resolved, one derived — is exactly the drift this task closes. Before the reply lands the suffix is simply absent: "(workflow default)" alone is incomplete, never wrong.
- *A resolution carries the draft it describes.* `resolveKey` (project, workflow, and the override triple) travels with the request and is compared on arrival: a reply for a draft the user has already moved past is dropped, and a failed one clears rather than leaves stale text naming the wrong model. The workflows view caches per scope+name and drops the cache on registry reload — the file that just changed is precisely the one whose resolution may have moved.
- *The picker preview keeps the registry's wording.* Resolution belongs to the committed workflow; fetching one per highlighted option would be a request per cursor move. `renderWorkflowDetail` uses the resolution only when the name matches the committed draft.
- *Unavailability checking grows to level-4 steps.* With a resolved agent in hand, a step naming no agent can finally be checked against adapter availability — "the default adapter is missing" is a warning that was impossible to give before, and the old comment saying such steps are "never accused" no longer applies.
- *Proof is a live test, not a stub.* `TestWorkflowsViewNamesTheAdapterDefault` drives the real handlers through the real client; a stubbed resolution would only assert the TUI renders what it was handed, which was never in doubt. The new-task live test additionally asserts the form names the resolved agent and spells out the unnamed model.

**Windows testing findings (owner, 2026-08-11).** Five reports from a real run on Windows 11 against the `v0.1.0-rc1` artifact. Three were my own doing, two were genuine defects:

- [x] **T4.8 — Concurrent control requests killed live claude runs.** ✓ 2026-08-11 §7.4 asserted requests were serial "because an agent blocks on its pending request". Real claude disproves it: it batches parallel tool calls, so a restricted run raises several `can_use_tool` requests at once, and the adapter failed the attempt on the second with `input_protocol_error`. **No fixture could have caught this** — `cmd/fakeagent` only ever emits one, which is exactly the assumption under test. The adapter now **queues** concurrent requests and promotes the next when the current one is answered; the engine and clients still see one at a time, because a task has one `awaiting_input` state. Delivery needed a mux goroutine: `readLoop` owns and closes the events channel, and the CLI is blocked on *both* requests so no later stream line would ever carry the second. Spec §7.4 rewritten — the serialization is the adapter's job, not a promise the CLI keeps.
  *Verified:* the old "second request is a violation" test replaced by one asserting the queue, plus a promotion test that fails if the queued request is never surfaced (the shape that would hang a real run).

- [x] **T4.9 — The shipped `docs-update` example set `permission_mode: restricted`.** ✓ 2026-08-11 Two of the five reports were this one example: cursor **refused every step on Windows** (no sandbox, §9.4) and claude **prompted for permission on every non-allowlisted tool** — both correct behavior for restricted, and both read as bugs to someone who never asked for it. A shipped example is copied on day one; demonstrating an edge case that breaks one adapter on one OS is a bad trade. Now full-auto like the others, with the restricted opt-in documented in a comment and in `docs/workflows.md`. The engine's default was never wrong: `resolvePermission` returns `FullAuto` for anything unset.

- [x] **T4.10 — README: archive logo and global workflows.** ✓ 2026-08-11 The README inside a release archive rendered no logo — it referenced `docs/assets/logo.png`, which the archive does not carry — so the image is now an absolute raw URL that works in the repo, in the archive, and anywhere else the file travels. The quickstart also only documented **project-scoped** workflows; it now names the config-dir location per OS, states that project scope shadows global, and shows both.

- [x] **T4.12 — `debug` config knob.** ✓ 2026-08-11 Requested after a run that prompted for permissions gave no way to see it had resolved to `restricted` — diagnosing it meant reading the stored snapshot out of the API by hand. `debug: true` in `config.yaml` writes a `vincent.debug` line into every step's transcript: resolved agent/model/effort, **permission mode**, `on_input`, workdir, timeout, attempt, and the **full argv** of the spawned process. Command steps get the same, including which shell §8.3 resolved. The daemon also logs the transcript path at info, so "where did it write the log" has an answer you can grep for.
  *Design:* the record goes in the transcript rather than the daemon log, so it travels with the run a user pastes into an issue. Off by default because argv carries the rendered prompt. `agent.RunHandle` gained `Argv()` — the argv was unanswerable from outside the adapter, which is exactly the question a misbehaving run raises.
  *Verified:* a real claude run with `debug: true` recorded `permission_mode: full-auto` and the complete argv — the two facts whose absence cost an hour of debugging on 2026-08-11.

- [x] **T4.13 — Board shows the workflow.** ✓ 2026-08-11 The board named the current step but not the workflow it belongs to, and "survey" means nothing without "docs-update". New WORKFLOW column, dropped **after** the step name under width pressure: a workflow alone still says what a task is doing, while a step name alone does not.

- [x] **T4.14 — Richer agent output in the detail view.** ✓ 2026-08-11 The output pane rendered a tool use as its bare name (`▸ Bash`) — a keyword, not an event — while all three adapters had the detail to hand and threw it away. `ToolUse` now carries `Summary` and `CallID`, filled by **one shared extractor** over an ordered preference of *argument names* (`command`, `file_path`, `pattern`, `path`, `url`, `query`, `prompt`, `description`) rather than three tables of tool-name → field: the names converge because the underlying tools do, and a key nobody sends costs nothing — the summary comes back empty and the bare name renders exactly as before. `pattern` deliberately precedes `path` (a search carries both, and "Grep TODO" says more than "Grep internal"); `command` precedes `description` (cursor's shell calls carry both, and the command is the subject while the description is a sentence about it).
  *Two things the grill session pulled forward.* **`CallID`** was not in the original filing: claude batches parallel tool calls (T4.8), so "the result under the call is that call's result" is false in exactly the case that matters, and correlation had to be on the wire before anything could render a result beneath its call. And the summary is **flattened to one line and capped at the adapter** (200 runes) — a heredoc otherwise decides how many rows a record occupies, and a cap in the TUI would leave every future client to reinvent it.
  *Wire (§13.2, v0 change):* `tools: []string` → `tools: [{name, summary, call_id}]`. Nothing durable broke — normalized records are recomputed from the raw file on every read and never stored, live chunks are ephemeral — so the handler, the live publisher and the one in-tree client moved in one commit rather than carrying two shapes. The unrecognized-line half of the original done-when moved to **T4.16**, which is where the flood protection it has to respect lives.
  *Verified:* the extractor table-tested over all three dialects incl. truncation and the rune-safe cut; claude asserted against the **verbatim captured** `tool_use` line from `stream_permission_allow_2.1.226.jsonl`, cursor against both calls in `tools_2026.08.04.jsonl` (the edit's `args.path` one level down, the shell call's `command` beating its `description`), codex against `tooluse.jsonl`; and the round trip through the real handler in `apiclient`'s live test, which is what would catch client and server drifting.

- [x] **T4.16 — The output view shows what the agent is doing.** ✓ 2026-08-11 The pane rendered assistant text and tool names and nothing else: the model's reasoning was captured on disk and shown to nobody, a tool call was never followed by its outcome, unrecognized lines collapsed behind a count with no way to see them, and **long lines were clipped at the pane width** — the viewport never set `SoftWrap`, so a paragraph of assistant text was cut and the remainder unreachable.
  *Done when:* thinking and tool outcomes render, unrecognized lines are reachable, and no line is silently cut.
  *Two new event types, and both were already in streams vincent recorded and discarded.* `EventToolResult` — claude replays outcomes on `user` lines as `tool_result` blocks, codex closes a tool item with `item.completed`, cursor with `tool_call/completed`; all three fell through to `agent.raw`. It carries an **outcome only**, never the tool's output body, which can be hundreds of lines and is already in the transcript. `EventThinking` — claude's reasoning blocks are in this repo's **own captured fixtures** and were being dropped by a parser that switched on `text`/`tool_use` alone.
  *The §9.7 reversal, stated rather than slipped in.* "thinking normalizes to `unknown` — never surfaced live" was a binding Phase 5 decision. It was right about cursor's *shape* and wrong to generalize: claude delivers whole blocks and never had the fragment problem, so the rule suppressed reasoning for an adapter that streams no deltas at all. What it protected survives as a constraint on the **interface** — `EventThinking` is whole blocks only, so cursor's parser accumulates deltas and emits at `completed`. Two costs, both pinned by tests: `Event.Raw` gains a documented exception (a coalesced block's Raw is the line that *closed* it, which keeps transcript offsets correct), and a run killed mid-block loses the buffer. The swallowed deltas still normalize to `unknown` — they are genuinely unmodeled lines, and `v` should show them.
  *Where honesty beat coverage, twice.* Cursor's tool outcome keys on the **presence** of `result.success` rather than on any failure shape, because no failed cursor tool call has ever been captured (the fixture's `completed` payloads are reconstructed — the capture machine had a hook that rejected every call); presence is correct in both directions and degrades to a true statement. And codex reasoning is **not** implemented — filed as T4.17 — because a guessed shape fails *silently*: reasoning never appears, and nothing distinguishes that from a model that did not reason.
  *The clipping was the biggest defect and was found by reading the code, not the report.* The pane never enabled wrapping, so a paragraph was cut at the pane edge. `SoftWrap` would have been one line and was rejected: it folds continuations to column 0, where wrapped reasoning is indistinguishable from assistant prose — destroying the distinction the new gutter exists to make. The pane wraps itself with a hanging indent, wrapping plain text and styling after, so no ANSI-aware wrapping is involved. Words are styled in runs rather than individually: a `Render` per word puts an escape sequence between every pair, so no phrase a human reads ever exists as contiguous text.
  *Rendering:* two-column gutter (blank for prose, `· ` reasoning, `▸ ` tool, `✓ `/`✗ ` outcomes indented under their call), a blank line before an assistant message that follows anything else, no timestamps — nine columns of every line to answer what the timeline already answers per attempt. `agent.result` shows its outcome instead of its text, which every dialect repeats from messages already on screen (cursor's is the whole turn concatenated); the text is kept when nothing else rendered, and always on error.
  *`v` cycles compact → normal → verbose,* session state, and the pane title names any level but the default — `v` is the one key here whose effect can be invisible, on a run with no reasoning and nothing unrecognized.
  *Verified — automated:* claude's thinking and tool_result asserted against the **captured** `stream_permission_allow_2.1.226.jsonl` (incl. that the `signature` attestation blob never reaches a pane), cursor's coalescing against `success_2026.08.04.jsonl` with parser-isolation and Raw-exception tests, codex's `exit 0` against `tooluse.jsonl`; wrapping tested for hanging indent, hard-split long words, multi-byte rune boundaries and trailing whitespace; the level matrix, turn separation and result de-duplication as unit tests; chunk-vs-record shape parity in `internal/taskrun` and the round trip through the real handlers in `apiclient`. Lint clean on all three GOOS.
  *Verified — walkthrough (owner, 2026-08-11).* Every test above proves a **parser**; none of them proves a **pane**, and "is this readable" is a judgement — the same reason `scripts/m3-gate.sh` seeds a walkthrough instead of asserting, and the same shape as T4.15, where no rendering test could prove what PATH launchd hands a process. The owner walked the TUI against a real claude run and judged the pane good; that judgement is the acceptance and it is recorded as the owner's, not reconstructed from output the executing agent saw. The leg also covers the one path no fixture reaches: live SSE chunks come from `taskrun.publishAgentEvent`, a **separate** mapping from `api.normalizeLine`, so a running step and the same attempt re-opened render through different code. The shape test pins those two against each other, which only proves they agree with what was written — watching a real run is what proves they agree in fact.

- [x] **T4.18 — `[`/`]` are in the binding registry and handled nowhere.** ✓ 2026-08-12 Found while adding `v` (T4.16). The registry — the single source `?`, the palette and the footer render from — promises `[`/`]` switch the output pane's tabs, per the §15 keys decision; `detail.updateKey` implements only the `d` alias, so both keys fall through to the viewport and do nothing. A key the help promises and the app ignores is the exact failure the registry was built to make impossible, which makes this a gap in the registry's *test*, not just in the handler.
  *Done when:* `[`/`]` switch tabs, and a test walks the registry asserting every panel-scoped key is handled by its view.
  *2026-08-12 — both halves met.* **The handler** is one case: `]`, `[` and `d` all reach `toggleTab`, in the switch that runs before focus is consulted, so they work from either pane the way `v` and `f` already do. They **toggle** rather than stepping a direction — with two tabs, "next", "previous" and "the other one" are the same move — and the comment says where that has to split if a third tab ever lands. Choosing prev/next now would have been machinery for a set of two.
  *The test is the half that matters,* because the handler was never the hard part: a registry that is only *rendered* proves a row exists, not that anything answers it. `TestEveryPanelKeyIsHandled` walks `bindings`, and every `scopePanel` row must have a probe that drives the **real view** with the key and asserts the effect its label promises — 28 rows across all eight contexts. A row with no probe fails, and a probe whose row is gone fails too, so coverage cannot rot in either direction.
  *Two design points in the probe harness.* `registryKey` turns a registry string back into a press and **asserts the round trip** (`msg.String() == key`): a probe feeding a key the registry does not publish would prove nothing, and a registry key no press can produce is its own defect. And the probes assert *effects*, not consumption — `[`/`]` were being consumed the whole time, by the viewport, which is precisely why "the key was swallowed" is not evidence.
  *The two scroll rows are the one honest exception:* an unrendered viewport cannot scroll, so they assert the press reached the pane's scroll path (which re-syncs follow) and leave movement to the existing follow and mouse-wheel tests.
  *Verified by reverting:* with the handler put back to `case "d"`, `output/]` fails along with both alias tests, and `d` keeps passing — the shape of the original bug exactly. Full suite green; lint clean on all three GOOS.

- [x] **T4.17 — Codex reasoning items are not normalized.** ✓ 2026-08-12 T4.16 surfaces thinking for claude and cursor. Codex emits reasoning too, but **no capture in this repo contains one**, and the convention is table-driven tests against captured real-CLI output. Implementing against a documented-but-unobserved shape fails silently if it is wrong — reasoning simply never appears, and nothing distinguishes that from a model that did not reason.
  *Blocked on:* a real `codex exec --json` capture containing a reasoning item.
  *Done when:* codex reasoning normalizes to `agent.thinking` like the others, asserted against that capture.
  *2026-08-12 — unblocked by the owner and closed the same day.* `testdata/reasoning_0.147.0.jsonl` is a real `codex exec --json` run at `-c model_reasoning_effort=high`, captured by the owner. **The wait paid for itself in one line:** reasoning arrives as `item.completed` with item type `reasoning`, carrying whole `text`, **four times in one turn** — no `item.started` to correlate against and no deltas to accumulate. That is claude's shape, not cursor's, so it needs none of the cursor parser's delta buffering and satisfies `EventThinking`'s whole-blocks-only contract directly. A blind implementation had a real chance of guessing cursor's shape and failing exactly the way this task predicted: silently.
  *Emission is effort-dependent, which the old fixture already hinted at and nobody read.* `success.jsonl` ends with `reasoning_output_tokens: 25` and contains no reasoning item at all. Tokens spent is not evidence of an item to parse — worth knowing before anyone reports reasoning as missing.
  *The change is nine lines* in `stream.go`'s `item.completed` switch; every consumer already existed. `EventThinking` has been carried by `taskrun.publishAgentEvent` and `api.normalizeLine` since T4.16, so codex reasoning reaches the live tail, the transcript and the `v` levels with no further wiring — and because §13.2 normalizes on **read**, the reasoning in codex runs recorded before today renders now.
  *Verified by removing the case:* the fixture test drops to `thinking events = 0, want 4`. It also pins the two things a wrong implementation would get away with — reasoning must not become `ResultText`, and must not be normalized as assistant output — plus an empty-text item staying `EventUnknown`, since a blank thinking block would put an empty gutter line in the pane for nothing. Spec §9.1's "filed as T4.17, blocked on a capture" paragraph is rewritten to record what the capture said.

- [x] **T4.11 — The TUI cannot show a full transcript.** ✓ 2026-08-12 The detail view fetches a bounded *tail* (`DefaultTailBytes`, capped at `maxRecords`) with no way to page back, so a failed step cannot be diagnosed from the TUI — which is where a user is when it fails. The transcript on disk is the complete record and the ranged endpoint already serves it; only the UI is missing. Partial mitigation shipped: `vincent task show` now prints each attempt's transcript path.
  *Done when:* a step's whole transcript is reachable from the detail view — scrollback, or `$EDITOR`/pager the way `e` already opens a workflow file.
  *2026-08-12 — the done-when's second option, deliberately.* `e` on the detail view hands the displayed attempt's transcript to `$EDITOR` through the existing `openEditorPath`, which is the same helper and the same meaning `e` already carries in the workflows view: *open the thing you are looking at in your editor*. The key is handled beside `]` and `v`, before focus is consulted, so it works from either pane.
  **Scrollback was the other option and is rejected on a primitive that does not exist.** The §13.2 range reads *forwards* from a byte position — `tail=` snaps to a record boundary, `offset=` is taken as given — so "the window before this one" cannot be asked for. Paging back would mean either growing the pane without bound (against `maxRecords`, which exists because §18 allows gigabytes) or refetching from zero on every page. Handing the file to a program built for whole files costs nothing and inherits its scrolling, search and memory behaviour.
  *What opens is the raw JSONL, not the pane's rendering* — the lossless record, including lines no parser recognizes, and the same file `vincent task show` prints the path of. The two ways in therefore name one artifact.
  *Two nothings are told apart.* `openEditorPath` must not create the file it is given, so a **pruned** transcript would otherwise open as an empty buffer — indistinguishable from a step that produced nothing. It is stat-ed first and reported as pruned (§12.3 retention), while a **gate**, which never opened a transcript at all, says that instead. A failed viewer is reported too: it took the terminal over, so the action bar is the only place its failure can surface.
  *Discoverability is half the fix.* The pane's existing truncation line now reads `… earlier output truncated — press e for the whole transcript`, which puts the way out at the one moment a reader is looking straight at the output they cannot see. A key nobody finds would leave the task implemented and not fixed.
  *Verified:* the whole path against a real on-disk transcript longer than the pane's window (the editor receives that exact path, once, from either focus), the pruned and no-transcript branches, a failing viewer, and the truncation line naming the key. The **T4.18 registry walk caught the new binding immediately** — adding the row failed the build until a probe proved the view answers it, which is the first time that guard has paid for itself.

- [x] **T4.23 — The environment an agent runs in is ambient.** ✓ 2026-08-12 Nothing in the chain from CLI to agent process decides it: the detached spawn inherits the launching shell's (`internal/daemon/spawn.go`), `RunSpec.Env` is never populated for agent steps (`internal/taskrun/steps.go`), and each adapter overrides only a non-nil one (`internal/agent/cursor/cursor.go`, `internal/agent/codex/codex.go`). The same task, workflow and agent therefore run differently depending on the environment of whatever started the daemon, and nothing records which one that was.
  *Filed on a wrong diagnosis, twice, and kept on an observation that outlived both.* It was written believing an inherited `SHELL` explained T5.7's blocked Windows leg; it was then re-written believing the environment was irrelevant and hooks were the whole story. Both were one layer short. The trigger is `MSYSTEM` — so the environment *is* load-bearing, and the daemon does inherit it by accident, which is this task's actual claim (see T5.7, 2026-08-12).
  *What it is **not** is a shipping defect, and that prediction is retracted.* The installed logon-started daemon carries no `MSYSTEM` and runs cursor workflows normally — 28 edits, zero blocks, a real commit. Nothing is currently biting; this stays a latent design gap, priced accordingly. The live example it now rests on is narrow but real: a daemon parented by Git Bash cannot run a cursor step that edits, and no vincent config can express the difference.
  *Scrubbing variables is the obvious fix and is rejected.* An inherited `SHELL` or `USERPROFILE` is frequently the *right* value — the second one is where an agent CLI's own credentials live. Deleting either would trade a rare, loud failure for quiet breakage across every adapter, on a path with no coverage. The defect is that the value is **accidental and unrecorded**, not that it is set.
  *Shape:* a §12.3 key pinning what agent steps inherit, defaulting to today's inherit-everything so nothing changes for anyone who does not ask, with the daemon logging the resolved set once at startup. Command steps already carry their own `env:` (`internal/workflow/workflow.go`), and `commandEnv` (`internal/taskrun/steps.go`) is where the two would have to meet.
  *Done when:* a task's agent environment is reproducible from config alone, and the daemon log says what it was.
  *2026-08-12 — done, per a grill session that changed the shape in two places.* **§12.3 gains `environment:`** — `inherit` (`all` | `none` | a list) → `unset` → `set`, resolved in that order, defaulting to `{inherit: all}`, which is byte-for-byte what the daemon did implicitly before. `config.Environment.Resolve` is a pure function of a base environment, so it is tested against the cases that matter rather than against whatever the test machine happens to export.
  **Scope widened from the recorded Shape, deliberately and not silently.** The note said "a §12.3 key pinning what *agent steps* inherit". It governs **every child**: agent steps through `RunSpec.Env`, command and check steps through `commandEnv`. A command step running `go test` is as exposed to an ambient variable as an agent is, and two environment stories for two step types would be a second seam to keep true.
  *`RunSpec.Env` needed no new plumbing* — it was defined, documented `nil = inherit`, and honored by all three adapters from the start. Nothing ever set it, so every adapter's `if spec.Env != nil` branch was dead code. The engine now **always** populates it, even for the inherit-everything default: passing nil would hand the adapter the ambient environment again, and *decided* is the point.
  *The environment is resolved per spawn, not cached at startup,* because config hot-reloads — a step starting after a reload must run under the policy in force when it started.
  **Three findings the tests produced, all kept:**
  1. *An empty `inherit` list meant "everything".* Inferring the mode from `len(Names)` read the narrowest request expressible as the widest. `Inherit` now carries an explicit `Mode`, which also deletes the both-none-and-a-list state rather than validating against it.
  2. *Windows entries like `=C:=C:\work` were being dropped.* Their name legitimately begins with `=`; cutting on the first byte named them `""`. Go's own exec special-cases them, so the inherit-everything default was quietly **not** everything on Windows. `splitEnv` searches from index 1.
  3. *The MSYSTEM fix works precisely because vincent sets the block directly.* T5.7 found `env -u` and `unset` could not clear it — the MSYS runtime re-injects it into every child spawned **by bash**. A native Go parent writing `cmd.Env` is not subject to that, so `unset: [MSYSTEM]` genuinely removes it. That is the one live case this task rests on, and it is now expressible.
  *Logging is the done-when's other half:* the resolved variable **names**, sorted, at info, at startup and again on any reload that moves the set — a line that goes stale is worse than one that never spoke. **Never the values, at any level**, and deliberately **not** in step transcripts even under `debug: true`: an environment block is where every agent CLI's credentials live and a transcript is what people paste. It took two wrong diagnoses to find MSYSTEM; one line naming it would have ended that in a look.
  *A missing `PATH` (or `SystemRoot` on Windows) warns and runs.* §9.5 resolves adapters with `exec.LookPath` in the daemon and starts them by absolute path, so an agent with no `PATH` starts perfectly and fails several steps later when the CLI shells out — silent and late. A hermetic environment with an absolute-path toolchain is a legitimate request, so vincent announces the hazard rather than correcting it, the same temperament as an adapter that fails rather than quietly downgrading `restricted`.
  *Daemon-global only.* A workflow cannot pin its own: that needs a §8.2 addition and a second precedence chain beside §8.6's, and command steps already carry `env:` for additions. Additive later.
  *Verified by reverting both wirings:* the command path and the agent path each fail with the ambient variable visibly leaking through (`ambient=[inherited-by-accident]`), which is the defect itself reproduced on demand. Plus the resolver's cases, an `inherit: []` regression test, the odd-Windows-entry case, `Names` dropping values, the load-bearing check, YAML round-trips, and a `commandEnv` layering test pinning that a step's own `env:` still outranks a daemon-wide `set`. A new `report-env` fakeagent scenario carries the agent-side assertion, per the convention that scenarios live there rather than as adapter special-cases.
  *Not blocking M5* — T5.7's gate records the environment it ran under instead, which is the cheap half and makes the next environmental failure diagnosable in one look rather than in two days.

**Windows testing findings (owner, 2026-08-12).** Four reports from the T4.1 Windows service leg, each against the previous one's fix: T4.19 against the service, T4.20 against T4.19's task, and T4.21/T4.22 against T4.20's hidden window — one report, two independent defects, because the window the owner saw was the daemon's console *retitled by a failing agent probe*.

- [x] **T4.21 — Hiding the console window lost a race it cannot win; the daemon now releases it.** ✓ 2026-08-12 After a reboot the window T4.20 was written to remove was on the desktop again, titled `C:\windows\system32\cmd.exe` — which is what made it read as a stray `cmd` window and is the thread that unravels the whole report. It is the *daemon's own* console: npm's `codex.cmd` shim ends with `title %COMSPEC% & node …`, the agent probes inherit the daemon's console, and so a probe renamed the daemon's window after itself.
  *Why hiding failed where T4.20's A/B said it worked.* On Windows 11 the default terminal application is Windows Terminal, so the console conhost creates is **handed off** to it: the handoff replaces the console window, and the surviving window is an ordinary Windows Terminal tab in another process. `ShowWindow(SW_HIDE)` on the `GetConsoleWindow` handle is therefore a one-shot action against a window the terminal host is free to supersede — and Windows Terminal's cold start at logon is far slower than the daemon's first few milliseconds, which is exactly the ordering T4.20's warm A/B run did not have. Measured on the reported machine: `pid=16580 class=PseudoConsoleWindow visible=True` with a `conhost.exe 0x4` child, one Windows Terminal tab per pseudo-console, and hiding that same HWND by hand from outside the process worked — proving the mechanism was sound and the *moment* was wrong.
  *`FreeConsole` is not a race, because it is not a window property.* The daemon stops being a console client; the last client leaving ends the console session, so the host exits and takes the window with it — including a handoff still in flight. The T4.20 objection to it is answered rather than overridden: the standard handles are pointed at `NUL` **first**, so the `io.MultiWriter` that would stop at the first stderr error keeps writing the log file, and child processes inherit a null device instead of a dead console. If the null device cannot be opened the console is kept, because a live window beats a silent log.
  *The second half is that the probes had to stop spawning bare `exec.Cmd`s.* With no console of its own, a console-subsystem child is *given* a console — a window — unless its creator passes `CREATE_NO_WINDOW`. T3.8 established this for step processes and for git; the six `--version`/`--help`/`status`/`models` probes were missed, so releasing the console without fixing them would have traded one window at logon for three. `procx.NoWindow` is now exported for probes that want the flag without a Job object, and `agent.Probe` is the one runner all of them go through. This also fixes a latent case that predates today: a daemon started detached (`daemon start`, the TUI's auto-start) already had no console, so its probes were already entitled to windows of their own.
  *What is deliberately not fixed:* the flash between the scheduler creating the process and the daemon reaching that call. Only the creator can pass `CREATE_NO_WINDOW`, and the creator is the scheduler; T4.20 already recorded that a non-interactive (S4U) principal would cost the user's DPAPI-protected agent-CLI credentials. The flag keeps the name `--hide-console` although it no longer hides: every task registered by T4.20 carries that spelling, and renaming it would mean a daemon that fails to start at logon in exchange for a more accurate word.
  *`console=` on the startup log record.* `DetachConsole` returns what it did — `detached`, `shared`, `none`, `kept`, `attached` — and it is logged. T4.20's defect was invisible in the log and needed a screenshot plus a window enumeration to find; the next one is a `grep`.
  *Verified — automated:* `TestNoWindowIsSetForProbes` pins the exported flag; full suite (200 tests in the touched packages, whole suite green) and `golangci-lint` for windows/darwin/linux clean.
  *Verified — through a real Task Scheduler (2026-08-12), an isolated task against scratch dirs so the machine's own registration was untouched.* Task `Running`, `console=detached` in the log, **zero** top-level windows owned by the daemon and **no** conhost child, all three agents detected, daemon healthy on its port, owner `lszabo` rather than SYSTEM. Console-class windows on the desktop were counted continuously through a full `?refresh=true` probe sweep: 10 before, peak 10 — the probes put nothing on screen. The guard was walked separately by starting the daemon inside a real `cmd.exe`: `console=shared`, the console kept, the terminal alive throughout and closed normally on stop.
  *Verified — at a real logon (owner, 2026-08-12), which is the leg T4.20 marked done without.* The machine's own registration, a cold terminal host, and the ordering that defeated hiding: reported clean, with nothing left on the desktop. Recorded on **T4.1**'s Windows row, which closes on the same walk — the two were always the same observation asked for by two tasks.

- [x] **T4.22 — A probe that timed out was cached as a permanent verdict, and reported as an exit status.** ✓ 2026-08-12 Found in the same log as T4.21, and a separate defect with a separate cause. The logon daemon recorded `agent not available agent=codex error="codex --version failed: exit status 1"` for a CLI that was installed and healthy, and went on serving that answer for its whole lifetime: `GET /v1/agents` still said `available=false` twenty minutes later, while running the same probe by hand answered `codex-cli 0.147.0` in under a second.
  *The message was a lie the platform tells.* On Windows a `context` deadline is enforced by `TerminateProcess(pid, 1)`, so a probe killed by its own bound reports exactly what a genuine failure reports — `exit status 1` — and `.Output()` keeps the child's stderr inside an `*exec.ExitError` nobody unwraps. The timing in the log is what gives it away: `probed_at` to the WARN is **14.5 s** against a 10 s bound, and an immediate failure cannot take fourteen seconds. `agent.Probe` now wraps `context.DeadlineExceeded` on a timeout and carries the first meaningful stderr line otherwise.
  *The cache turned a moment into a property.* §9.6 keys entries by binary identity because help output is a pure function of the binary — true of an answer, false of a failure: nothing about `codex.cmd` changed when it was too slow, so nothing invalidated the entry. Failed entries now expire after a minute (`failureTTL`), successful ones still never do. Re-probing an absent CLI costs no subprocess at all, an unresolved path failing before anything is spawned, which is why the TTL applies to every adapter rather than to a special case.
  *Why the probes were that slow at all, and the bound that follows.* The first minute after logon is when every agent CLI is paged in from a cold disk and scanned, and it is also exactly when the daemon primes its catalog: in the reported log `claude` took 8 s and `cursor` 10 s, both nearly at their own limits, and codex — a Node CLI behind an npm `.cmd` shim, i.e. `cmd.exe` plus `node` — went past. The bounds are 20 s/25 s rather than 10 s/15 s. Nothing waits on the prime (it is async), and a request-time probe only re-runs when the binary changed, the last one failed, or `?refresh=true` asked.
  *One adapter bug fell out of the same finding.* `cursor`'s `logged_in` treated any `*exec.ExitError` as "ran and refused", so a timed-out `status` probe on Windows — exit status 1 — reported a definite "not authenticated" for an account that was logged in. §9.5 has a value for "could not tell" and that is now what a timeout returns.
  *Verified:* `TestCatalogCacheFailureExpires` (failure cached within the TTL, re-probed past it, heals, and a clean probe still never expires), `TestProbeTimeoutIsNotAnExitStatus`, `TestProbeFailureCarriesStderr` (both driving a real subprocess), `TestFirstLine`; whole suite and three-OS lint green. On the live daemon that produced the report, `?refresh=true` returned `codex available=True version=0.147.0` — the failure was transient, as the diagnosis said.


- [x] **T4.20 — The installed Windows task ran in a console window that stopped the daemon when closed.** ✓ 2026-08-12 T4.19's Scheduled Task worked and was still a bad install: nothing appeared in `services.msc`, so the install read as failed, and at logon a terminal opened on the desktop with the daemon running in it. Closing that window stopped the daemon, because closing a console sends `CTRL_CLOSE_EVENT` to every process attached to it. The window was not clutter, it was an unlabelled kill switch.
  *Nothing in a task definition suppresses it.* `<Hidden>` — already `false` in the template — governs whether the *task* is listed in Task Scheduler, not whether its process draws anything, and an `InteractiveToken` principal by definition runs on the user's desktop. Only the creator of a process can pass `CREATE_NO_WINDOW`, which is exactly what `internal/procx` does for every agent and command step (T3.8), and here the creator is the scheduler. So the fix has to live in the daemon: the action is now `vincent daemon --hide-console`, and `daemon.HideConsole` hides the window it was handed.
  *Two details in that are load-bearing.* It hides **only when this process is the console's sole owner** (`GetConsoleProcessList` returns 1), so the flag typed by hand in a shell does nothing instead of taking the user's terminal down with it — verified by running it in a real `cmd.exe`, which stayed open. And it **hides** rather than `FreeConsole`s: foreground logging writes stderr and the log file through one `io.MultiWriter`, which stops at the first write error, so invalidating stderr's handle would silently take the *file* half of every log record with it.
  *Letting the action run `daemon start` was the alternative and is rejected.* The existing detached spawn already gives the daemon no console at all, but the task's process would then be a launcher that exits immediately: the registration would sit at `Ready` while the daemon ran, `service status` would report the wrong thing, and `RestartOnFailure` — the analog of `Restart=on-failure` this platform's §12.1 parity rests on — would only ever see the launcher's exit code. The daemon stays the task's own process.
  *The install was also unrepeatable, which is a separate defect found on the way.* This machine's task was registered from an **elevated** prompt (the era when removing the LocalSystem service required one), so its definition is owned by `BUILTIN\Administrators` with the account itself left `Read, Synchronize` — and both `schtasks /Create /F` and `/Delete` answer `ERROR: Access is denied`, naming neither the owner nor the remedy. The owner could neither reinstall nor uninstall without elevation, i.e. could not apply this fix. Registered from an ordinary prompt, `CREATOR OWNER` grants the account full control and every later install and uninstall is unelevated — confirmed by creating one each way and reading both ACLs. `install` and `uninstall` now diagnose the denied case and print the one elevated `uninstall` that clears it, keyed on the **definition's ACL** rather than on schtasks' message, which is written in the machine's UI language.
  *And the discoverability half of the report is answered where it was asked.* `status` reports `Task Scheduler Library\vincent` and "Scheduled Task, at logon — in Task Scheduler, not services.msc"; `service --help` and the README say the same. The first thing a user told "install succeeded" did was open `services.msc` and conclude it had not.
  *One hazard fixed in the tests.* `internal/cli`'s e2e leg asserted that no service is installed and then ran `service uninstall` expecting success — on a developer's own machine, where a per-user unelevated registration legitimately exists, that leg **removes it**, and on Windows nothing would have stopped it. It now skips when the host has a real registration; the not-installed surface is still asserted on a clean runner.
  *Verified — automated:* the rendered arguments carry `--hide-console` and still name the foreground `daemon` subcommand; full suite and `golangci-lint` for windows/darwin/linux green.
  *Verified — through a real Task Scheduler (owner, 2026-08-12), A/B on an isolated task against scratch dirs so the machine's own registration was never touched.* With the flag: the daemon's one top-level window is `class=PseudoConsoleWindow visible=False`, the task state is `Running`, `daemon status` reports it healthy on its port, and the process owner is the user. Same definition with the flag removed: `visible=True` — the window the report was about. The guard was walked separately in a real terminal, which survived.
  *Not covered here:* the console window is created by the scheduler before the daemon runs, so it flashes once at logon before being hidden. Suppressing it entirely needs a non-interactive principal (S4U), which would cost the user's DPAPI-protected agent-CLI credentials — the thing T4.19 changed platforms to keep.

- [x] **T4.19 — The installed Windows service was a second, invisible daemon.** ✓ 2026-08-12 `service install`/`uninstall` worked from an elevated prompt, and the TUI went on starting a daemon of its own anyway. Two daemons, two databases, two ports, no overlap. The SCM defaults an empty `ServiceStartName` to **LocalSystem** (confirmed on the machine: System event 7045, `Service Account: LocalSystem`), and `daemon.Run` resolves its directories from the process environment at startup — so the service's `LOCALAPPDATA` was the SYSTEM profile and it wrote `vincent.db`, `token` and `daemon.json` under `C:\Windows\System32\config\systemprofile\`, where `connect.go`'s probe never looks. Nothing was broken in the TUI: it looked in the user's data dir, found nothing, and did what §12.1 tells it to.
  *The comment claimed the fix that was missing.* `service_windows.go` said the dirs were "pinned through the environment captured at install time (`Options.resolve`)" — `o.Dirs` was resolved and then never passed to `CreateService`, which got the bare `"daemon"` argument. The plist and the unit do bake them in; Windows was written as if it did.
  *Pinning the dirs was rejected as the fix, and this is the substance of the task.* It would have made the TUI connect — to a daemon running as SYSTEM. §16's agents are full-auto by design *because* they hold exactly the invoking user's privileges; as SYSTEM they would hold more, while holding none of that user's agent-CLI credentials, `.gitconfig` or `PATH`. The symptom would have gone away and left a privilege escalation and a pile of failing logins behind it.
  *So the Windows backend is now a Scheduled Task triggered at logon (§12.1 rewritten, PR W's "Windows Service" reversed explicitly).* `InteractiveToken` principal, the account's **SID** rather than `DOMAIN\name` — this machine is Entra-joined and reports `S-1-12-1-…`, which Task Scheduler resolved to `OPSWAT\lszabo` on its own. It needs **no elevation** for install, uninstall or status, which makes Windows the third platform where §12.1's "per-user, because the OS user is the trust boundary" is actually true rather than asserted.
  *Four scheduler defaults each stop a long-running daemon and are overridden:* `ExecutionTimeLimit` `PT0S` (the default `P3D` kills a task after three days), both battery settings `false` (a laptop's daemon would refuse to start unplugged and be stopped when the charger came out), `StopOnIdleEnd` `false` (stops the task when the user comes back). `RestartOnFailure` is the analog of `Restart=on-failure`, and correct for the same reason: a nonzero exit is a failure, a daemon that exited 0 was asked to stop.
  *Two mechanics that are not incidental.* A task's `Exec` action has **no environment**, so the dirs travel as new `vincent daemon --config-dir/--data-dir` flags, which publish the very variables the plist and unit set — one resolution point, not a second precedence rule (§12.2). And the definition is written **UTF-16LE with a BOM**, because `schtasks /Create /XML` rejects non-ASCII UTF-8 with a message naming neither the encoding nor the value.
  *`PATH` is deliberately **not** captured here, reversing T4.15's exclusion rather than extending it.* A task in the user's logon session already has the user's own `PATH`, `%APPDATA%\npm` included — the thing T4.15 was about. Capturing it would swap a live correct value for a stale one. (The old reason was the opposite: a LocalSystem service inherited the *machine* environment, which has no per-user npm prefix at all.)
  *Migration is handled, not documented away.* A pre-existing LocalSystem service is detected with a query-rights SCM handle (no elevation to ask), **refused** by `install` with the command that fixes it, removed by `uninstall`, and named by `status` — it is machine-wide, so that one removal is the last Windows operation that needs an Administrator prompt. `internal/daemon`'s `svc.IsWindowsService()` branch stays: a task's parent is the scheduler's `svchost`, not `services.exe`, so it correctly reports false, and it is still what would make a hand-rolled `sc.exe create` survive the SCM's 30-second handshake.
  *Uninstall stays graceful.* The SCM's Stop control used to give the daemon §12.4's shutdown; `schtasks /End` is a hard terminate, which would kill running agents mid-step on the very platform this fixes. `uninstall` asks the daemon to stop over its own API first, best effort, and only then ends and deletes the task.
  *Verified — automated:* the definition decoded back from XML (not substring-matched, since every value is escaped on the way in) asserting the principal, the logon trigger's scope, all four defaults and the restart policy; `quoteArg` against the `CommandLineToArgvW` rules a data dir ending in a separator would otherwise break; the UTF-16LE encoding. Full suite and `golangci-lint` for windows/darwin/linux green.
  *Verified — through a real Task Scheduler (owner, 2026-08-12), which is the only way any of this is observable.* Installed **unelevated**; `Get-CimInstance Win32_Process` reports the daemon's owner as `lszabo`, not SYSTEM, with `daemon --config-dir "…\Roaming\vincent" --data-dir "…\Local\vincent"` on its command line; `vincent daemon status` — the same `ReadRuntimeInfo` + `CheckHealth` pair `connect.go` probes with — found it at pid 44964, so there is one daemon and the client sees it; `service uninstall` removed the task and left **no** stale `daemon.json`, which is the marker of a graceful shutdown.
  *Not covered here:* logon-survival across a reboot. That is T4.1's Windows row — and its criterion changes from "survives reboot" to "starts at the next logon", the same promise a LaunchAgent and a non-lingering systemd user unit make. *(Walked at a real logon and reported clean on 2026-08-12; T4.1 closed there.)*

**macOS testing finding (owner, 2026-08-11).** One report from the T4.1 macOS service leg.

- [x] **T4.15 — An installed service found no agent CLI.** ✓ 2026-08-11 With the daemon running as the installed launchd agent, the TUI reported **every** adapter missing; the same daemon started by hand in a terminal found claude and codex. §9.5 resolves an adapter with `exec.LookPath`, and a LaunchAgent runs with launchd's own `PATH` — `/usr/bin:/bin:/usr/sbin:/sbin` — which contains no Homebrew, no npm prefix, no nvm shim dir and no `~/.local/bin`, i.e. none of the four places an agent CLI actually installs to. Nothing was broken in the daemon; it simply could not see anything.
  *The fix is the one this package already made for the dirs.* `Options.resolve` captures `os.Getenv("PATH")` at install time and the backends bake it in: `EnvironmentVariables/PATH` in the plist, `Environment="PATH=…"` in the unit. The shell running `service install` has, by construction, the PATH that works. **This was a latent Linux bug too** — a systemd user manager's default PATH is barely wider — found only because macOS was walked first.
  *Escaping came with it, and is not incidental:* a PATH entry containing `&` or `<` makes launchd reject the whole plist with a message naming neither, and `%` starts a **specifier** anywhere in a systemd unit, so `/opt/50%n` would expand rather than resolve. PATH is additionally quoted in the unit because an unquoted `Environment=` splits on whitespace and would silently truncate at the first space.
  *Two deliberate limits, both documented in §12.1, the README and `service --help`.* The captured PATH goes **stale** — a CLI installed somewhere new needs a re-install to be seen, the same recapture contract the dirs have. And **Windows is excluded**: the SCM has no per-service environment, so its service inherits the machine environment, which does not carry a per-user npm prefix. The §12.3 `agents.<name>.path` knob is absolute, never consults PATH, and remains the standing answer on all three.
  *Verified:* plist and unit rendering assert the PATH is present, quoted and escaped, incl. the plist re-parsed as XML with an `&` in it; `resolve` asserts the capture. **And through a real launchd** — the owner re-installed the service from the fixed binary the same day and the TUI listed the agent CLIs, which is the only way this defect is observable: no rendering test can prove what PATH launchd hands a process.

- [x] **T4.7 — Report the §8.6 model and effort resolution.** ✓ 2026-08-10 T3.8 finding (2026-08-10), narrowed: the **agent** side landed with the walkthrough fixes — the registry already reports each step's agent, so the new-task form names it. Model and effort have no such field on the wire, so "(workflow default)" is all the form can honestly say about them, and the workflow-step list still cannot name which adapter "adapter default" resolves to. Both need new read-only API surface, which relitigates the PR L decision that kept resolution server-side — do that explicitly, not by having the TUI re-implement §8.6.
  *Done when:* the model and effort rows name what an unset override resolves to, and a step with no agent names the adapter that will run it.
  *2026-08-10:* landed per the PR T decisions above. `POST /v1/resolve` serves the per-step triple with its §8.6 source; `agent.ResolveWithSources` is the single implementation both the engine and the endpoint use. Verified: 12 handler tests (precedence, agent-scoped reset, level-4 naming, scope shadowing, null non-agent steps, error envelope), the resolver table extended to pin every field's source, TUI unit tests for the stale-reply and failed-reply guards, and two live tests against the real handlers.

---

## Phase 5 — Cursor adapter (M5, post-v1)

Milestone acceptance (§19 M5): a workflow whose steps name `agent: cursor` runs
unattended to completion against the real `cursor-agent`, on each OS.

**Phase 5 decisions (grill session, 2026-08-11) — all pinned against
`cursor-agent 2026.08.04-aaa8809`, probed live on Windows 11:**

- *Post-v1, not folded into Phase 4.* §2's v1 goal says "two agent adapters" and §20 filed more adapters as out-of-v1; both are recorded decisions, so cursor is promoted explicitly (§20 carries a dated strike-through) rather than quietly. M4's charter is polish, and T4.6's ten-minute fresh-machine clock deliberately excludes agent-CLI onboarding — folding a third CLI into that phase would inflate the prerequisite list or corrupt what the gate measures. **M5 was built on a branch in parallel with M4** (it collides only on `config.go`, `daemon.go`, README). **Revised the same day at the owner's direction:** the adapter work merged into `master` *ahead* of M4 rather than waiting for it, so v1's tree carries three adapters; §2's goal is annotated rather than restated, and §19's ‡ footnote records the reversal. The milestone sequencing survives the change — T5.6's remaining legs and T5.7's gate still follow M4 — and T4.6's clock is untouched, since an uninstalled `cursor-agent` is just an unavailable adapter (§9.5).
- *`--model` is always passed, and the saved-model side effect is accepted — confirmed by the owner 2026-08-12 after the alternative was put to them.* Cursor **persists** `--model` to `~/.cursor/cli-config.json`, so every step vincent runs resets the model the user chose in their own interactive `cursor-agent` sessions. The alternative — pass `--model` only when §8.6 resolves one — protects that default but makes a task recorded as "adapter default" possibly a different model from the one that ran. A truthful record outranks an annoyance in a neighbouring tool, and the side effect is documented in the README rather than hidden. The reasoning also lives at the point of the behaviour in `internal/agent/cursor/cursor.go`, where someone deleting the line will meet it.
- *The spec section is §9.7, appended after §9.6, not inserted at §9.4.* Section numbers are identifiers cited from code comments; renumbering §9.4–§9.6 would invalidate every citation. Reading order loses, referential integrity wins.
- *Adapter name `cursor`, binary `cursor-agent` — and the distinction is a trap, not a detail.* `cursor` resolves on PATH to the **editor launcher** (`…\Programs\cursor\resources\app\bin\cursor.cmd` on this machine); resolving it would spawn a GUI and hang the step. `binaryName` is `cursor-agent` and nothing else; the config key and workflow `agent:` value stay `cursor`.
- *Invocation:* `cursor-agent -p --output-format stream-json --trust`, cwd = worktree, **prompt via stdin** (verified: the echoed `user` line carries piped text — the Windows 8 KB argv rule holds without a fight). Full-auto adds `--force`; restricted adds `--sandbox enabled`. `--trust` rides in **both** modes: every task runs in a worktree the CLI has never seen, and a trust prompt in a headless run is a hang, not a question. `--worktree`/`--worktree-base` are never passed — worktrees belong to vincent (§10).
- *`--force` is `--dangerously-skip-permissions` wearing a friendlier name.* §16 gains a line saying so; the mildness of the spelling is exactly why it is called out.
- *Restricted mode fails on Windows instead of degrading (found while verifying the pinned argv against the real CLI, 2026-08-11).* `--sandbox enabled` exits 1 with "Sandbox requires macOS or Linux" before doing any work, so `buildArgs` returns `ErrRestrictedUnsupported` and the step never starts. Falling back to `--force` was rejected outright: it would run full-auto a step that explicitly asked not to be, on exactly one OS, which is the failure nobody thinks to check for. `--auto-review` is not a substitute (it prompts for whatever its classifier won't clear — a hang, headless — and is account-gated), nor is allowlist mode (global `cli-config.json` state, no per-run flag). `sandboxAvailable` is a package var, not a build tag, so both legs are tested on every OS CI runs.
- *Effort is not supported, and that is honest rather than lossy.* Cursor has no effort flag: reasoning depth is encoded in the model id (`claude-sonnet-5-thinking-xhigh`) or in a per-model bracket override whose parameter name **varies by model** (`effort` in the help example, `reasoning` in the gpt-5.4 family's own `modelParameters`). Composing `--model 'X[effort=Y]'` was rejected: an invalid bracket is a run-time `ActionRequiredError`, i.e. a burned worktree and a retry, where an empty catalog is a validation-time answer. `Efforts` is empty (mirroring codex's empty `Models`), the adapter ignores `RunSpec.Effort`, and §8.2 already errors on a claude/codex effort value reaching a cursor step.
- *Models are probed and explicitly untrusted.* `cursor-agent models` enumerates ~180 ids (source `cli`) over an **authenticated network call** — the first probe in vincent that is not a pure function of the binary, so §9.6's "never stale by construction" gains an exception paragraph. Worse, the list is over-broad: `gpt-5.4-nano-low` was listed and then rejected at run time with `AI Model Not Found`. Membership stays advisory in both directions; curated floor is `auto` alone.
- *`--model` is always passed, defaulting to `auto`.* Cursor persists the selection to `~/.cursor/cli-config.json`, so an omitted `--model` means "whatever the last invocation chose" — possibly a *previous vincent step*. Passing it always makes runs deterministic and makes cursor the first adapter with a non-empty `DefaultModel`, which the T4.7 `/v1/resolve` level-4 seam reports with no further change. Accepted cost, documented in §9.7 and §16: a cursor step overwrites the user's saved interactive model selection. Determinism beats preserving an interactive preference; `auto` is at least cursor's own default.
- *Its own stream package; no sharing with claude.* The dialect is claude-**shaped** and not claude: `thinking/{delta,completed}` events claude has no analog for, tool names as the **object key** (`editToolCall` → `edit`), camelCase usage keys, `result.result` holding the concatenation of *every* assistant message rather than the last. Two dialects that merely rhyme sharing a parser is how both break. `thinking` normalizes to `unknown` — transcripted verbatim, never surfaced live, because token-level reasoning deltas bury the assistant text the tail exists to show.
- *Errors arrive on stderr, not in the stream.* A bad model id exits 1 with no `result` line at all. Codex's `tailWriter` + "stream ended without a result event" is therefore load-bearing here, not defensive: it is the everyday shape of a typo'd model.
- *`supports_input: false`, verbatim version string.* No input-format flag and no control channel exist, so cursor steps never enter `awaiting_input` and `on_input` has no effect (codex precedent). The version is calver + sha (`2026.08.04-aaa8809`) and is recorded whole — there is no semver gate to parse it into, and the sha is part of the binary's identity.
- *"Zero core changes" (§9.1) held for the daemon and broke at two edges,* both recorded as tasks rather than absorbed silently: the §15 picker renders every option with no viewport (fine for 3 models, not 180), and `cmd/fakeagent` picks its dialect from argv shape, which cursor's claude-shaped argv collides with. **The fakeagent discriminator becomes `--trust`** — cursor is the only adapter that passes it, it is passed in both permission modes, and it keeps argv faithful to the real CLIs (the rule becomes: `exec` first ⇒ codex, else `--trust` ⇒ cursor, else claude).
- *`logged_in` graduates from a Go field to the wire.* `Availability.LoggedIn` has existed since M1 and is surfaced nowhere; cursor is the first adapter that can answer it cheaply (`cursor-agent status`). "Installed, version-probes clean, fails every run at the API" is otherwise indistinguishable from healthy — a genuinely new failure mode, so it becomes a nullable `logged_in` on `GET /v1/agents` and `/v1/info`, with the new-task form flagging it like an unavailable agent.
- *The picker window is fixed at 10 rows, not derived from terminal height (T5.4).* The picker is embedded in a form with rows above and below it, so a height-filling list would be wrong even on a tall terminal; a fixed window keeps the hint line, the error line and the rest of the form on screen at 80×24. Filtering is `/` — the board's and projects view's existing convention — and searches the note as well as the label, because provenance (`cli`/`curated`) is on screen and therefore searchable as far as a user is concerned. `esc` clears a filter before it closes the picker: escaping a typo must not discard the selection. **`capturesInput` had to learn about the filter in two places** (`newTask` and `projectForm`) or a `q` typed into it would quit the TUI — the identical M3 gate finding the code comment one row above already records.
- *Fixtures are captured from real runs and sanitized.* The probe streams (success, tool-use, invalid-model failure) land in `internal/agent/cursor/testdata/` named with the CLI version, per the existing convention — with absolute paths, session ids, and account identifiers scrubbed. In `tools_2026.08.04.jsonl` the `tool_call/completed` payloads and closing messages are **reconstructed** to their documented success shape: the capture machine had a user-level Cursor hook that rejected every tool call, an artifact of that machine rather than of the CLI. The `started` lines — the only ones the adapter normalizes — are verbatim, and the test file header says all of this.
- *The three-OS matrix earned its keep immediately (2026-08-11).* `TestDaemonViewReflectsLiveDaemon` passed on Windows and Linux and failed on macOS alone: the adapters block elides to the pane width, macOS temp paths (`/var/folders/df/djsxfhc…/T/…`) are far longer, and "not logged in" was cut mid-word. Two fixes, because both sides were wrong — the renderer had put a *blocking* condition after the absolute path, making it the first thing lost on a narrow terminal, so it now leads the row; and the test asserts the marker on the cursor row rather than the phrase, since the marker sits where nothing can push it off. A test asserting on elidable text is a test asserting on terminal width.

- [x] **T5.1 — Cursor adapter package.** ✓ 2026-08-11 `internal/agent/cursor`: `Detect` (`--version`, verbatim calver+sha; `status` for `logged_in`), `Path`, `Options`/`Curated` (probe `models`, curated floor `auto`, empty efforts), `Start`/`RunHandle` (stdin prompt, tree-kill via `procx`, stderr tail), `NewLineParser` (§13.2 normalized replay), and `doc.go` citing §9.7.
  *Done when:* table-driven parser tests pass against the captured `testdata/*.jsonl`, including the no-`result` stderr path; adapter tests run green on all three OSes.
  *2026-08-11:* code complete on `feat/cursor-adapter`. Verified locally on Windows: full suite green (895 tests), `go run mage.go lint` clean, and the pinned full-auto argv driven against the **real** cursor-agent 2026.08.04-aaa8809 end to end (exit 0, `result/success`). Fixtures captured from real runs and sanitized. **Remaining:** CI green on macOS and Linux, which is the only leg of the done-criterion a Windows box cannot supply.
- [x] **T5.2 — fakeagent cursor dialect.** ✓ 2026-08-11 Third dialect selected by `--trust` on argv (`exec` ⇒ codex, `--trust` ⇒ cursor, else claude); `FAKEAGENT_DIALECT=cursor` for the `--version` shape; a `models` subcommand for the options probe; the existing scenarios (`success`, `error-event`, `nonzero-exit`, `hang`, `big-usage`, `sleep`, `FAKEAGENT_DELAY_MS`, `FAKEAGENT_EDIT_FILE`) emitting cursor-shaped events, plus `no-result` for the stderr-only failure. Input scenarios are deliberately absent — `supports_input: false`.
  *Done when:* `internal/agent/cursor` tests drive fakeagent end to end and the three dialects are covered by one discriminator test each. *Depends: T5.1*
  *2026-08-11:* `cmd/fakeagent/cursor.go` plus `dialect_test.go` — all four discriminator legs (codex, cursor full-auto, cursor restricted, claude), the `FAKEAGENT_SCENARIO_CURSOR` override, and the `models`/`status` probe subcommands. Pending the same CI legs as T5.1.
- [x] **T5.3 — Wiring.** ✓ 2026-08-11 `agents.cursor.path` in `internal/config`, the `config.yaml` bootstrap template, registry construction in `daemon.go`, and the `agentPaths` map in `api/server.go`. Registration order `claude, codex, cursor`; `DefaultAgent` stays `claude`.
  *Done when:* `GET /v1/agents` reports three adapters, config hot-reload reaches a future cursor run, and a workflow with `agent: cursor` validates. *Depends: T5.1*
  *2026-08-11:* wired; existing api/config/workflow suites pass unchanged. **Remaining:** an explicit assertion that `GET /v1/agents` lists three adapters — the current tests cover the shape, not the count.
- [x] **T5.4 — Picker viewport and filter.** ✓ 2026-08-11 `internal/tui` `picker` grows a scrolling window with a position indicator and incremental type-to-filter; the free-text row survives filtering.
  *Done when:* a 180-option catalog is navigable in an 80×24 terminal, covered by TUI unit tests at small heights; existing picker tests still pass unchanged.
  *2026-08-11:* landed. `pickerWindow` = 10 rows with ▲/▼ counts, `/` opens an incremental filter over label **and** note, cursor indexes matches rather than options. **Deviation from the done-when, stated rather than quietly absorbed:** the window is a fixed 10 rows, not derived from terminal height, so there are no "small heights" to test — the picker is embedded in a form with rows above and below, so a height-filling list would be wrong even on a tall terminal. 7 new unit tests (windowing, scroll-follow, filter→select, free-row survival, esc-clears-before-close, mode split, preselection); all 259 pre-existing TUI tests pass unchanged.
- [x] **T5.5 — `logged_in` on the wire.** ✓ 2026-08-11 Nullable `logged_in` on `GET /v1/agents` and `GET /v1/info`, plumbed from `Availability.LoggedIn` through `apiclient`; the new-task form flags "installed, not authenticated" alongside "unavailable".
  *Done when:* handler tests pin `null` for claude/codex and a definite boolean for cursor, and a live TUI test asserts the flag. *Depends: T5.1, T5.3*
  *2026-08-11:* landed. `apiclient.Agent.NotAuthenticated()`/`Unusable()` carry the rule once; the agent picker, the daemon view (⚠ + "not logged in" instead of a contradictory ✓) and the board summary all read it. Handler tests pin all three states; `TestDaemonViewReflectsLiveDaemon` grew a third adapter that resolves but is logged out, so the flag is proven through a real probe, real handlers and the real client.
- [x] **T5.6 — Docs & examples.** ✓ 2026-08-11 §9.7 reflected in README (cursor as a supported agent, `cursor-agent` install pointer, the `--model` config side effect), `docs/workflows.md` (effort belongs in the model id for cursor), and one shipped example using `agent: cursor`.
  *Done when:* the example validates via `vincent workflow validate` in CI. *Depends: T4.4, T5.3*
  *2026-08-11:* README leg done — an "Agent CLIs" table naming all three adapters and their binaries, plus the three cursor-specific caveats a user will otherwise meet the hard way (effort lives in the model id, `--model` overwrites their saved CLI selection, `restricted` needs macOS or Linux).
  *2026-08-11 (second pass):* `examples/cursor-review.yaml` ships, and `TestShippedExamplesValidate` parses every file in `examples/` against the curated catalogs — at the time a deliberate substitution for the done-when, which names `vincent workflow validate` from T4.2. It earned its keep immediately, catching two real defects in the example on its first two runs: a plain YAML scalar cannot contain `": "`, and `check` is a *field* on agent/command steps rather than a step type.
  *2026-08-11 (third pass):* **done-when met literally.** T4.2 landed the same day, so `TestCommandsAgainstLiveDaemon` now runs `vincent workflow validate` over every file in `examples/` through the real binary, on all three OSes in CI. The unit test stays as the fast guard that needs no built binary; the CLI test is the contract. `docs/workflows.md` is **not** part of this task's done-when and remains T4.4's to write.
- [x] **T5.7 — Phase gate (M5 acceptance).** ✓ 2026-08-12 Walkthrough doc in the `m3-gate.md` precedent, hand-run against the **real** `cursor-agent` on Windows 11 and macOS, Linux covered by the fakeagent-backed gate in CI: a cursor-agent workflow runs to completion, a deliberately invalid model surfaces the stderr tail, an unauthenticated CLI is flagged before the task is created, and a `restricted` step is refused on Windows while succeeding on macOS.
  *Done when:* all three legs recorded here. **M5 complete.** *Depends: T5.1–T5.6*
  *2026-08-11:* `scripts/m5-gate.sh` (4 assertive curl scenarios, m2-gate precedent — M5's acceptance is a daemon running a task, so it is scripted rather than an m3-style human walkthrough) plus `docs/gates/m5-gate.md` for the real-CLI legs and the recorded-runs table. Added to CI on all three OSes. **Results so far — all four scenarios pass on Windows against fakeagent; scenario 2 passes against the *real* cursor-agent** (invalid model, no `result` event, stderr tail in the step record). **Scenario 1 against the real CLI is blocked environmentally, not by a defect:** `--version`, `status` and the `models` probe all answered from the real binary and the agent step itself succeeded, but two local `pretooluse` hooks are registered in PowerShell syntax while cursor-agent runs hooks through bash, so every edit is rejected and the commit step finds nothing to commit. The gate now detects that exact shape and says so. **Remaining:** fix or remove those hooks and re-run scenario 1; run scenarios 1, 2 and 4 on macOS.
  *2026-08-12 — that diagnosis was one layer short, and the remaining work changes with it.* There are no hooks to go and fix: `~/.cursor/cli-config.json` registers none, there is no `~/.cursor/hooks.json`, and no project-level `.cursor/`. What actually differs between a failing run and a working one is **the shell the daemon was launched from** — vincent hands its own environment to the agent unchanged, Git Bash exports `SHELL` to native children as a real Windows path where PowerShell leaves it unset, and the hook chain picks both the shell it *runs* a hook with and the syntax it *writes* that hook in from that one variable. The general defect is **T4.23**; the gate's own answer is to record the launch context, since a leg recorded without it is not reproducible. `m5-gate.sh` now prints `SHELL`/`MSYSTEM`/`COMSPEC` as its first line, scenario 1's failure message points at the shell before the hooks, and `m5-gate.md` carries a "Launched from" column. **Remaining:** re-run scenario 1 from PowerShell; run scenarios 1, 2 and 4 on macOS.
  *2026-08-12, later — **the note above is wrong** and is kept only because this file does not delete its history.* Re-run from PowerShell with `SHELL` provably not exported, scenario 1 failed **identically**. The launching shell was never the variable, and "no hooks are registered" was an artifact of the search, not a fact: `~/.cursor` carries a `.gitignore` whose body is `*`, and ripgrep honours it, so a tree that was never read came back as a tree that was empty. Absence of evidence was recorded as evidence of absence, and the day that cost was spent chasing the wrong layer.
  *The actual cause: **Cursor imports Claude Code's hooks**.* Cursor's hook log looks for `~/.cursor/hooks.json`, finds none, and loads `~/.claude/settings.json` instead — `cursor-agent` does the same (`claudeUserHooks`, `claudeProjectHooks`, `claudeProjectLocalHooks` in its bundle). It wraps each imported command in a PowerShell preamble, writing the payload to `%TEMP%\cursor-hooks-XXXXXX\`, then evaluates that string with **bash**: composed for one shell, executed by another, so every imported hook errors and an erroring hook blocks the tool call. The two blockers on the reporting machine were `rtk hook claude` and a context-mode Claude *plugin* hook. Nothing was registered on the Cursor side, which is why the original write-up could not name a config file — the file was `~/.claude/settings.json`. A Cursor bug on Windows; vincent is not involved beyond passing its environment through. [Reported upstream 2026-08-12](https://forum.cursor.com/t/cursor-agent-windows-imported-claude-code-hooks-are-composed-as-powershell-but-executed-with-bash-silently-blocking-every-tool-call/168129) — when it is fixed, the scratch-home procedure in `m5-gate.md` is deleted, not kept.
  *Scenario 1 **passes** against the real `cursor-agent` (Windows 11, `2026.08.11-e8db854`).* The remedy needs nothing disabled: run the daemon with `USERPROFILE`/`HOME` pointed at a scratch home holding a junction to the real `.cursor` and no `.claude`. Verified at two levels — the bare CLI probe went from 3 rejections and no file written to **0 and `hi.txt` created**, then the full gate passed every assertion including *the branch carries the edit*, the one failing since 2026-08-11. Recorded in `m5-gate.md` with the procedure; the banner now reports `USERPROFILE` (the variable that actually decides the outcome) and resolves `cursor-agent` through `cmd.exe`, since a `.cmd` shim is invisible to a bash `command -v` while Go's `exec.LookPath` honours `PATHEXT`. **Remaining: the macOS legs, and nothing else.**
  *2026-08-12, closing — **M5 is complete**, and the trigger is one variable.* macOS (owner, `2026.08.11-e8db854`): all four scenarios pass against the fakeagent and scenarios 1 and 2 against the real CLI, with **no prerequisite dance** — including scenario 4's other half, `restricted` genuinely *running* rather than being refused, which no other platform can prove. With Windows already recorded and Linux covered by CI, every leg of the done-when is met.
  *The Windows blocker narrows to `MSYSTEM`, and the earlier explanations were each one layer short.* Isolated on a fresh registry-derived environment plus exactly one addition: clean → file written, 0 blocks; clean + `MSYSTEM=MINGW64` → **no file, 5 blocks**; clean + Git Bash on `PATH` without `MSYSTEM` → file written, 0 blocks. Not `PATH`, not the availability of bash, not `SHELL` — `MSYSTEM` alone, which Cursor evidently reads to decide it should run a hook through bash, and which it composes for PowerShell. **It cannot be unset from inside Git Bash**: the MSYS runtime injects it into every child's environment block, so `env -u MSYSTEM` and `unset MSYSTEM` both leave a native child seeing `MINGW64` (verified; `env -u` was tried against the real gate and still failed). That is why the gate — a bash script — could never pass on Windows without the scratch home, and why the owner's original "works from PowerShell, not from Git Bash" was right from the first message: every later "PowerShell" test still had bash as the daemon's parent.
  *And the shipped configuration was never affected.* An installed, logon-started daemon (Scheduled Task, no `MSYSTEM`) ran a cursor workflow to `done`: 28 `editToolCall`s, **zero** hook blocks, a real commit on the branch. The prediction that the installed daemon would be silently crippled was wrong — see **T4.23**.

- [x] **T5.8 — `restricted_unsupported` as its own failure reason.** ✓ 2026-08-11 Discovered while writing the M5 gate: a restricted step refused for lack of a sandbox was reported as `agent_unavailable`, which sends the user to reinstall a CLI that is installed and healthy. `agent.ErrRestrictedUnsupported` is a sentinel in the adapter **interface** package — not in `agent/cursor`, or `taskrun` would depend on a concrete adapter — that any adapter can return from `Start`; the engine classifies it as the new `ReasonRestrictedUnsupported`. `cursor.SetSandboxAvailable` lets other packages exercise both legs on every OS instead of the Windows leg being the only one ever executed.
  *Done when:* the engine records the distinct reason and the gate asserts it. *2026-08-11:* `TestEngineRestrictedUnsupportedIsItsOwnReason` (blocked task, block reason, step failure reason, **and that no process was spawned**), m5-gate scenario 4, spec §9.4 and §18.

- [x] **T5.9 — Engine-level cursor coverage.** ✓ 2026-08-11 Also from the gate work: `internal/taskrun` had adapter-level tests for cursor and API/TUI tests above it, but nothing proving the *engine* runs a cursor step end to end — the seam where the resolved triple, the stream normalization and the recorded usage meet. `TestEngineRunsCursorStep` mirrors `TestEngineRunsCodexStep`: rendered prompt over stdin, `agent=cursor`, model recorded, **effort empty**, tokens from the camelCase keys, cost nil.
  *Done when:* the test passes on all three OSes in CI.
